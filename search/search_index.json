{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pyForwardFolding","text":"<p>AI-generated documentation \u2014 provisional</p> <p>This documentation site was drafted by an AI assistant by reading the source code. It is a first pass and will be reviewed and rewritten by a human maintainer at a later date. Individual claims may be wrong, incomplete, or out of date \u2014 when in doubt, cross-check against the source under <code>pyForwardFolding/</code> and the examples under <code>examples/</code>.</p> <p>pyForwardFolding is a JAX-based Python framework for forward-folding analyses of binned data, originally developed for neutrino-telescope physics but applicable to any analysis that builds a predicted histogram from weighted Monte Carlo events and fits it to data.</p> <p>It provides:</p> <ul> <li>A compositional model description: factors combine into components, which combine into models.</li> <li>A binning layer that turns weighted per-event predictions into histograms.</li> <li>A YAML-driven configuration system so analyses can be defined declaratively, without writing Python.</li> <li>Built-in likelihoods (Poisson and SAY) with Gaussian / uniform priors.</li> <li>Minimizers (SciPy L-BFGS-B and iMinuit) and a statistics module for hypothesis tests,   pseudo-experiments, Asimov fits, discovery potential, and parameter scans.</li> <li>A JAX backend so the full forward model is differentiable and JIT-compilable.</li> </ul>"},{"location":"#at-a-glance","title":"At a glance","text":"<pre><code>import pyForwardFolding as pyFF\n\n# 1. Build the analysis from a YAML config\nana = pyFF.config.analysis_from_config(\"test.yaml\")\nparams, priors = pyFF.config.params_from_config(\"test.yaml\")\n\n# 2. Evaluate the predicted histograms for some data\npredicted, _ = ana.evaluate(data, params)\n\n# 3. Fit\nllh   = pyFF.likelihood.PoissonLikelihood(ana, priors)\nmini  = pyFF.minimizer.ScipyMinimizer(llh)\nresult, best_fit, fmin = mini.minimize(observed, data)\n</code></pre>"},{"location":"#where-to-go-next","title":"Where to go next","text":"<ul> <li>New to the framework? Start with Getting started \u2192 Installation   and the Quickstart.</li> <li>Want to understand the math and class structure? Read Concepts \u2192 Overview.</li> <li>Looking up what a particular factor does? See the Factor reference \u2014   every factor is documented with its required input variables and exposed parameters.</li> <li>Writing your first YAML config? Jump to Configuration \u2192 Overview.</li> </ul>"},{"location":"#project-layout","title":"Project layout","text":"<pre><code>pyForwardFolding/\n\u251c\u2500\u2500 pyForwardFolding/      # the library\n\u2502   \u251c\u2500\u2500 factor.py          # all per-event and per-bin Factor classes\n\u2502   \u251c\u2500\u2500 model_component.py # ModelComponent: product of factors\n\u2502   \u251c\u2500\u2500 model.py           # Model: sum of components\n\u2502   \u251c\u2500\u2500 binning.py         # AbstractBinning, RectangularBinning, ...\n\u2502   \u251c\u2500\u2500 binned_expectation.py\n\u2502   \u251c\u2500\u2500 analysis.py        # Combines binned expectations\n\u2502   \u251c\u2500\u2500 likelihood.py      # PoissonLikelihood, SAYLikelihood, priors\n\u2502   \u251c\u2500\u2500 minimizer.py       # ScipyMinimizer, MinuitMinimizer\n\u2502   \u251c\u2500\u2500 statistics.py      # Hypothesis, HypothesisTest, pseudo-experiments\n\u2502   \u251c\u2500\u2500 config.py          # YAML \u2192 Analysis / dataset / parameters\n\u2502   \u251c\u2500\u2500 backend.py         # JAX backend abstraction\n\u2502   \u2514\u2500\u2500 clustering.py      # Event clustering utilities\n\u251c\u2500\u2500 examples/              # Worked examples (notebooks + YAML)\n\u2514\u2500\u2500 tests/\n</code></pre>"},{"location":"concepts/binning/","title":"Binning","text":"<p>A binning is the bridge between per-event predictions and a histogram. Every <code>BinnedExpectation</code> owns one <code>AbstractBinning</code> object.</p>"},{"location":"concepts/binning/#rectangularbinning","title":"RectangularBinning","text":"<p>The standard choice. An n-dimensional rectangular grid; each dimension is bound to one variable name and has its own list of bin edges.</p> <pre><code>from pyForwardFolding.binning import RectangularBinning\nimport jax.numpy as jnp\n\nbinning = RectangularBinning(\n    bin_variables=(\"log10_reco_energy\", \"cos_reco_zenith\"),\n    bin_edges=(jnp.linspace(2, 7, 31), jnp.linspace(-1, 0, 25)),\n)\n</code></pre> <p>In YAML:</p> <pre><code>binning:\n  type: RectangularBinning\n  bin_vars_edges:\n    - [\"log10_reco_energy\", \"linear\", [2, 7, 31]]   # linspace(2, 7, 31)\n    - [\"cos_reco_zenith\",   \"linear\", [-1, 0, 25]]\n    - [\"psi_deg\",           \"array\",  [0, 1, 3, 10, 30]]   # custom edges\n</code></pre> <p>Each entry is <code>[variable_name, edge_type, edges]</code>:</p> <ul> <li><code>linear</code>: <code>edges</code> is <code>[start, stop, num]</code>, expanded with <code>linspace</code>.</li> <li><code>array</code>: <code>edges</code> is the literal list of edges.</li> </ul> <p>Events outside any dimension's edges are masked out and contribute zero weight.</p>"},{"location":"concepts/binning/#bin-index-caching","title":"Bin-index caching","text":"<p><code>RectangularBinning</code> caches the per-event bin indices keyed by dataset name, so the bin assignment is computed once per dataset and re-used across every minimizer call. This is one of the main reasons fits are fast. Call <code>binning.clear_bin_indices(ds_key)</code> if you swap a dataset out in place.</p>"},{"location":"concepts/binning/#rectangularbinning2dto3d","title":"RectangularBinning2DTo3D","text":"<p>A specialization that takes 2D binning variables and inflates the resulting histogram to 3D by repeating along a third axis (useful when comparing against a 3D template). The bin edges along the third dimension are set at construction time but the events themselves are not binned in that dimension \u2014 every event is uniformly spread across the third axis.</p>"},{"location":"concepts/binning/#relaxedbinning","title":"RelaxedBinning","text":"<p>A differentiable, soft binning using a tanh kernel:</p> \\[ K(x; a, b) = \\tfrac{1}{2}\\left(1 + \\tanh\\!\\tfrac{x-a}{s}\\,\\tanh\\!\\tfrac{-(x-b)}{s}\\right) \\] <p>This gives each event a smooth membership weight in each bin rather than a hard 0/1, which is sometimes useful when fitting against bin boundaries that themselves depend on parameters.</p> <p>Note: <code>RelaxedBinning</code> is implemented in the codebase but the constructor currently raises <code>NotImplementedError</code>. If you need soft binning today, treat it as experimental.</p>"},{"location":"concepts/binning/#binning-vs-binned-factors","title":"Binning vs. binned factors","text":"<p>It's worth separating two concepts that both end up \"per bin\":</p> <ol> <li>Binning decides how an event becomes a histogram cell.</li> <li>Binned factors (subclasses of <code>AbstractBinnedFactor</code>) live on a <code>BinnedExpectation</code> and are    added to the resulting histogram, after the histogram is built. They are how per-bin    detector systematics enter the model.</li> </ol> <pre><code>hist = binning.build_histogram(weights \u00d7 model_weights) * lifetime\n     + sum( binned_factor.evaluate(...) * lifetime, over binned_factors )\n</code></pre> <p>See <code>PerBinPolynomial</code>, <code>SnowStormGradient</code>, and <code>ScaledTemplate</code> for concrete binned factors.</p>"},{"location":"concepts/factors/","title":"Factors","text":"<p>A factor is the smallest building block of a model. It takes a dictionary of per-event input variables and a dictionary of parameter values, and returns a per-event multiplicative weight.</p>"},{"location":"concepts/factors/#the-two-kinds-of-factor","title":"The two kinds of factor","text":"<pre><code>AbstractFactor\n\u251c\u2500\u2500 AbstractUnbinnedFactor   \u2190 operates on per-event MC, returns a per-event array\n\u2514\u2500\u2500 AbstractBinnedFactor     \u2190 evaluated after histogramming, returns a per-bin (mean, variance) tuple\n</code></pre> <p>Unbinned factors (the vast majority) are what you compose inside a <code>ModelComponent</code>. They are multiplied together to produce the per-event component weight.</p> <p>Binned factors (e.g. <code>SnowStormGradient</code>, <code>ScaledTemplate</code>, <code>PerBinPolynomial</code>) live on the <code>BinnedExpectation</code> and are added to the histogram after it has been built. They typically parametrize a per-bin systematic correction that is expensive to evaluate per event but cheap to evaluate per bin.</p>"},{"location":"concepts/factors/#anatomy-of-a-factor","title":"Anatomy of a factor","text":"<p>Every concrete factor class exposes two key attributes:</p> <ul> <li><code>factor_parameters: List[str]</code> \u2014 the internal names of the parameters it consumes.</li> <li><code>required_variables: List[str]</code> \u2014 the keys it expects to find in the input variable dictionary.</li> </ul> <p>For example, <code>PowerLawFlux</code> declares:</p> <pre><code>self.factor_parameters = [\"flux_norm\", \"spectral_index\"]\nself.req_vars = [\"true_energy\"]\n</code></pre> <p>When evaluated, it pulls <code>true_energy</code> from the dataset and <code>flux_norm</code> / <code>spectral_index</code> from the parameter dict (after applying any rename from <code>param_mapping</code>), and returns</p> \\[ F = \\text{flux\\_norm} \\cdot \\text{baseline\\_norm} \\cdot \\left(\\tfrac{E}{E_{\\text{pivot}}}\\right)^{-\\gamma}. \\]"},{"location":"concepts/factors/#parameter-renaming-and-fixing","title":"Parameter renaming and fixing","text":"<p><code>param_mapping</code> is the bridge between factor-internal parameter names (which the factor's mathematics uses) and globally exposed parameter names (which appear in the fit).</p> <pre><code>- name: \"powerlaw_astro\"\n  type: \"PowerLawFlux\"\n  pivot_energy: 1.0e5\n  baseline_norm: 1.0e-18\n  param_mapping:\n    flux_norm: \"astro_norm\"     # rename \u2192 exposed as astro_norm\n    spectral_index: \"astro_idx\" # rename \u2192 exposed as astro_idx\n\n- name: \"powerlaw_atmo\"\n  type: \"PowerLawFlux\"\n  pivot_energy: 1.0\n  baseline_norm: 1.4e-5\n  param_mapping:\n    flux_norm: \"atmo_norm\"      # different exposed name\n    spectral_index: 2.7         # numeric \u2192 fixed, not in the fit\n</code></pre> <p>Rules:</p> Value type in <code>param_mapping</code> Effect <code>str</code> Rename \u2014 parameter is exposed under the given name <code>int</code> / <code>float</code> (not <code>bool</code>) Fix \u2014 parameter is held at this constant and not exposed (key not listed) Identity \u2014 parameter is exposed under its factor-internal name <code>param_mapping=None</code> All parameters are exposed under their factor-internal names <p>This is why two <code>PowerLawFlux</code> factors can coexist in the same model: just give them different <code>param_mapping</code> values.</p>"},{"location":"concepts/factors/#composition","title":"Composition","text":"<p>Inside a <code>ModelComponent</code>, factors are multiplied in evaluation order:</p> <pre><code>component_weight_i = \u220f_f F_f(X_i, \u03b8_f)\n</code></pre> <p>That means order doesn't matter mathematically, but giving factors meaningful names helps with debugging and with reading the <code>repr_markdown()</code> output.</p> <p>Inside a <code>Model</code>, components are summed, each multiplied by its baseline weight (the inverse-generation weight column of your MC):</p> <pre><code>event_weight_i = w_i \u00b7 \u03a3_C m_C(X_i, \u03b8_C)\n</code></pre> <p>where the baseline weight column is named per component, so you could in principle give different components different baseline weights (e.g. <code>astro_baseline_weight</code> vs. <code>atmo_baseline_weight</code>) for samples generated under different assumptions.</p>"},{"location":"concepts/factors/#built-in-factors-at-a-glance","title":"Built-in factors at a glance","text":"<p>The full catalogue lives at Factor reference. At time of writing the library ships:</p> <ul> <li>Fluxes: <code>PowerLawFlux</code>, <code>BrokenPowerLawFlux</code>, <code>FluxNorm</code>, <code>FlavorRatio</code></li> <li>Atmospheric shape: <code>GaisserZenithFactor</code></li> <li>Galactic plane: <code>GalacticPlaneBox</code>, <code>SegmentedPlane</code></li> <li>Atmospheric self-veto: <code>VetoThreshold</code>, <code>FixedVeto</code></li> <li>Reweighting / interpolation: <code>DeltaGamma</code>, <code>ModelInterpolator</code>, <code>GradientReweight</code>,   <code>ClassifierGradientReweight</code>, <code>SnowstormGauss</code></li> <li>Cuts: <code>SoftCut</code></li> <li>Binned (per-bin): <code>SnowStormGradient</code>, <code>ScaledTemplate</code>, <code>PerBinPolynomial</code></li> </ul> <p>Adding a new factor is a small amount of code (one subclass with <code>evaluate</code>, <code>construct_from</code>, and the two attribute lists). See How-to \u2192 Custom factor.</p>"},{"location":"concepts/fitting/","title":"Fitting and statistics","text":""},{"location":"concepts/fitting/#minimizers","title":"Minimizers","text":"<p>Both minimizers wrap an <code>AbstractLikelihood</code> and flatten the parameter dictionary into a vector for the underlying optimizer.</p>"},{"location":"concepts/fitting/#scipyminimizer-l-bfgs-b","title":"ScipyMinimizer (L-BFGS-B)","text":"<p>Default, simple, gradient-based via <code>scipy.optimize.minimize</code>. Uses JAX <code>value_and_grad</code> to provide analytic gradients to L-BFGS-B.</p> <pre><code>from pyForwardFolding.minimizer import ScipyMinimizer\n\nmini = ScipyMinimizer(llh, tol=1e-10)\nresult, best_fit, fmin = mini.minimize(observed, data)\n</code></pre> <p>Pass <code>fixed_pars={\"astro_norm\": 1.5}</code> to hold parameters during the minimisation \u2014 this is how you do profile-likelihood scans.</p>"},{"location":"concepts/fitting/#minuitminimizer","title":"MinuitMinimizer","text":"<p>Wraps iminuit. Gives you Minuit's covariance estimate and the familiar MIGRAD output.</p> <pre><code>from pyForwardFolding.minimizer import MinuitMinimizer\n\nmini = MinuitMinimizer(llh, tol=1e-2, strategy=1, simplex_prefit=False)\ninfo, best_fit, fmin = mini.minimize(observed, data)\nprint(info[\"hess_inv\"])   # iminuit covariance\n</code></pre> <p><code>info</code> carries <code>success</code>, <code>message</code>, <code>nfev</code>, <code>njev</code>, <code>hess_inv</code>.</p>"},{"location":"concepts/fitting/#hypotheses","title":"Hypotheses","text":"<p>A <code>Hypothesis</code> bundles a likelihood, a set of fixed parameters, and a minimizer. It represents one parameter sub-space for a fit.</p> <pre><code>from pyForwardFolding.statistics import Hypothesis\n\nH0 = Hypothesis(\"null\",  llh, fixed_pars={\"astro_norm\": 0.0})  # background only\nH1 = Hypothesis(\"alt\",   llh)                                  # everything free\n</code></pre> <p><code>H.evaluate(observed, dataset)</code> runs the fit and returns the best-fit log-likelihood (with <code>detailed=True</code> you also get the minimizer info and the best-fit parameter dict). <code>H.generate_pseudo_experiments(nexp, dataset)</code> is a generator of Poisson-fluctuated histograms. <code>H.asimov_experiment(dataset)</code> returns the (un-fluctuated) Asimov histogram.</p>"},{"location":"concepts/fitting/#hypothesis-tests","title":"Hypothesis tests","text":"<p><code>HypothesisTest</code> wraps two hypotheses and a dataset and provides the standard frequentist machinery:</p> <pre><code>from pyForwardFolding.statistics import HypothesisTest\n\ntest = HypothesisTest(H0, H1, dataset)\nts   = test.test(observed)                       # \u22122 \u0394log L on one observation\nnull = test.null_dist(nexp=1000)                 # null TS distribution\nalt  = test.alt_dist(nexp=1000)                  # alternative TS distribution\npower = test.power(nexp=1000, sigma_level=3)     # P(reject H0 | H1)\n</code></pre> <p>For one-degree-of-freedom tests you also get:</p> <ul> <li><code>test.discovery_potential(nexp_null, nexp_alt, sigma_level=3)</code> \u2014 root-finds the value of the   free parameter at which the median TS under H1 equals the \u03c3-threshold of the null distribution.</li> <li><code>test.discovery_potential_asimov(sigma_level=3)</code> \u2014 same idea but using the Asimov asymptotic \u03c7\u00b2   shortcut (no pseudo-experiments).</li> <li><code>test.scan(observed, scan_points)</code> \u2014 \u22122 \u0394log L profile of the free parameter.</li> <li><code>test.uncertainty(observed, sigma_level)</code> \u2014 symmetric \u00b1\u03c3 uncertainty from the profile.</li> </ul>"},{"location":"concepts/fitting/#pseudo-experiments","title":"Pseudo-experiments","text":"<p><code>PseudoExpGenerator</code> evaluates the analysis once and then yields independent Poisson-fluctuated copies of the predicted histograms.</p> <pre><code>from pyForwardFolding.statistics import PseudoExpGenerator\n\ngen = PseudoExpGenerator(ana, dataset, parameter_values)\nfor fake_obs in gen.generate(nexp=1000):\n    ...\n</code></pre>"},{"location":"concepts/fitting/#fisher-information-and-covariance","title":"Fisher information and covariance","text":"<p><code>Analysis</code> itself can compute the Fisher information matrix (Poisson) and invert it for a covariance / variance estimate without doing a real fit:</p> <pre><code>fim = ana.fisher_information(datasets, parameter_values)   # {comp_name: matrix}\ncov = ana.covariance(datasets, parameter_values)            # {\"total\": ..., comp_name: ...}\nvar = ana.variance(datasets, parameter_values)              # {comp_name: {param_name: variance}}\n</code></pre> <p>This uses <code>jax.jacfwd</code> on the per-bin expectation and is differentiable end-to-end.</p>"},{"location":"concepts/likelihood/","title":"Likelihood and priors","text":"<p>Once an <code>Analysis</code> can predict histograms, the likelihood combines those predictions with observed counts and (optionally) priors on the parameters.</p>"},{"location":"concepts/likelihood/#available-likelihoods","title":"Available likelihoods","text":""},{"location":"concepts/likelihood/#poissonlikelihood","title":"PoissonLikelihood","text":"<p>The standard binned-Poisson log-likelihood (constant terms dropped):</p> \\[ \\log \\mathcal{L} = \\sum_A \\left[ -\\mu_A + n_A \\log\\mu_A - \\log(n_A!) \\right] \\] <p>evaluated per bin and summed. Empty-expectation bins are skipped by default (substitute a tiny \\(\\mu \\to 10^{-8}\\) to keep gradients finite); set <code>empty_bins=\"throw\"</code> to error instead.</p> <pre><code>from pyForwardFolding.likelihood import PoissonLikelihood, UniformPrior\n\npriors = [UniformPrior(prior_seeds, prior_bounds)]\nllh = PoissonLikelihood(ana, priors)\nvalue = llh.llh(observed_counts, datasets, parameter_values)\n</code></pre>"},{"location":"concepts/likelihood/#saylikelihood","title":"SAYLikelihood","text":"<p>The \"Arg\u00fcelles, Schneider, Yuan\" effective likelihood that accounts for limited MC statistics (arXiv:1901.04645). It marginalises the Poisson mean over a Gamma prior whose width comes from the per-bin MC sum-of-squared-weights:</p> \\[ \\alpha = \\tfrac{\\mu^2}{\\sigma_\\mu^2} + 1, \\quad \\beta = \\tfrac{\\mu}{\\sigma_\\mu^2} \\] \\[ \\log\\mathcal{L}_\\text{SAY} = \\alpha\\log\\beta + \\log\\Gamma(n + \\alpha) - \\log\\Gamma(n+1) - (n+\\alpha)\\log(1+\\beta) - \\log\\Gamma(\\alpha) \\] <p>Use this when MC statistics are limited relative to data \u2014 it's a drop-in replacement for <code>PoissonLikelihood</code>:</p> <pre><code>from pyForwardFolding.likelihood import SAYLikelihood\nllh = SAYLikelihood(ana, priors)\n</code></pre>"},{"location":"concepts/likelihood/#priors","title":"Priors","text":"<p>Every exposed parameter of the analysis must appear in at least one prior \u2014 the likelihood constructor checks this and raises <code>ValueError</code> on a mismatch.</p>"},{"location":"concepts/likelihood/#uniformprior","title":"UniformPrior","text":"<p>A dummy prior that contributes 0 to the log-likelihood but does carry bounds and seeds. Use this for any parameter you want to leave un-pulled.</p> <pre><code>from pyForwardFolding.likelihood import UniformPrior\n\nUniformPrior(\n    prior_seeds={\"astro_norm\": 1.0, \"astro_index\": 2.0},\n    prior_bounds={\"astro_norm\": (0.0, float(\"inf\")), \"astro_index\": (-float(\"inf\"), float(\"inf\"))},\n)\n</code></pre>"},{"location":"concepts/likelihood/#gaussianunivariateprior","title":"GaussianUnivariatePrior","text":"<p>A product of independent Gaussian pulls (un-normalised \u2014 only the quadratic term is added). Use this for nuisance parameters constrained to within some tolerance of a nominal value.</p> <pre><code>from pyForwardFolding.likelihood import GaussianUnivariatePrior\n\nGaussianUnivariatePrior(\n    prior_params={\"kaon_pion_ratio\": (0.213, 0.05)},  # (mean, std)\n    prior_seeds={\"kaon_pion_ratio\": 0.213},\n    prior_bounds={\"kaon_pion_ratio\": (0.0, 1.0)},\n)\n</code></pre> <p>The corresponding YAML keys are <code>prior_params_gauss</code>, <code>prior_seeds_gauss</code>, and <code>prior_bounds_gauss</code>, loaded automatically by <code>params_from_config</code>. Parameters that appear only in <code>prior_*</code> (uniform) get a <code>UniformPrior</code>; parameters that appear in <code>prior_*_gauss</code> get an additional <code>GaussianUnivariatePrior</code>.</p>"},{"location":"concepts/likelihood/#combining","title":"Combining","text":"<p>The total log-likelihood is the sum of the binned term and all prior log-PDFs:</p> \\[ \\log \\mathcal L_\\text{total} = \\log\\mathcal L_\\text{binned} + \\sum_p \\log p_\\text{prior}. \\] <p>A single fit may use multiple priors (e.g. one <code>UniformPrior</code> for the free parameters of interest plus one <code>GaussianUnivariatePrior</code> for nuisance pulls). The constructor concatenates seeds and bounds across priors for the minimizer.</p>"},{"location":"concepts/overview/","title":"Conceptual overview","text":""},{"location":"concepts/overview/#what-forward-folding-means-here","title":"What \"forward folding\" means here","text":"<p>You have a collection of Monte Carlo events with true properties \\(X = (E, \\theta, \\ldots)\\), reconstructed quantities \\(\\hat X = (\\hat E, \\hat\\theta, \\ldots)\\), and per-event weights \\(w_i\\) that encode the detector response so that the differential rate is</p> \\[ R(X) = w \\cdot M(X \\mid \\theta). \\] <p>\\(M(X \\mid \\theta)\\) is the differential flux model with free parameters \\(\\theta\\).</p> <p>A \"forward-folded\" prediction in bin \\(A\\) is obtained by re-weighting every MC event with the current model and histogramming by their reconstructed quantities:</p> \\[ \\mu_A(X, \\hat X, \\theta) = \\sum_i I_A(\\hat X_i) \\cdot w_i \\cdot M(X_i \\mid \\theta), \\] <p>where \\(I_A\\) is the bin indicator. This avoids fitting a parametric form to histogrammed data; instead the histogram itself is differentiable in \\(\\theta\\) through the per-event weights.</p>"},{"location":"concepts/overview/#how-the-math-maps-to-classes","title":"How the math maps to classes","text":"<p>A model is built compositionally:</p> \\[ M(X \\mid \\theta) = \\sum_C m_C(X \\mid \\theta_C), \\quad\\text{with}\\quad m_C(X \\mid \\theta_C) = \\prod_f F_{f,C}(X \\mid \\theta_f). \\] <p>The classes mirror this structure exactly:</p> Symbol Class Role \\(F(X \\mid \\theta_f)\\) <code>Factor</code> (subclasses of <code>AbstractUnbinnedFactor</code>) A single multiplicative per-event factor \\(m_C(X^D) \\cdot w_i^D \\cdot S^D(X^D)\\) <code>ModelComponent</code> Product of factors \\(M^D(X^D \\mid \\theta) \\cdot w^D_i\\) <code>Model</code> Sum of components, with one baseline-weight name per component \\(\\mu^D_A(X^D, \\hat X^D, \\theta)\\) <code>BinnedExpectation</code> Histograms one model over one binning and lifetime \\(S_A^D\\) <code>AbstractBinnedFactor</code> (e.g. <code>SnowStormGradient</code>, <code>ScaledTemplate</code>) Additive per-bin correction \\(\\hat\\mu^D_A(X^D, \\hat X^D, \\theta)\\) <code>Analysis</code> Collection of binned expectations (multi-detector) \\(\\mathcal L\\) <code>Likelihood</code> Evaluates the binned likelihood + priors"},{"location":"concepts/overview/#why-factor-composition","title":"Why factor composition?","text":"<p>The factor abstraction lets you build complicated flux models from small reusable pieces. For example, an atmospheric flux component might be the product of:</p> <ul> <li>a <code>PowerLawFlux</code> (spectral shape),</li> <li>a <code>GaisserZenithFactor</code> (atmospheric zenith dependence),</li> <li>a <code>VetoThreshold</code> or <code>FixedVeto</code> (atmospheric self-veto effect),</li> </ul> <p>while an astrophysical component might be a single <code>PowerLawFlux</code> or a <code>BrokenPowerLawFlux</code>. Both components reuse the same <code>PowerLawFlux</code> class with different configuration.</p> <p>Detector systematics that act per-event (e.g. ice optical-property reweighting via <code>SnowstormGauss</code>, <code>GradientReweight</code>, or <code>ClassifierGradientReweight</code>) are also just factors \u2014 they slot into a component multiplicatively without any special-casing.</p> <p>Systematics that act per-bin (e.g. SnowStorm gradients integrated over MC ensembles, or scaled template additions) are binned factors. They live on the <code>BinnedExpectation</code> rather than inside the <code>Model</code> and are added to the histogram after it has been built.</p>"},{"location":"concepts/overview/#parameter-exposure-and-naming","title":"Parameter exposure and naming","text":"<p>Every factor declares an internal list of <code>factor_parameters</code> (e.g. <code>[\"flux_norm\", \"spectral_index\"]</code>). When you build a factor you can pass <code>param_mapping</code> to control how those internal names appear in the global parameter dictionary that the likelihood sees:</p> <ul> <li>Rename: map an internal name to a string \u2014 e.g. <code>flux_norm: \"astro_norm\"</code> exposes the   parameter as <code>astro_norm</code>.</li> <li>Fix: map an internal name to a number \u2014 e.g. <code>spectral_index: 2.7</code> removes that parameter   from the free parameters of the fit and uses 2.7 directly.</li> <li>Default: parameters not listed in <code>param_mapping</code> are exposed under their original name.</li> </ul> <p>This is what makes two <code>PowerLawFlux</code> factors in the same model \u2014 one for astrophysical, one for atmospheric \u2014 coexist without name collisions.</p>"},{"location":"concepts/overview/#multi-detector-multi-sample-analyses","title":"Multi-detector / multi-sample analyses","text":"<p>A single <code>Analysis</code> holds a dict of named binned expectations. You can use this to:</p> <ul> <li>Combine multiple detectors (e.g. tracks and cascades, IceCube and KM3NeT).</li> <li>Combine multiple event selections / topologies within the same detector.</li> <li>Share parameters across samples \u2014 because each component pulls from the same flat parameter   dictionary, a parameter renamed <code>astro_norm</code> in two different factors is automatically the same   parameter at fit time.</li> </ul> <p>Each binned expectation has its own dataset, its own binning, its own lifetime, and optionally its own binned factors, but they all evaluate the likelihood jointly.</p>"},{"location":"concepts/overview/#the-fitting-story","title":"The fitting story","text":"<ol> <li>Choose a likelihood:    <code>PoissonLikelihood</code> (standard binned Poisson) or    <code>SAYLikelihood</code> (the Arg\u00fcelles, Schneider, Yuan likelihood that    accounts for limited MC statistics, arXiv:1901.04645).</li> <li>Attach priors. <code>UniformPrior</code> is a dummy (provides bounds but no log-PDF contribution);    <code>GaussianUnivariatePrior</code> adds Gaussian pulls.</li> <li>Pick a minimizer. <code>ScipyMinimizer</code> uses L-BFGS-B and is the    simplest option. <code>MinuitMinimizer</code> wraps iMinuit and exposes    covariance / MIGRAD diagnostics.</li> <li>For tests on top of fits, use the <code>statistics</code> module:    <code>Hypothesis</code>, <code>HypothesisTest</code>, <code>PseudoExpGenerator</code>, discovery potential, Asimov asymptotics.</li> </ol> <p>The full math (including detector systematics and multi-detector combination) is laid out in the README's model-structure section.</p>"},{"location":"configuration/components-and-models/","title":"Components and models","text":""},{"location":"configuration/components-and-models/#components","title":"<code>components:</code>","text":"<p>A component is a product of factors. Each entry lists factor names (defined under <code>factors:</code>) by name; the loader looks them up and groups them.</p> <pre><code>components:\n  - name: \"astro\"\n    factors: [\"powerlaw_astro\"]\n  - name: \"atmo\"\n    factors: [\"atmo_powerlaw\", \"atmo_zenith\"]\n</code></pre> <p>Factor names must be unique within a component.</p> <p>A <code>ModelComponent</code> is mathematically \\(m_C(X \\mid \\theta_C) = \\prod_f F_{f,C}(X \\mid \\theta_f)\\); see Concepts \u2192 Overview for the math.</p>"},{"location":"configuration/components-and-models/#models","title":"<code>models:</code>","text":"<p>A model is a sum of components, where each component is paired with a baseline-weight key naming the column in your dataset that holds the per-event generation weight for that component.</p> <pre><code>models:\n  - name: \"model\"\n    components:\n      - name: \"astro\"\n        baseline_weight: \"baseline_weight\"\n      - name: \"atmo\"\n        baseline_weight: \"baseline_weight\"\n</code></pre> <p>Multiple components can share a single baseline-weight column (as above) if the MC was generated under a common weighting scheme. If different components come from different MC sets with different generation weights, give them distinct baseline-weight column names.</p> <p>Mathematically: \\(M^D(X^D \\mid \\theta) \\cdot w^D_i\\) where the per-event \\(w_i^D\\) is the baseline weight named here.</p> <p>Component names within one model must be unique.</p>"},{"location":"configuration/components-and-models/#how-they-feed-into-histograms","title":"How they feed into histograms","text":"<p>The <code>histograms:</code> section ties one or more <code>(model, dataset)</code> pairs to a binning. So a single model can be evaluated against multiple datasets (multi-detector / multi-sample analyses) by listing several pairs in the <code>models:</code> field of a histogram entry:</p> <pre><code>histograms:\n  - name: combined\n    binning: ...\n    models:\n      - [\"model\", \"ic86_tracks\"]\n      - [\"model\", \"ic86_cascades\"]\n</code></pre> <p>See Histograms for details.</p>"},{"location":"configuration/datasets/","title":"Datasets","text":"<p>The <code>datasets:</code> section is optional. It lets the loader read DataFrames from disk and translate them into the dict-of-dicts format that <code>analysis.evaluate(...)</code> expects.</p> <p>If you don't use this section, you're responsible for building the dataset dict yourself (as in the Quickstart). Most analyses do build the dataset in Python rather than from the loader, because pre-processing and cuts usually live outside YAML.</p>"},{"location":"configuration/datasets/#schema","title":"Schema","text":"<pre><code>datasets:\n  - name: \"ic86_tracks\"                        # Used as the top-level dataset key\n    path: \"/path/to/events.parquet\"            # File on disk\n    param_mapping:                              # Map output keys (used by factors / binning)\n      true_energy:                              # ...to input column names with optional transforms\n        df_key: \"energy_true\"\n      log10_reco_energy:\n        df_key: \"energy_reco\"\n        transform: \"log10\"\n      cos_reco_zenith:\n        df_key: \"zenith_reco\"\n        transform: \"cos\"\n      baseline_weight:\n        df_key: \"weight\"\n    # Optional: precomputed median energy stashed in the dataset under \"median_energy\"\n    median_energy:\n      energy_key: \"energy_true\"\n      weight: \"weight\"\n</code></pre>"},{"location":"configuration/datasets/#what-the-loader-does","title":"What the loader does","text":"<p>For each entry:</p> <ol> <li>Loads the DataFrame from <code>path</code> via <code>pyForwardFolding.config.load_dataframe</code>, which dispatches    on file extension. Supported formats: CSV (<code>.csv</code>, <code>.txt</code>), Parquet (<code>.parquet</code>), Feather    (<code>.feather</code>, <code>.ft</code>), HDF5 (<code>.h5</code>, <code>.hdf</code>, <code>.hdf5</code>), Excel (<code>.xlsx</code>, <code>.xls</code>), Pickle (<code>.pkl</code>,    <code>.pickle</code>).</li> <li>For each entry of <code>param_mapping</code>, pulls the column named by <code>df_key</code>, optionally applies a    <code>transform</code> (which must be a method on <code>pyForwardFolding.backend.backend</code> \u2014 common choices are    <code>log10</code>, <code>cos</code>, <code>sin</code>, <code>exp</code>), and stores it under the output key.</li> <li>If <code>median_energy</code> is provided, computes    <code>backend.weighted_median(df[energy_key], df[weight])</code> and stores it under the    key <code>\"median_energy\"</code> \u2014 useful for factors like <code>DeltaGamma</code> that operate around a sample's    median energy.</li> </ol> <p>The returned object is:</p> <pre><code>{\n    \"ic86_tracks\": {\n        \"true_energy\":       &lt;jax array&gt;,\n        \"log10_reco_energy\": &lt;jax array&gt;,\n        \"cos_reco_zenith\":   &lt;jax array&gt;,\n        \"baseline_weight\":   &lt;jax array&gt;,\n        \"median_energy\":     &lt;jax array&gt;,        # only if requested\n    },\n    \"another_dataset\": { ... },\n}\n</code></pre> <p>which is exactly the shape <code>analysis.evaluate(datasets, parameter_values)</code> expects.</p>"},{"location":"configuration/datasets/#tips","title":"Tips","text":"<ul> <li>The output keys (left side of <code>param_mapping</code>) must match what your factors and binning   expect. Cross-check against the <code>required_variables</code> of your <code>Analysis</code>:</li> </ul> <pre><code>ana = pyFF.config.analysis_from_config(\"config.yaml\")\nprint(ana.required_variables)\n</code></pre> <ul> <li>The <code>df_key</code> (right side) is just the column name in your DataFrame. Use whatever your   upstream pipeline produces.</li> <li>Multiple datasets can map the same <code>df_key</code> to different output names if needed \u2014 e.g. one   dataset's <code>\"e_reco\"</code> and another's <code>\"reco_energy\"</code> both feed <code>\"log10_reco_energy\"</code>.</li> </ul>"},{"location":"configuration/factors/","title":"Factors section","text":"<p>Every entry under <code>factors:</code> becomes one <code>AbstractUnbinnedFactor</code> instance, identified by <code>name</code>. Other YAML sections refer to factors by these names.</p>"},{"location":"configuration/factors/#common-keys","title":"Common keys","text":"<p>Every factor entry has at least:</p> <pre><code>- name: &lt;unique string&gt;      # Identifier; referenced from components.factors\n  type: &lt;FactorClassName&gt;    # Must match a key in FACTORSTR_CLASS_MAPPING\n  param_mapping:             # Optional. Rename or fix individual parameters.\n    &lt;internal_param&gt;: &lt;new_name_or_numeric_value&gt;\n  ...                        # Factor-specific keys (pivot_energy, baseline_norm, ...)\n</code></pre> <p>The available <code>type</code> values map directly to factor classes. The full list is in the Factor reference; the most common ones are:</p> <code>type:</code> Models <code>PowerLawFlux</code> Spectral shape \u221d E^(\u2212\u03b3) <code>BrokenPowerLawFlux</code> Power law with a spectral break at logEbreak <code>FluxNorm</code> Pure scalar normalization <code>GaisserZenithFactor</code> Atmospheric pi/K zenith bracket <code>FlavorRatio</code> Per-flavor rescaling (uses true_type) <code>GalacticPlaneBox</code> / <code>SegmentedPlane</code> Galactic-plane selectors <code>VetoThreshold</code> / <code>FixedVeto</code> Atmospheric self-veto reweighting <code>DeltaGamma</code> Spectral-index nuisance pull <code>ModelInterpolator</code> Smooth interpolation between two MC models <code>GradientReweight</code> / <code>ClassifierGradientReweight</code> Per-event gradient reweighting <code>SnowstormGauss</code> Gaussian reweighting for systematics <code>SoftCut</code> Differentiable sigmoid cut"},{"location":"configuration/factors/#param_mapping","title":"<code>param_mapping</code>","text":"<p>This is how you bridge factor-internal parameter names to globally exposed parameter names and how you fix parameters at compile time.</p> <pre><code>param_mapping:\n  flux_norm: \"astro_norm\"   # rename: exposed as astro_norm in the fit\n  spectral_index: 2.7       # fix: held at 2.7, not in the fit\n</code></pre> Value type Meaning String Rename to that name Int / float (not bool) Fix at that value, drop from the free parameters Key not listed Identity \u2014 exposed under its internal name <code>param_mapping</code> omitted All parameters exposed under their internal names <p>Tip: when two factors share the same internal parameter (e.g. two <code>PowerLawFlux</code>'s both have <code>flux_norm</code>), use <code>param_mapping</code> to give them distinct exposed names. Otherwise they'll intentionally share the same fit parameter, which is occasionally what you want but usually isn't.</p>"},{"location":"configuration/factors/#examples","title":"Examples","text":"<p>A bare <code>PowerLawFlux</code>, exposing the natural parameter names:</p> <pre><code>- name: \"astro\"\n  type: \"PowerLawFlux\"\n  pivot_energy: 1.0e5\n  baseline_norm: 1.0e-18\n  # no param_mapping \u2192 flux_norm and spectral_index are exposed under those names\n</code></pre> <p>A <code>PowerLawFlux</code> with both parameters renamed and a <code>FluxNorm</code> reusing the same fit parameter:</p> <pre><code>- name: \"atmo_shape\"\n  type: \"PowerLawFlux\"\n  pivot_energy: 1.0\n  baseline_norm: 1.4e-5\n  param_mapping:\n    flux_norm: \"atmo_norm\"\n    spectral_index: \"atmo_index\"\n\n- name: \"atmo_scale\"\n  type: \"FluxNorm\"\n  param_mapping:\n    flux_norm: \"atmo_norm\"     # same name \u2192 same parameter at fit time\n</code></pre> <p>A <code>GaisserZenithFactor</code> fixing the K/\u03c0 ratio at the canonical value:</p> <pre><code>- name: \"atmo_zenith\"\n  type: \"GaisserZenithFactor\"\n  pivot_energy: 1.0e5\n  epsilon_pi: 115.0\n  epsilon_K:  850.0\n  param_mapping:\n    kaon_pion_ratio: 0.213\n</code></pre> <p>For every factor's full set of constructor keys, see the Factor reference.</p>"},{"location":"configuration/histograms/","title":"Histograms","text":"<p>The <code>histograms:</code> section is a list of <code>BinnedExpectation</code> definitions. Together they form the <code>Analysis</code>.</p>"},{"location":"configuration/histograms/#anatomy","title":"Anatomy","text":"<pre><code>histograms:\n  - name: det1                            # Used as the dict key in Analysis.expectations\n    binning:                              # \u2192 AbstractBinning.construct_from(...)\n      type: RectangularBinning\n      bin_vars_edges:\n        - [\"log10_reco_energy\", \"linear\", [2, 7, 31]]\n        - [\"cos_reco_zenith\",   \"linear\", [-1, 0, 25]]\n    lifetime: 315600000                   # Seconds. Multiplied into the histogram.\n    models:                               # List of [model_name, dataset_key] pairs\n      - [\"model\", \"dataset\"]\n    hist_factors:                         # Optional. List of binned factors.\n      - name: snowstorm_grad\n        type: SnowStormGradient\n        parameters: [\"ice_absorption\", \"dom_efficiency\"]\n        gradient_names: [\"abs\", \"qeff\"]\n        default: [1.0, 1.0]\n        split_values: [1.0, 1.0]\n        gradient_pickle: \"gradients/snowstorm_ic86.pkl\"\n</code></pre>"},{"location":"configuration/histograms/#binning","title":"<code>binning</code>","text":"<p>Dispatched through <code>AbstractBinning.construct_from</code>. The available <code>type:</code> values are documented in Binning reference. For <code>RectangularBinning</code>, each <code>bin_vars_edges</code> entry is <code>[variable_name, edge_type, edges]</code> with <code>edge_type</code> either <code>\"linear\"</code> (then <code>edges</code> is <code>[start, stop, num]</code>) or <code>\"array\"</code> (then <code>edges</code> is the literal list).</p>"},{"location":"configuration/histograms/#lifetime","title":"<code>lifetime</code>","text":"<p>Multiplicative factor applied to the histogram (and squared into the per-bin sum-of-squared-weights). The conventional choice is seconds, so a per-event rate in Hz becomes a count.</p>"},{"location":"configuration/histograms/#models","title":"<code>models</code>","text":"<p>A list of <code>[model_name, dataset_key]</code> pairs (note the order \u2014 model first, dataset second). The dataset key is the top-level key of the dict you pass to <code>analysis.evaluate(datasets, ...)</code>, and the dataset must contain (a) every <code>required_variables</code> of the chosen model and (b) every variable the binning needs.</p> <p>Multi-detector example:</p> <pre><code>- name: combined\n  binning: ...\n  models:\n    - [\"model\", \"ic86_tracks\"]\n    - [\"model\", \"ic86_cascades\"]\n</code></pre>"},{"location":"configuration/histograms/#hist_factors-optional","title":"<code>hist_factors</code> (optional)","text":"<p>Binned factors (subclasses of <code>AbstractBinnedFactor</code>) added to the histogram after the per-event prediction. They are dispatched through <code>AbstractBinnedFactor.construct_from</code> and currently include <code>SnowStormGradient</code>, <code>ScaledTemplate</code>, <code>PerBinPolynomial</code>. See the Factor reference \u2192 Binned factors for their YAML keys.</p>"},{"location":"configuration/histograms/#what-the-analysis-ends-up-as","title":"What the Analysis ends up as","text":"<pre><code>ana.expectations            # OrderedDict[name, BinnedExpectation]\nana[\"det1\"].lifetime        # 315600000\nana[\"det1\"].binning         # the RectangularBinning instance\nana[\"det1\"].models          # [the_model_object]\nana[\"det1\"].binned_factors  # [SnowStormGradient(...), ...] or []\n</code></pre> <p>The <code>Analysis</code> object has a rich <code>__repr__</code> and Jupyter <code>_repr_markdown_</code>, so simply printing it (or displaying it in a notebook) gives you a full structural breakdown \u2014 useful for sanity-checking a config.</p>"},{"location":"configuration/overview/","title":"Configuration overview","text":"<p>pyForwardFolding analyses are described in a single YAML file that the <code>pyForwardFolding.config</code> module compiles into an <code>Analysis</code>, a parameter-seed dict, and a list of priors.</p> <p>The full reference example used throughout these pages is <code>examples/test.yaml</code>.</p>"},{"location":"configuration/overview/#top-level-structure","title":"Top-level structure","text":"<pre><code>factors:        # Per-event multiplicative factors  \u2192 AbstractUnbinnedFactor instances\ncomponents:     # Products of factors               \u2192 ModelComponent\nmodels:         # Sums of components with baseline weights \u2192 Model\nhistograms:     # Binning + lifetime + dataset-model pairing + optional binned factors \u2192 BinnedExpectation\n                # An Analysis is the dict of named histograms.\n\nmodel_parameters:      # Seed values used when evaluating the analysis without fitting\nprior_bounds:          # Bounds for UniformPrior parameters\nprior_seeds:           # Seeds for UniformPrior parameters\n\n# Optional \u2014 only required if you want Gaussian priors on some parameters\nprior_bounds_gauss:    # Bounds for parameters that also get a Gaussian pull\nprior_params_gauss:    # (mean, std) for each Gaussian-pulled parameter\nprior_seeds_gauss:     # Seeds for Gaussian-pulled parameters\n\n# Optional \u2014 only required if you want to build the dataset from disk via the loader\ndatasets:              # Mapping of dataset name \u2192 DataFrame file + column transforms\n</code></pre>"},{"location":"configuration/overview/#loader-entry-points","title":"Loader entry points","text":"<pre><code>import pyForwardFolding as pyFF\n\nana                       = pyFF.config.analysis_from_config(\"config.yaml\")\nparams, priors            = pyFF.config.params_from_config(\"config.yaml\")\ndata                      = pyFF.config.dataset_from_config(\"config.yaml\")   # optional\nmodels_per_histogram_dset = pyFF.config.models_from_config(\"config.yaml\")    # advanced\n</code></pre> <p>All four accept either a path (<code>str</code>) or an already-loaded <code>dict</code>.</p>"},{"location":"configuration/overview/#build-order","title":"Build order","text":"<pre><code>factors   \u2500\u2500\u25b6  components   \u2500\u2500\u25b6  models   \u2500\u2500\u25b6  histograms (BinnedExpectation)   \u2500\u2500\u25b6  Analysis\nparameters \u2500\u2500\u2500\u2510\npriors    \u2500\u2500\u2500\u2500\u2534\u2500\u2500\u25b6 Likelihood \u2500\u2500\u25b6 Minimizer / Hypothesis / HypothesisTest\ndatasets  \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25b6 evaluation\n</code></pre> <p>The loaders enforce this order, so the YAML sections can appear in any order in the file as long as cross-references resolve.</p>"},{"location":"configuration/overview/#walkthrough-sections","title":"Walkthrough sections","text":"<ul> <li>Factors \u2014 every factor type, its YAML keys, and what they mean.</li> <li>Components &amp; models \u2014 bundling factors and assigning baseline weights.</li> <li>Histograms \u2014 binning, lifetime, dataset binding, binned factors.</li> <li>Parameters &amp; priors \u2014 seeds, bounds, uniform vs. Gaussian.</li> <li>Datasets \u2014 letting the loader build datasets from on-disk DataFrames.</li> </ul>"},{"location":"configuration/parameters-and-priors/","title":"Parameters and priors","text":"<p>These sections feed the <code>params_from_config</code> loader, which produces the parameter-seed dictionary used by <code>analysis.evaluate(...)</code> and the list of priors used by the likelihood.</p>"},{"location":"configuration/parameters-and-priors/#model_parameters","title":"<code>model_parameters</code>","text":"<p>A dict mapping exposed parameter names (the names you established through <code>param_mapping</code> in the factors section) to default values. These are the values used when you evaluate the analysis without fitting (e.g. to draw the prediction at the seed).</p> <pre><code>model_parameters:\n  astro_norm: 1.44\n  astro_index: 2.0\n  atmo_norm: 1.0\n</code></pre>"},{"location":"configuration/parameters-and-priors/#prior_bounds-and-prior_seeds","title":"<code>prior_bounds</code> and <code>prior_seeds</code>","text":"<p>Parameters listed here are wrapped in a <code>UniformPrior</code> \u2014 i.e. they contribute nothing to the log-likelihood but provide bounds and seeds to the minimizer. This is the default for any parameter of interest you want to leave un-pulled.</p> <pre><code>prior_bounds:\n  astro_norm:  [0.0, .inf]\n  astro_index: [-.inf, .inf]\n  atmo_norm:   [0.0, .inf]\n\nprior_seeds:\n  astro_norm: 1.44\n  astro_index: 2.0\n  atmo_norm: 1.0\n</code></pre> <p><code>.inf</code> is the YAML 1.1 spelling that <code>pyyaml</code> parses as a float infinity. Use <code>-.inf</code> for negative infinity.</p>"},{"location":"configuration/parameters-and-priors/#prior__gauss-optional","title":"<code>prior_*_gauss</code> (optional)","text":"<p>For parameters you want to pull toward a nominal value with a Gaussian penalty, add the parallel <code>_gauss</code> sections. The loader will then construct an additional <code>GaussianUnivariatePrior</code>.</p> <pre><code>prior_bounds_gauss:\n  kaon_pion_ratio: [0.0, 1.0]\n  ice_absorption:  [0.5, 1.5]\n\nprior_params_gauss:\n  kaon_pion_ratio: [0.213, 0.05]      # (mean, sigma)\n  ice_absorption:  [1.0,   0.10]\n\nprior_seeds_gauss:\n  kaon_pion_ratio: 0.213\n  ice_absorption:  1.0\n</code></pre>"},{"location":"configuration/parameters-and-priors/#what-params_from_config-returns","title":"What <code>params_from_config</code> returns","text":"<pre><code>params, priors = pyFF.config.params_from_config(\"config.yaml\")\n# params: {\"astro_norm\": 1.44, \"astro_index\": 2.0, \"atmo_norm\": 1.0}\n# priors: [UniformPrior(...), GaussianUnivariatePrior(...)]   # if _gauss section present\n#         [UniformPrior(...)]                                  # otherwise\n</code></pre> <p>The full set of exposed parameters must be covered by the union of all priors \u2014 otherwise the likelihood constructor will raise <code>ValueError</code>. So:</p> <ul> <li>Every free parameter that should appear in the fit needs an entry in either   <code>prior_bounds</code>/<code>prior_seeds</code> (uniform) or in the <code>_gauss</code> triple.</li> <li>A parameter can appear in both \u2014 <code>UniformPrior</code> then provides the bounds, and the Gaussian prior   adds the pull on top.</li> </ul>"},{"location":"examples/toy_example_w_config/","title":"Toy example w config","text":"In\u00a0[1]: Copied! <pre>%load_ext autoreload\n%autoreload 2\n</pre> %load_ext autoreload %autoreload 2 In\u00a0[2]: Copied! <pre>import numpy as np \nimport scipy as sp\nimport matplotlib.pyplot as plt\nimport pyForwardFolding as pyFF\nimport jax\njax.config.update(\"jax_enable_x64\", True)\nimport jax.numpy as jnp\nfrom math import inf\n</pre> import numpy as np  import scipy as sp import matplotlib.pyplot as plt import pyForwardFolding as pyFF import jax jax.config.update(\"jax_enable_x64\", True) import jax.numpy as jnp from math import inf <pre>WARNING:root:Set JAX to use 64-bit precision\n</pre> In\u00a0[3]: Copied! <pre>n_events = int(5E6)\n</pre> n_events = int(5E6) In\u00a0[29]: Copied! <pre>event_energies = 10**np.random.uniform(2, 8, size=n_events)\nevent_zenith = np.arccos(np.random.uniform(-1, 1, size=n_events))\n\nreco_energy = 10**(np.log10(event_energies) + np.random.normal(0, 0.2, size=n_events))\nreco_zenith = np.arccos(\n    np.clip(np.cos(event_zenith) + np.random.normal(0, 0.05, size=n_events), -1, 1)\n)\n\nbaseline_weights = event_energies * np.log(10) * 6 / n_events * 1e5\n\ndata = {\"dataset\": {\n    \"log10_reco_energy\": jnp.log10(jnp.asarray(reco_energy)),\n    \"cos_reco_zenith\": jnp.cos(jnp.asarray(reco_zenith)),\n    \"baseline_weight\": jnp.asarray(baseline_weights),\n    \"true_energy\": jnp.asarray(event_energies),\n    \"true_zenith\": jnp.asarray(event_zenith),\n}}\n</pre> event_energies = 10**np.random.uniform(2, 8, size=n_events) event_zenith = np.arccos(np.random.uniform(-1, 1, size=n_events))  reco_energy = 10**(np.log10(event_energies) + np.random.normal(0, 0.2, size=n_events)) reco_zenith = np.arccos(     np.clip(np.cos(event_zenith) + np.random.normal(0, 0.05, size=n_events), -1, 1) )  baseline_weights = event_energies * np.log(10) * 6 / n_events * 1e5  data = {\"dataset\": {     \"log10_reco_energy\": jnp.log10(jnp.asarray(reco_energy)),     \"cos_reco_zenith\": jnp.cos(jnp.asarray(reco_zenith)),     \"baseline_weight\": jnp.asarray(baseline_weights),     \"true_energy\": jnp.asarray(event_energies),     \"true_zenith\": jnp.asarray(event_zenith), }}  In\u00a0[30]: Copied! <pre>fig, ax = plt.subplots()\nh = ax.hist(reco_energy, bins=np.linspace(1e2, 1e8, 100), weights=baseline_weights)\nplt.legend()\nplt.loglog()\n</pre> fig, ax = plt.subplots() h = ax.hist(reco_energy, bins=np.linspace(1e2, 1e8, 100), weights=baseline_weights) plt.legend() plt.loglog()  <pre>/var/folders/gt/9_v597xs11q6md_5kgwx0g700000gn/T/ipykernel_2622/3820931122.py:3: UserWarning: No artists with labels found to put in legend.  Note that artists whose label start with an underscore are ignored when legend() is called with no argument.\n  plt.legend()\n</pre> Out[30]: <pre>[]</pre> In\u00a0[31]: Copied! <pre>config = \"./test.yaml\"\n</pre> config = \"./test.yaml\" In\u00a0[32]: Copied! <pre>ana = pyFF.config.analysis_from_config(config)\nparams, priors = pyFF.config.params_from_config(config)\n</pre> ana = pyFF.config.analysis_from_config(config) params, priors = pyFF.config.params_from_config(config) In\u00a0[33]: Copied! <pre>obs, ssq = ana.evaluate(data,params)\n</pre> obs, ssq = ana.evaluate(data,params) In\u00a0[34]: Copied! <pre>cobs, cssq = ana.evaluate_per_component(data,params)\n</pre> cobs, cssq = ana.evaluate_per_component(data,params) In\u00a0[35]: Copied! <pre>plt.stairs(obs[\"det1\"].sum(axis=1),label=\"total\")\nplt.stairs(cobs[\"det1\"][\"astro\"].sum(axis=1),label=\"astro\")\nplt.stairs(cobs[\"det1\"][\"atmo\"].sum(axis=1),label=\"atmo\")\nplt.legend()\nplt.yscale(\"log\")\n</pre> plt.stairs(obs[\"det1\"].sum(axis=1),label=\"total\") plt.stairs(cobs[\"det1\"][\"astro\"].sum(axis=1),label=\"astro\") plt.stairs(cobs[\"det1\"][\"atmo\"].sum(axis=1),label=\"atmo\") plt.legend() plt.yscale(\"log\") In\u00a0[36]: Copied! <pre>llh = pyFF.likelihood.PoissonLikelihood(ana,priors)\nmini = pyFF.minimizer.ScipyMinimizer(llh)\n</pre> llh = pyFF.likelihood.PoissonLikelihood(ana,priors) mini = pyFF.minimizer.ScipyMinimizer(llh) In\u00a0[37]: Copied! <pre>mini.minimize(obs,data)\n</pre> mini.minimize(obs,data) Out[37]: <pre>(  message: CONVERGENCE: RELATIVE REDUCTION OF F &lt;= FACTR*EPSMCH\n   success: True\n    status: 0\n       fun: 1663.6568047521996\n         x: [ 2.000e+00  1.440e+00  1.000e+00]\n       nit: 1\n       jac: [ 1.178e-06  1.412e-07  1.096e-03]\n      nfev: 6\n      njev: 6\n  hess_inv: &lt;3x3 LbfgsInvHessProduct with dtype=float64&gt;,\n {'astro_index': np.float64(2.0000000000001266),\n  'astro_norm': np.float64(1.4400000000000144),\n  'atmo_norm': np.float64(1.0000000001043308)},\n 1663.6568047521996)</pre> In\u00a0[38]: Copied! <pre>norms = np.linspace(0,3,11)\nllhs = []\nfor norm in norms:\n    res = mini.minimize(obs,data,fixed_pars={\"astro_norm\":norm})\n    llhs.append(res[2])\nllhs = np.asarray(llhs)\n</pre> norms = np.linspace(0,3,11) llhs = [] for norm in norms:     res = mini.minimize(obs,data,fixed_pars={\"astro_norm\":norm})     llhs.append(res[2]) llhs = np.asarray(llhs) In\u00a0[39]: Copied! <pre>dllhs = 2*(llhs-np.min(llhs))\n</pre> dllhs = 2*(llhs-np.min(llhs)) In\u00a0[40]: Copied! <pre>plt.plot(norms,dllhs)\nplt.xlabel(\"astro_norm\")\nplt.ylabel(\"-2DeltaLLH\")\n</pre> plt.plot(norms,dllhs) plt.xlabel(\"astro_norm\") plt.ylabel(\"-2DeltaLLH\") Out[40]: <pre>Text(0, 0.5, '-2DeltaLLH')</pre> In\u00a0[\u00a0]: Copied! <pre>\n</pre>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"<ul> <li>Python \u2265 3.10, &lt; 4</li> <li>A working installation of JAX (pulled in automatically as a dependency).</li> </ul> <p>The required runtime dependencies, as declared in <code>pyproject.toml</code>, are:</p> <ul> <li><code>jax==0.6.1</code>, <code>jaxlib==0.6.1</code></li> <li><code>ml_dtypes==0.5.1</code></li> <li><code>numpy==2.2.6</code>, <code>pandas&gt;=2.2</code>, <code>pyArrow&gt;=19</code></li> <li><code>pyyaml&gt;=6.0</code></li> <li><code>iminuit&gt;=2.31</code></li> <li><code>scikit-learn&gt;=1.6</code></li> </ul>"},{"location":"getting-started/installation/#from-source","title":"From source","text":"<pre><code>git clone https://github.com/chrhck/pyForwardFolding.git\ncd pyForwardFolding\npip install .\n</code></pre> <p>For an editable install (recommended when developing):</p> <pre><code>pip install -e .\n</code></pre>"},{"location":"getting-started/installation/#optional-dependency-groups","title":"Optional dependency groups","text":"<p>The project defines optional dependency groups in <code>pyproject.toml</code>:</p> Group Installs Use for <code>tests</code> <code>pytest</code>, <code>pytest-mock</code>, <code>coverage</code> Running the test suite <code>checks</code> <code>mypy</code>, <code>ruff</code> Type-checking and linting <code>docs</code> <code>mkdocs-material</code>, <code>mkdocstrings</code>, <code>mkdocs-jupyter</code> Building this documentation site <p>Install with, e.g.:</p> <pre><code>pip install -e \".[docs,tests]\"\n</code></pre>"},{"location":"getting-started/installation/#using-hatch-recommended-for-development","title":"Using Hatch (recommended for development)","text":"<p>The project ships a Hatch configuration. Hatch will create and manage a virtual environment in <code>.venv/</code> for you:</p> <pre><code>hatch env create\nhatch shell        # activate the environment\nhatch run check    # ruff + mypy + spell check\nhatch run format   # auto-format\n</code></pre>"},{"location":"getting-started/installation/#verifying-the-install","title":"Verifying the install","text":"<pre><code>import pyForwardFolding as pyFF\nprint(pyFF.__version__)\n</code></pre> <p>On import, the package enables 64-bit precision in JAX (you'll see a one-time warning to that effect). All numerical operations in pyForwardFolding go through this backend.</p>"},{"location":"getting-started/quickstart/","title":"Quickstart","text":"<p>This page walks through a minimal end-to-end analysis: build a toy Monte Carlo dataset, configure a two-component model (astrophysical + atmospheric) from YAML, predict histograms, and fit.</p> <p>The full notebook this is adapted from lives at <code>examples/toy_example_w_config.ipynb</code> and the YAML config at <code>examples/test.yaml</code>.</p>"},{"location":"getting-started/quickstart/#1-generate-or-load-monte-carlo-data","title":"1. Generate (or load) Monte Carlo data","text":"<p>pyForwardFolding consumes per-event Monte Carlo. Each event needs:</p> <ul> <li>True quantities used by your model factors (here: <code>true_energy</code>, <code>true_zenith</code>).</li> <li>Reconstructed quantities used by your binning (here: <code>log10_reco_energy</code>, <code>cos_reco_zenith</code>).</li> <li>A baseline weight (here: <code>baseline_weight</code>) \u2014 typically the inverse generation probability   multiplied by any per-event normalization that you don't want to refit.</li> </ul> <pre><code>import numpy as np\nimport jax.numpy as jnp\nimport pyForwardFolding as pyFF\n\nn = int(5e6)\nevent_energies = 10 ** np.random.uniform(2, 8, size=n)\nevent_zenith   = np.arccos(np.random.uniform(-1, 1, size=n))\n\nreco_energy = 10 ** (np.log10(event_energies) + np.random.normal(0, 0.2, size=n))\nreco_zenith = np.arccos(\n    np.clip(np.cos(event_zenith) + np.random.normal(0, 0.05, size=n), -1, 1)\n)\nbaseline_weights = event_energies * np.log(10) * 6 / n * 1e5\n\ndata = {\n    \"dataset\": {\n        \"log10_reco_energy\": jnp.log10(jnp.asarray(reco_energy)),\n        \"cos_reco_zenith\":  jnp.cos(jnp.asarray(reco_zenith)),\n        \"baseline_weight\":  jnp.asarray(baseline_weights),\n        \"true_energy\":      jnp.asarray(event_energies),\n        \"true_zenith\":      jnp.asarray(event_zenith),\n    }\n}\n</code></pre> <p>The top-level key <code>\"dataset\"</code> is the dataset key that the YAML config will refer to.</p>"},{"location":"getting-started/quickstart/#2-define-the-analysis-in-yaml","title":"2. Define the analysis in YAML","text":"<p>Save the following as <code>test.yaml</code>. It declares two factors (an astrophysical power-law flux and an atmospheric Gaisser-shape \u00d7 power-law flux), groups them into two components (<code>astro</code>, <code>atmo</code>), combines them into one model, and bins the prediction in a 30 \u00d7 24 (<code>log10_reco_energy</code>, <code>cos_reco_zenith</code>) histogram:</p> <pre><code>factors:\n  - name: \"powerlaw\"\n    type: \"PowerLawFlux\"\n    pivot_energy: 1.0e5\n    baseline_norm: 1.0e-18\n    param_mapping:\n      flux_norm: \"astro_norm\"\n      spectral_index: \"astro_index\"\n  - name: \"atmo_powerlaw\"\n    type: \"PowerLawFlux\"\n    pivot_energy: 1.0\n    baseline_norm: 0.000014\n    param_mapping:\n      flux_norm: \"atmo_norm\"\n      spectral_index: 2.7        # fixed (numeric value, not a string)\n  - name: \"atmo_zenith\"\n    type: \"GaisserZenithFactor\"\n    pivot_energy: 1.0e5\n    baseline_norm: 1.0e-18\n    param_mapping:\n      kaon_pion_ratio: 0.213     # fixed\n\ncomponents:\n  - name: \"astro\"\n    factors: [\"powerlaw\"]\n  - name: \"atmo\"\n    factors: [\"atmo_powerlaw\", \"atmo_zenith\"]\n\nmodels:\n  - name: \"model\"\n    components:\n      - { name: \"astro\", baseline_weight: \"baseline_weight\" }\n      - { name: \"atmo\",  baseline_weight: \"baseline_weight\" }\n\nhistograms:\n  - name: det1\n    binning:\n      type: RectangularBinning\n      bin_vars_edges:\n        - [\"log10_reco_energy\", \"linear\", [2, 7, 31]]\n        - [\"cos_reco_zenith\",   \"linear\", [-1, 0, 25]]\n    lifetime: 315600000           # 10 years in seconds\n    models: [[\"model\", \"dataset\"]]\n\nmodel_parameters:\n  astro_norm: 1.44\n  astro_index: 2.0\n  atmo_norm: 1.0\n\nprior_bounds:\n  astro_norm:  [0.0, .inf]\n  astro_index: [-.inf, .inf]\n  atmo_norm:   [0.0, .inf]\n\nprior_seeds:\n  astro_norm: 1.44\n  astro_index: 2.0\n  atmo_norm: 1.0\n</code></pre> <p>A line-by-line walkthrough of every section lives at Configuration \u2192 Overview.</p>"},{"location":"getting-started/quickstart/#3-build-the-analysis","title":"3. Build the analysis","text":"<pre><code>ana    = pyFF.config.analysis_from_config(\"test.yaml\")\nparams, priors = pyFF.config.params_from_config(\"test.yaml\")\nprint(ana)              # rich textual / markdown summary\n</code></pre> <p><code>ana</code> is an <code>Analysis</code> \u2014 a container of one or more <code>BinnedExpectation</code> objects. <code>params</code> is a dict of parameter seeds and <code>priors</code> is a list of prior objects ready to pass to a likelihood.</p>"},{"location":"getting-started/quickstart/#4-predict-histograms","title":"4. Predict histograms","text":"<pre><code>predicted, predicted_ssq = ana.evaluate(data, params)\n# predicted[\"det1\"] is a (30, 24) JAX array \u2014 counts per (E, cos\u03b8) bin\n</code></pre> <p>To get the prediction broken down per model component (e.g. <code>astro</code> vs. <code>atmo</code>):</p> <pre><code>per_comp, _ = ana.evaluate_per_component(data, params)\nper_comp[\"det1\"][\"astro\"]   # only the astrophysical contribution\nper_comp[\"det1\"][\"atmo\"]    # only the atmospheric contribution\n</code></pre>"},{"location":"getting-started/quickstart/#5-fit","title":"5. Fit","text":"<pre><code>llh  = pyFF.likelihood.PoissonLikelihood(ana, priors)\nmini = pyFF.minimizer.ScipyMinimizer(llh)\n\n# Treat the prediction itself as observed data (\"Asimov fit\")\nresult, best_fit, fmin = mini.minimize(predicted, data)\nprint(best_fit)   # should recover params (no statistical fluctuation)\n</code></pre> <p>For a real fit, replace <code>predicted</code> with the observed counts dict (<code>{\"det1\": observed_counts_2d}</code>). For a Poisson pseudo-experiment, use <code>Hypothesis.generate_pseudo_experiments</code>.</p>"},{"location":"getting-started/quickstart/#6-scan-a-parameter","title":"6. Scan a parameter","text":"<pre><code>import numpy as np\n\nnorms = np.linspace(0, 3, 11)\nts = []\nfor x in norms:\n    _, _, fmin = mini.minimize(predicted, data, fixed_pars={\"astro_norm\": x})\n    ts.append(fmin)\nts = 2 * (np.asarray(ts) - np.min(ts))   # \u22122 \u0394log L profile\n</code></pre>"},{"location":"getting-started/quickstart/#whats-next","title":"What's next","text":"<ul> <li>Read Concepts \u2192 Overview for the math behind factors, components, and binning.</li> <li>See the Configuration reference for every key the YAML schema accepts.</li> <li>See the Factor reference for the parameters and required variables of   every available factor.</li> </ul>"},{"location":"how-to/build-from-config/","title":"Build an analysis from a YAML config","text":"<p>This is the recommended way to set up an analysis: keep the structural description in YAML, keep the dataset construction in Python.</p>"},{"location":"how-to/build-from-config/#minimal-recipe","title":"Minimal recipe","text":"<pre><code>import pyForwardFolding as pyFF\n\nana    = pyFF.config.analysis_from_config(\"path/to/config.yaml\")\nparams, priors = pyFF.config.params_from_config(\"path/to/config.yaml\")\n</code></pre> <p><code>ana</code> is ready to evaluate; <code>params</code> is a dict of seed values; <code>priors</code> is a list of prior objects.</p>"},{"location":"how-to/build-from-config/#inspecting-what-you-built","title":"Inspecting what you built","text":"<p><code>Analysis</code> and all its sub-objects have rich <code>__repr__</code> and <code>_repr_markdown_</code> implementations:</p> <pre><code>print(ana)                       # plain-text tree of expectations \u2192 models \u2192 components \u2192 factors\nana                              # in Jupyter: rendered as markdown\nana.required_variables           # set of dataset keys you must provide\nana.exposed_parameters           # set of parameter names the fit will see\n</code></pre> <p>For a graphical view (requires <code>networkx</code> and optionally <code>pygraphviz</code>):</p> <pre><code>fig = ana.render_graph(figsize=(14, 10), layout=\"hierarchical\")\n</code></pre>"},{"location":"how-to/build-from-config/#common-pitfalls","title":"Common pitfalls","text":"<p>ValueError: Mismatch between exposed parameters and prior variables Every parameter exposed by the analysis must appear in some prior. The fix is usually either (a) add the missing parameter to <code>prior_seeds</code>/<code>prior_bounds</code> in the YAML, or (b) <code>fix</code> it in the relevant factor's <code>param_mapping</code> if you don't want it in the fit.</p> <p>ValueError: Baseline weight '...' not found in input variables The <code>baseline_weight:</code> keyword in <code>models:</code> refers to a key your dataset must contain. Check the spelling against your dataset dict.</p> <p>Unknown factor type: ... The <code>type:</code> field must match a key in <code>pyForwardFolding.factor.FACTORSTR_CLASS_MAPPING</code>. List the available ones from a Python prompt:</p> <pre><code>from pyForwardFolding.factor import FACTORSTR_CLASS_MAPPING\nprint(list(FACTORSTR_CLASS_MAPPING))\n</code></pre>"},{"location":"how-to/build-from-config/#building-the-dataset","title":"Building the dataset","text":"<p>Two options:</p> <ol> <li>Build it in Python (most common). Just provide a <code>dict[str, dict[str, jax.Array]]</code> matching    what <code>ana.required_variables</code> requests.</li> <li>Use the YAML loader by adding a <code>datasets:</code> section and calling    <code>pyFF.config.dataset_from_config(\"config.yaml\")</code> \u2014 see Configuration \u2192 Datasets.</li> </ol>"},{"location":"how-to/build-from-config/#building-an-analysis-in-python-instead","title":"Building an analysis in Python instead","text":"<p>For programmatic use (e.g. inside tests, or when generating configurations) you can skip YAML entirely and assemble the objects yourself:</p> <pre><code>from pyForwardFolding.factor import PowerLawFlux\nfrom pyForwardFolding.model_component import ModelComponent\nfrom pyForwardFolding.model import Model\nfrom pyForwardFolding.binning import RectangularBinning\nfrom pyForwardFolding.binned_expectation import BinnedExpectation\nfrom pyForwardFolding.analysis import Analysis\nimport jax.numpy as jnp\n\nastro = PowerLawFlux(\"powerlaw\", pivot_energy=1e5, baseline_norm=1e-18,\n                      param_mapping={\"flux_norm\": \"astro_norm\", \"spectral_index\": \"astro_index\"})\ncomp  = ModelComponent(\"astro\", [astro])\nmodel = Model.from_pairs(\"model\", [(\"baseline_weight\", comp)])\n\nbinning = RectangularBinning(\n    bin_variables=(\"log10_reco_energy\", \"cos_reco_zenith\"),\n    bin_edges=(jnp.linspace(2, 7, 31), jnp.linspace(-1, 0, 25)),\n)\nexp = BinnedExpectation(\"det1\", [(\"dataset\", model)], binning, lifetime=3.156e8)\nana = Analysis({\"det1\": exp})\n</code></pre>"},{"location":"how-to/custom-factor/","title":"Write a custom factor","text":"<p>The factor API is small. To add a new factor you implement one subclass and register it in the <code>type:</code> dispatch table.</p>"},{"location":"how-to/custom-factor/#recipe","title":"Recipe","text":"<pre><code>from typing import Any, Dict, List, Optional, Union\nfrom pyForwardFolding.backend import Array, backend\nfrom pyForwardFolding.factor import AbstractUnbinnedFactor, FACTORSTR_CLASS_MAPPING\n\n\nclass MyCustomFactor(AbstractUnbinnedFactor):\n    \"\"\"One-line summary.\n\n    Longer description: what physics it models, which other factors it\n    composes with, edge cases.\n\n    Parameters required by this factor: ``my_param``.\n    Variables required by this factor:  ``my_variable``.\n\n    Args:\n        name (str): Identifier for the factor.\n        my_constant (float): A configuration constant (not fitted).\n        param_mapping (dict, optional): As for all factors.\n    \"\"\"\n\n    def __init__(\n        self,\n        name: str,\n        my_constant: float,\n        param_mapping: Optional[Dict[str, Union[str, float]]] = None,\n    ):\n        super().__init__(name, param_mapping)\n        self.my_constant = my_constant\n\n        # Declare what this factor consumes:\n        self.factor_parameters: List[str] = [\"my_param\"]\n        self.req_vars: List[str] = [\"my_variable\"]\n\n    @classmethod\n    def construct_from(cls, config: Dict[str, Any]) -&gt; \"MyCustomFactor\":\n        # Build from a YAML dict (the ``factors:`` entry).\n        return cls(\n            name=config[\"name\"],\n            my_constant=config[\"my_constant\"],\n            param_mapping=config.get(\"param_mapping\", None),\n        )\n\n    def evaluate(\n        self,\n        input_variables: Dict[str, Union[Array, float]],\n        parameter_values: Dict[str, float],\n    ) -&gt; Array:\n        # Pull only the variables this factor declares it needs:\n        from pyForwardFolding.factor import (\n            get_required_variable_values,\n            get_parameter_values,\n        )\n        inputs = get_required_variable_values(self, input_variables)\n        params = get_parameter_values(self, parameter_values)\n\n        x = inputs[\"my_variable\"]\n        a = params[\"my_param\"]\n        return backend.exp(a * x * self.my_constant)\n\n\n# Register so YAML configs can refer to it by name:\nFACTORSTR_CLASS_MAPPING[\"MyCustomFactor\"] = MyCustomFactor\n</code></pre>"},{"location":"how-to/custom-factor/#three-things-to-get-right","title":"Three things to get right","text":"<ol> <li><code>self.factor_parameters</code> is the list of internal parameter names. They get bridged to    global names through <code>param_mapping</code>. Users will not see these strings unless they're left    un-renamed.</li> <li><code>self.req_vars</code> is the list of dataset keys the factor reads. Returning per-event arrays of    the wrong shape will fail at histogramming time, not at factor evaluation, so cross-check.</li> <li>Use <code>pyForwardFolding.backend.backend</code>, not raw NumPy or JAX. Going through the backend    keeps the factor differentiable and JIT-compatible.</li> </ol>"},{"location":"how-to/custom-factor/#binned-factors","title":"Binned factors","text":"<p>If your factor naturally acts at the histogram level (e.g. a per-bin systematic), subclass <code>AbstractBinnedFactor</code> instead. The differences:</p> <ul> <li>The constructor takes a <code>binning: AbstractBinning</code> argument and so does <code>construct_from</code>.</li> <li><code>evaluate</code> returns a <code>(mu_add, ssq_add)</code> tuple \u2014 both per-bin arrays of shape <code>binning.hist_dims</code>,   or <code>(mu_add, None)</code> if you don't have variance information.</li> </ul> <p>See <code>pyForwardFolding.factor.ScaledTemplate</code> for a minimal example.</p>"},{"location":"how-to/custom-factor/#smoke-test","title":"Smoke test","text":"<pre><code>import jax.numpy as jnp\n\nf = MyCustomFactor(\"my_factor\", my_constant=0.5)\nout = f.evaluate(\n    {\"my_variable\": jnp.linspace(0, 1, 10)},\n    {\"my_param\": 1.5},\n)\nprint(out.shape)               # (10,)\nprint(f.exposed_parameters)    # ['my_param']\nprint(f.required_variables)    # ['my_variable']\n</code></pre> <p>Add a unit test in <code>tests/test_factor.py</code> and you're done.</p>"},{"location":"how-to/hypothesis-test/","title":"Perform a hypothesis test","text":"<p>The <code>statistics</code> module wraps the fit machinery in standard frequentist primitives: hypotheses, null/alternative distributions, pseudo-experiments, discovery potential, parameter scans, and uncertainties.</p>"},{"location":"how-to/hypothesis-test/#set-up-two-hypotheses","title":"Set up two hypotheses","text":"<pre><code>from pyForwardFolding.statistics import Hypothesis, HypothesisTest\n\nH0 = Hypothesis(\"null\", llh, fixed_pars={\"astro_norm\": 0.0})   # background-only\nH1 = Hypothesis(\"alt\",  llh)                                   # signal + background\n\ntest = HypothesisTest(H0, H1, dataset)\n</code></pre> <p><code>HypothesisTest.from_likelihood(llh, dataset, fixed_params)</code> is a shortcut that builds H0 with the given fixed parameters and H1 with everything free.</p>"},{"location":"how-to/hypothesis-test/#evaluate-the-test-statistic-on-observed-data","title":"Evaluate the test statistic on observed data","text":"<pre><code>ts = test.test(observed_data)           # \u22122 \u0394log L\nprint(test.dof)                          # degrees of freedom = 1 in this example\nprint(test.free_parameters)              # {'astro_norm'}\n</code></pre>"},{"location":"how-to/hypothesis-test/#null-and-alternative-distributions","title":"Null and alternative distributions","text":"<pre><code>ts_null = test.null_dist(nexp=1000)\nts_alt  = test.alt_dist(nexp=1000)\n</code></pre> <p>Each pseudo-experiment Poisson-fluctuates the prediction once, fits H0 and H1, and records \\(-2(\\log\\mathcal L_{H_0} - \\log\\mathcal L_{H_1})\\). Generation uses <code>Hypothesis.generate_pseudo_experiments</code> internally.</p>"},{"location":"how-to/hypothesis-test/#discovery-potential","title":"Discovery potential","text":"<p>Find the smallest signal strength for which the median alternative TS exceeds the \u03c3-threshold of the null distribution (only works for one degree of freedom).</p> <pre><code>null, alt, x_disc = test.discovery_potential(\n    nexp_null=1000,\n    nexp_alt=200,\n    sigma_level=3,\n)\nprint(f\"3\u03c3 discovery at astro_norm = {x_disc:.3f}\")\n</code></pre> <p>Pass <code>null_dist=...</code> to reuse a previously generated null distribution. The Asimov asymptotic shortcut (no pseudo-experiments) is</p> <pre><code>x_disc_asimov = test.discovery_potential_asimov(sigma_level=3)\n</code></pre> <p>This uses the \u03c7\u00b2 quantile at the chosen \u03c3-level instead of fluctuated pseudo-experiments and is typically a fine starting point that you can later refine with the full Monte-Carlo flavour above.</p>"},{"location":"how-to/hypothesis-test/#profile-likelihood-scan-and-uncertainty","title":"Profile-likelihood scan and uncertainty","text":"<pre><code>xs, ts_profile = test.scan(observed_data, scan_points=21)         # \u22122 \u0394log L vs free parameter\nsigma_unc       = test.uncertainty(observed_data, sigma_level=1)   # \u00b11\u03c3 symmetric\n</code></pre> <p>Both are one-degree-of-freedom only (i.e. exactly one free parameter that's fixed in H0).</p>"},{"location":"how-to/hypothesis-test/#power-preject-h0-h1","title":"Power (P(reject H0 | H1))","text":"<pre><code>p = test.power(nexp=1000, sigma_level=3)\nprint(f\"3\u03c3 power = {p:.2%}\")\n</code></pre>"},{"location":"how-to/hypothesis-test/#choosing-the-minimizer","title":"Choosing the minimizer","text":"<p>Both hypotheses accept a <code>minimizer</code> argument; default is <code>ScipyMinimizer(llh)</code>. If you want Minuit diagnostics in your fits:</p> <pre><code>from pyForwardFolding.minimizer import MinuitMinimizer\nH1 = Hypothesis(\"alt\", llh, minimizer=MinuitMinimizer(llh, simplex_prefit=True))\n</code></pre> <p>Same minimizer is reused across pseudo-experiments, so any caching it does is preserved.</p>"},{"location":"how-to/multi-detector/","title":"Set up a multi-detector / multi-sample analysis","text":"<p>The basic idea: one <code>Analysis</code> holds several <code>BinnedExpectation</code> objects keyed by name, each with its own dataset, binning, lifetime, and binned factors. Parameters with the same exposed name in different samples become the same fit parameter automatically.</p>"},{"location":"how-to/multi-detector/#yaml","title":"YAML","text":"<p>Two histograms, one per detector, sharing the same model:</p> <pre><code>factors:\n  - name: powerlaw\n    type: PowerLawFlux\n    pivot_energy: 1.0e5\n    baseline_norm: 1.0e-18\n    param_mapping:\n      flux_norm: astro_norm\n      spectral_index: astro_index\n\ncomponents:\n  - { name: astro, factors: [powerlaw] }\n\nmodels:\n  - name: model\n    components:\n      - { name: astro, baseline_weight: baseline_weight }\n\nhistograms:\n  - name: ic86_tracks\n    binning:\n      type: RectangularBinning\n      bin_vars_edges:\n        - [\"log10_reco_energy\", \"linear\", [2.5, 7, 31]]\n        - [\"cos_reco_zenith\",   \"linear\", [-1, 0, 25]]\n    lifetime: 315600000\n    models: [[\"model\", \"ic86_tracks\"]]\n  - name: ic86_cascades\n    binning:\n      type: RectangularBinning\n      bin_vars_edges:\n        - [\"log10_reco_energy\", \"linear\", [3, 7, 21]]\n        - [\"cos_reco_zenith\",   \"linear\", [-1, 0, 13]]\n    lifetime: 315600000\n    models: [[\"model\", \"ic86_cascades\"]]\n\nmodel_parameters:  { astro_norm: 1.0, astro_index: 2.5 }\nprior_seeds:       { astro_norm: 1.0, astro_index: 2.5 }\nprior_bounds:\n  astro_norm:  [0.0, .inf]\n  astro_index: [-.inf, .inf]\n</code></pre>"},{"location":"how-to/multi-detector/#dataset","title":"Dataset","text":"<pre><code>data = {\n    \"ic86_tracks\":   {...},\n    \"ic86_cascades\": {...},\n}\nana = pyFF.config.analysis_from_config(\"config.yaml\")\nana.evaluate(data, params)\n# \u2192 {\"ic86_tracks\": (31, 24) array, \"ic86_cascades\": (20, 12) array}, plus matching ssq dict\n</code></pre>"},{"location":"how-to/multi-detector/#different-per-detector-systematics","title":"Different per-detector systematics","text":"<p>Each <code>BinnedExpectation</code> has its own <code>hist_factors</code>, so detector-specific binned systematics live on the relevant entry only:</p> <pre><code>- name: ic86_tracks\n  binning: ...\n  models: [[\"model\", \"ic86_tracks\"]]\n  hist_factors:\n    - { name: snowstorm_tracks, type: SnowStormGradient, ... }\n\n- name: ic86_cascades\n  binning: ...\n  models: [[\"model\", \"ic86_cascades\"]]\n  hist_factors:\n    - { name: snowstorm_cascades, type: SnowStormGradient, ... }\n</code></pre> <p>Per-event detector systematics (e.g. one <code>SnowstormGauss</code> per detector) typically end up in separate components that are then composed differently per model. You can either:</p> <ul> <li>Have one shared model and let per-event factors read from per-detector dataset columns, or</li> <li>Use different models per histogram (the <code>models:</code> of a histogram is <code>[[model_name, dataset_key], ...]</code>,   so you can list any combination).</li> </ul>"},{"location":"how-to/multi-detector/#joint-fitting","title":"Joint fitting","text":"<p>The likelihood is summed over all expectations automatically:</p> <pre><code>llh  = pyFF.likelihood.PoissonLikelihood(ana, priors)\nmini = pyFF.minimizer.ScipyMinimizer(llh)\nmini.minimize(\n    observed={\"ic86_tracks\": tracks_hist, \"ic86_cascades\": cascades_hist},\n    dataset=data,\n)\n</code></pre> <p><code>astro_norm</code> and <code>astro_index</code> are common parameters and are fitted jointly. If you wanted, say, a separate spectral index per detector, you'd give the two <code>PowerLawFlux</code> factors different <code>spectral_index \u2192 ...</code> rename targets and put them in different components (e.g. <code>astro_tracks</code> vs. <code>astro_cascades</code>) feeding different models.</p>"},{"location":"how-to/run-a-fit/","title":"Run a fit","text":""},{"location":"how-to/run-a-fit/#setup","title":"Setup","text":"<p>Start from an <code>Analysis</code>, a parameter seed dict, and a list of priors \u2014 as produced by <code>pyFF.config.analysis_from_config(...)</code> and <code>pyFF.config.params_from_config(...)</code>.</p> <pre><code>ana   = pyFF.config.analysis_from_config(\"config.yaml\")\nparams, priors = pyFF.config.params_from_config(\"config.yaml\")\n\nllh   = pyFF.likelihood.PoissonLikelihood(ana, priors)\nmini  = pyFF.minimizer.ScipyMinimizer(llh, tol=1e-10)\n</code></pre> <p>For limited-MC analyses use <code>SAYLikelihood</code>; for Minuit's covariance use <code>MinuitMinimizer</code>.</p>"},{"location":"how-to/run-a-fit/#best-fit-on-real-data","title":"Best-fit on real data","text":"<pre><code>observed = {\"det1\": observed_counts_2d}          # one entry per BinnedExpectation\nresult, best_fit, fmin = mini.minimize(observed, data)\nprint(best_fit)\n</code></pre> <p><code>result</code> is the underlying optimiser object (scipy <code>OptimizeResult</code> or iMinuit info dict), <code>best_fit</code> is the dict of best-fit parameter values, and <code>fmin</code> is the negative log-likelihood at the minimum.</p>"},{"location":"how-to/run-a-fit/#profile-likelihood-scan","title":"Profile likelihood scan","text":"<pre><code>import numpy as np\n\nxs = np.linspace(0, 3, 21)\nts = []\nfor x in xs:\n    _, _, fmin = mini.minimize(observed, data, fixed_pars={\"astro_norm\": x})\n    ts.append(fmin)\nts = 2 * (np.asarray(ts) - np.min(ts))    # \u22122 \u0394 log L\n</code></pre> <p><code>fixed_pars</code> accepts an arbitrary dict and pins those parameters during minimisation. All remaining parameters are still profiled (re-minimised at each scan point).</p>"},{"location":"how-to/run-a-fit/#asimov-fit","title":"Asimov fit","text":"<p>To get the best-fit point for the (un-fluctuated) prediction itself \u2014 useful for sanity checks and for asymptotic sensitivity estimates \u2014 use the prediction as observed data:</p> <pre><code>asimov, _ = ana.evaluate(data, params)             # one expectation dict\n_, best_fit, _ = mini.minimize(asimov, data)\nassert all(abs(best_fit[k] - params[k]) &lt; 1e-6 for k in params)\n</code></pre>"},{"location":"how-to/run-a-fit/#using-minuit-instead","title":"Using Minuit instead","text":"<pre><code>mini = pyFF.minimizer.MinuitMinimizer(llh, tol=1e-2, strategy=1, simplex_prefit=True)\ninfo, best_fit, fmin = mini.minimize(observed, data)\nprint(info[\"hess_inv\"])                  # iMinuit covariance\nprint(info[\"success\"], info[\"message\"])  # MIGRAD diagnostics\n</code></pre> <p><code>simplex_prefit=True</code> runs <code>simplex()</code> before <code>migrad()</code>; useful for difficult starting points.</p>"},{"location":"how-to/run-a-fit/#fisher-information-sensitivity-no-fitting-required","title":"Fisher-information sensitivity (no fitting required)","text":"<p>For quick sensitivity studies you can skip the fit and use the Fisher-information matrix evaluated at the seed:</p> <pre><code>fim = ana.fisher_information(data, params)              # per-expectation matrices\ncov = ana.covariance(data, params)                       # {\"total\": ..., \"det1\": ...}\nvar = ana.variance(data, params)                         # {\"total\": {param: \u03c3\u00b2, ...}, ...}\n</code></pre> <p>This uses <code>jax.jacfwd</code> and the Poisson Fisher formula \\(I_{ij} = \\sum_k \\partial_i \\mu_k \\partial_j \\mu_k / \\mu_k\\).</p>"},{"location":"reference/binnings/","title":"Binning reference","text":"<p>The binning types available in YAML are dispatched through <code>AbstractBinning.construct_from(config)</code> and selected by the <code>type:</code> field.</p>"},{"location":"reference/binnings/#rectangularbinning","title":"<code>RectangularBinning</code>","text":"<p>A multi-dimensional rectangular grid. Currently the only fully supported binning.</p> <pre><code>binning:\n  type: RectangularBinning\n  bin_vars_edges:\n    - [\"log10_reco_energy\", \"linear\", [2, 7, 31]]   # linspace(2, 7, 31)\n    - [\"cos_reco_zenith\",   \"linear\", [-1, 0, 25]]\n    - [\"psi_deg\",           \"array\",  [0, 1, 3, 10, 30]]   # custom edges\n</code></pre> <p>Each entry of <code>bin_vars_edges</code> is <code>[variable_name, edge_type, edges]</code>:</p> <ul> <li><code>linear</code>: <code>edges</code> is <code>[start, stop, num]</code>, expanded with <code>jax.numpy.linspace</code>.</li> <li><code>array</code>: <code>edges</code> is the literal list of bin edges.</li> </ul> <p>Internals:</p> <ul> <li><code>binning.required_variables</code> returns the list of <code>variable_name</code>s.</li> <li><code>binning.hist_dims</code> returns the tuple of bin counts (one per dimension).</li> <li>Bin indices are cached per dataset key, so repeated <code>analysis.evaluate(...)</code> calls don't   re-compute them. Use <code>binning.clear_bin_indices(ds_key)</code> if you swap a dataset out in place.</li> </ul>"},{"location":"reference/binnings/#rectangularbinning2dto3d","title":"<code>RectangularBinning2DTo3D</code>","text":"<p>Specialization of <code>RectangularBinning</code> that takes 2D binning variables and inflates the resulting histogram to a 3D one by repeating along a third axis. Useful for comparing to a 3D template when the events themselves don't carry that third dimension.</p> <p>Constructed in Python (no shorthand YAML dispatch yet):</p> <pre><code>from pyForwardFolding.binning import RectangularBinning2DTo3D\nimport jax.numpy as jnp\n\nbinning = RectangularBinning2DTo3D(\n    bin_variables=(\"log10_reco_energy\", \"cos_reco_zenith\"),\n    bin_edges=(jnp.linspace(2, 7, 31), jnp.linspace(-1, 0, 25)),\n    bin_edges_3d=jnp.linspace(0, 1, 11),\n)\n</code></pre>"},{"location":"reference/binnings/#relaxedbinning","title":"<code>RelaxedBinning</code>","text":"<p>Differentiable, soft-edged 1D binning using a tanh kernel:</p> \\[ K(x; a, b) = \\tfrac{1}{2}\\left[1 + \\tanh\\!\\tfrac{x-a}{s}\\,\\tanh\\!\\tfrac{-(x-b)}{s}\\right]. \\] <pre><code>binning:\n  type: RelaxedBinning\n  bin_variable: log10_reco_energy\n  bin_edges: [2, 7, 31]    # linspace args\n  slope: 0.1\n</code></pre> <p>Warning</p> <p><code>RelaxedBinning</code> is wired into <code>AbstractBinning.construct_from</code>, but its constructor currently raises <code>NotImplementedError</code>. Treat it as experimental.</p>"},{"location":"reference/factors/","title":"Factor reference","text":"<p>This page documents every factor shipped with pyForwardFolding. For each factor we list:</p> <ul> <li>The YAML <code>type:</code> value and the corresponding Python class</li> <li>Its exposed parameters (internal names \u2014 what <code>param_mapping</code> keys are)</li> <li>Its required input variables (keys the dataset must provide)</li> <li>The constructor arguments in YAML form</li> <li>A short summary of what it computes</li> </ul> <p><code>param_mapping</code> rules (rename via string, fix via numeric) apply to every factor \u2014 see Concepts \u2192 Factors.</p> <p>The class registry is <code>pyForwardFolding.factor.FACTORSTR_CLASS_MAPPING</code>; every key below is a key in that dict.</p>"},{"location":"reference/factors/#unbinned-factors","title":"Unbinned factors","text":""},{"location":"reference/factors/#powerlawflux","title":"<code>PowerLawFlux</code>","text":"<p>Power-law differential flux</p> \\[F = \\Phi_0 \\cdot \\Phi_\\text{base} \\cdot \\left(\\tfrac{E}{E_\\text{pivot}}\\right)^{-\\gamma}\\] Attribute Value Exposed parameters <code>flux_norm</code>, <code>spectral_index</code> Required variables <code>true_energy</code> YAML keys <code>name</code>, <code>pivot_energy</code>, <code>baseline_norm</code>, optional <code>param_mapping</code> <pre><code>- name: astro\n  type: PowerLawFlux\n  pivot_energy: 1.0e5\n  baseline_norm: 1.0e-18\n  param_mapping:\n    flux_norm: astro_norm\n    spectral_index: astro_index\n</code></pre>"},{"location":"reference/factors/#brokenpowerlawflux","title":"<code>BrokenPowerLawFlux</code>","text":"<p>Broken power law with a smooth pivot adjustment to keep <code>flux_norm</code> interpretable at <code>pivot_energy</code>.</p> Attribute Value Exposed parameters <code>flux_norm</code>, <code>spectral_index_1</code>, <code>spectral_index_2</code>, <code>logEbreak</code> Required variables <code>true_energy</code> YAML keys <code>name</code>, <code>pivot_energy</code>, <code>baseline_norm</code>, optional <code>param_mapping</code> <p><code>logEbreak</code> is \\(\\log_{10}(E_\\text{break} / \\mathrm{GeV})\\).</p> <pre><code>- name: astro_broken\n  type: BrokenPowerLawFlux\n  pivot_energy: 1.0e5\n  baseline_norm: 1.0e-18\n  param_mapping:\n    flux_norm: astro_norm\n    spectral_index_1: gamma_low\n    spectral_index_2: gamma_high\n    logEbreak: log10_Ebreak\n</code></pre>"},{"location":"reference/factors/#fluxnorm","title":"<code>FluxNorm</code>","text":"<p>Pure scalar normalization \\(F = \\Phi_0\\). Useful for chaining a free overall scale onto a fixed shape.</p> Attribute Value Exposed parameters <code>flux_norm</code> Required variables (none) YAML keys <code>name</code>, optional <code>param_mapping</code> <pre><code>- name: galactic_norm\n  type: FluxNorm\n  param_mapping:\n    flux_norm: gp_norm\n</code></pre>"},{"location":"reference/factors/#gaisserzenithfactor","title":"<code>GaisserZenithFactor</code>","text":"<p>Atmospheric \u03c0/K Gaisser bracket</p> \\[B(E, \\theta) = \\frac{1}{1 + a_\\pi E \\cos\\theta^\\star / \\varepsilon_\\pi}               + R_{K/\\pi} \\cdot \\frac{1}{1 + b_K E \\cos\\theta^\\star / \\varepsilon_K},\\] <p>normalized so that \\(B(E_\\text{pivot}, \\text{vertical}) = 1\\). Uses the Chirkin (2004) Earth-curvature corrected effective \\(\\cos\\theta^\\star\\) when <code>earth_curvature: true</code>.</p> Attribute Value Exposed parameters <code>kaon_pion_ratio</code> Required variables <code>true_energy</code>, <code>true_zenith</code> (radians) YAML keys <code>name</code>, <code>pivot_energy</code>, optional <code>epsilon_pi</code> (default 115), <code>epsilon_K</code> (850), <code>a_pi</code> (1.0), <code>b_K</code> (1.0), <code>earth_curvature</code> (true), <code>param_mapping</code> <p>The factor uses \\(|\\cos\\theta|\\) internally so atmospheric symmetry is automatic.</p> <pre><code>- name: atmo_zenith\n  type: GaisserZenithFactor\n  pivot_energy: 1.0e5\n  param_mapping:\n    kaon_pion_ratio: 0.213          # fix at canonical value\n</code></pre>"},{"location":"reference/factors/#flavorratio","title":"<code>FlavorRatio</code>","text":"<p>Per-flavor scaling using <code>true_type</code> (PDG-like codes 12 = \u03bd_e, 14 = \u03bd_\u03bc, 16 = \u03bd_\u03c4, signs ignored). Muon neutrinos are kept at 1; electron and tau are rescaled.</p> Attribute Value Exposed parameters <code>nue_ratio</code>, <code>nutau_ratio</code> Required variables <code>true_type</code> YAML keys <code>name</code>, optional <code>param_mapping</code> <pre><code>- name: flavor\n  type: FlavorRatio\n</code></pre>"},{"location":"reference/factors/#segmentedplane","title":"<code>SegmentedPlane</code>","text":"<p>Galactic-plane component with per-longitude-segment norm and spectral index. Events outside the latitude window <code>|true_lat| \u2264 height</code> get zero weight.</p> Attribute Value Exposed parameters <code>galactic_norm_{i}</code>, <code>galactic_gamma_{i}</code> for each segment <code>i</code> Required variables <code>true_energy</code>, <code>true_lat</code>, <code>true_lon</code> YAML keys <code>name</code>, <code>reference_energy</code>, <code>baseline_flux</code>, <code>segment_edges</code> (list of longitude edges), <code>height</code> (latitude half-width in radians), optional <code>param_mapping</code> <pre><code>- name: gp_segmented\n  type: SegmentedPlane\n  reference_energy: 1.0e3\n  baseline_flux: 1.0\n  height: 0.087266            # ~5\u00b0 in radians\n  segment_edges: [-3.1416, -1.0, 0.0, 1.0, 3.1416]\n</code></pre>"},{"location":"reference/factors/#galacticplanebox","title":"<code>GalacticPlaneBox</code>","text":"<p>Pure box selector in galactic latitude (no fittable parameters, no longitude dependence). Returns 1 for \\(|\\text{true\\_lat}| \\le \\text{height}\\), else 0. Pair with a <code>PowerLawFlux</code> and a <code>FluxNorm</code> to model a galactic-plane component.</p> Attribute Value Exposed parameters (none) Required variables <code>true_lat</code> (radians) YAML keys <code>name</code>, <code>height</code>, optional <code>param_mapping</code> <pre><code>- name: gp_box\n  type: GalacticPlaneBox\n  height: 0.087266          # ~5\u00b0\n</code></pre>"},{"location":"reference/factors/#snowstormgauss","title":"<code>SnowstormGauss</code>","text":"<p>Gaussian reweighting against a per-event sampled systematic value. Reweights events drawn uniformly on <code>sys_sim_bounds</code> toward a Gaussian centred on <code>scale</code> with width <code>sys_gauss_width</code>.</p> Attribute Value Exposed parameters <code>scale</code> Required variables <code>req_variable_name</code> (whatever you pass in YAML) YAML keys <code>name</code>, <code>sys_gauss_width</code>, <code>sys_sim_bounds</code> (<code>[lo, hi]</code>), <code>req_variable_name</code>, optional <code>param_mapping</code> <pre><code>- name: snowstorm_abs\n  type: SnowstormGauss\n  sys_gauss_width: 0.05\n  sys_sim_bounds: [0.85, 1.15]\n  req_variable_name: ice_absorption_sample\n  param_mapping:\n    scale: ice_absorption\n</code></pre>"},{"location":"reference/factors/#deltagamma","title":"<code>DeltaGamma</code>","text":"<p>Spectral-index nuisance pull as \\((E / E_\\text{ref})^{-\\Delta\\gamma}\\). Typically combined with a <code>PowerLawFlux</code> to let the spectral index float as a nuisance parameter while keeping the parameter-of-interest fixed.</p> Attribute Value Exposed parameters <code>delta_gamma</code> Required variables <code>true_energy</code> (and historically <code>median_energy</code>) YAML keys <code>name</code>, <code>reference_energy</code>, optional <code>param_mapping</code> <pre><code>- name: spectral_nuisance\n  type: DeltaGamma\n  reference_energy: 1.0e5\n</code></pre>"},{"location":"reference/factors/#modelinterpolator","title":"<code>ModelInterpolator</code>","text":"<p>Differentiable interpolation between two precomputed event-weight sets:</p> \\[F_i = (1 - \\lambda) + \\lambda \\cdot \\frac{w_\\text{alt}}{w_\\text{base}},\\] <p>with safe handling of zero baseline weights. Useful when you have two MC weighting hypotheses and want to fit the mixing fraction.</p> Attribute Value Exposed parameters <code>lambda_int</code> Required variables <code>&lt;baseline_weight&gt;</code>, <code>&lt;alternative_weight&gt;</code> (names from YAML) YAML keys <code>name</code>, <code>baseline_weight</code>, <code>alternative_weight</code>, optional <code>param_mapping</code> <pre><code>- name: model_interp\n  type: ModelInterpolator\n  baseline_weight: w_conv\n  alternative_weight: w_prompt\n</code></pre>"},{"location":"reference/factors/#gradientreweight","title":"<code>GradientReweight</code>","text":"<p>Linear gradient reweighting (e.g. Barr parameters):</p> \\[F_i = \\frac{w_\\text{base} + \\sum_p \\alpha_p \\cdot g_{p,i}}{w_\\text{base}}.\\] <p>Each parameter <code>alpha_p</code> is exposed; the per-event gradient \\(g_{p,i}\\) is read from the dataset.</p> Attribute Value Exposed parameters The keys of <code>gradient_key_mapping</code> Required variables The values of <code>gradient_key_mapping</code> and <code>baseline_weight</code> YAML keys <code>name</code>, <code>gradient_key_mapping</code> (dict), <code>baseline_weight</code>, optional <code>param_mapping</code> <pre><code>- name: barr_gradients\n  type: GradientReweight\n  baseline_weight: w_conv\n  gradient_key_mapping:\n    barr_w: dw_w\n    barr_y: dw_y\n</code></pre>"},{"location":"reference/factors/#classifiergradientreweight","title":"<code>ClassifierGradientReweight</code>","text":"<p>Polynomial reweighting from classifier-fit log-weight gradients:</p> \\[r_i(\\alpha) = \\exp\\left(\\sum_{p, k} g_{p,k,i} \\cdot (\\alpha_p - \\alpha_p^\\text{nom})^k\\right).\\] <p>Each <code>(param, order)</code> pair in <code>poly_features</code> consumes one input column named per <code>gradient_col_template</code> (default <code>\"g_{param}_{order}\"</code>).</p> Attribute Value Exposed parameters The unique <code>param</code> names in <code>poly_features</code> Required variables One column per <code>(param, order)</code> tuple, named via <code>gradient_col_template</code> YAML keys <code>name</code>, <code>poly_features</code> (list of <code>[param, order]</code>), <code>nominal_values</code> (dict), optional <code>gradient_col_template</code>, optional <code>param_mapping</code> <pre><code>- name: cls_grad\n  type: ClassifierGradientReweight\n  poly_features:\n    - [abs,  1]\n    - [abs,  2]\n    - [qeff, 1]\n    - [qeff, 2]\n  nominal_values:\n    abs:  1.0\n    qeff: 1.0\n</code></pre>"},{"location":"reference/factors/#vetothreshold","title":"<code>VetoThreshold</code>","text":"<p>Second-order log-passing-fraction reweighting for the atmospheric self-veto:</p> \\[\\log_{10} \\mathrm{PF}_i = a_i + b_i \\cdot \\Delta E + c_i \\cdot \\Delta E^2,\\] <p>with \\(\\Delta E = E_\\text{rescale} \\cdot 10^{e_\\text{threshold}} - E_\\text{anchor}\\) and the per-event \\((a, b, c)\\) coefficients read from the dataset.</p> Attribute Value Exposed parameters <code>e_threshold</code> Required variables The column names you pass as <code>threshold_a</code>, <code>threshold_b</code>, <code>threshold_c</code> YAML keys <code>name</code>, <code>threshold_a</code>, <code>threshold_b</code>, <code>threshold_c</code>, <code>rescale_energy</code>, <code>anchor_energy</code>, optional <code>param_mapping</code> <pre><code>- name: veto\n  type: VetoThreshold\n  threshold_a: pf_a\n  threshold_b: pf_b\n  threshold_c: pf_c\n  rescale_energy: 100.0\n  anchor_energy: 100.0\n</code></pre>"},{"location":"reference/factors/#fixedveto","title":"<code>FixedVeto</code>","text":"<p>Applies a fixed per-event passing fraction column.</p> Attribute Value Exposed parameters (none) Required variables The column passed as <code>passing_fraction</code> YAML keys <code>name</code>, <code>passing_fraction</code>, optional <code>param_mapping</code> <pre><code>- name: pf\n  type: FixedVeto\n  passing_fraction: passing_fraction\n</code></pre>"},{"location":"reference/factors/#softcut","title":"<code>SoftCut</code>","text":"<p>Differentiable sigmoid cut on a variable: returns \\(\\sigma(\\mathrm{slope} \\cdot (x - x_\\text{cut}))\\).</p> Attribute Value Exposed parameters (none \u2014 <code>cut_value</code> is a configuration constant) Required variables <code>cut_variable</code> YAML keys <code>name</code>, <code>cut_variable</code>, <code>slope</code>, <code>cut_value</code>, optional <code>param_mapping</code> <pre><code>- name: e_softcut\n  type: SoftCut\n  cut_variable: log10_reco_energy\n  slope: 5.0\n  cut_value: 3.0\n</code></pre>"},{"location":"reference/factors/#binned-factors","title":"Binned factors","text":"<p>Binned factors live on a <code>BinnedExpectation</code> (under <code>hist_factors:</code> in YAML) and are added to the histogram after it has been built. They return a <code>(mu_add, ssq_add)</code> tuple per bin.</p>"},{"location":"reference/factors/#snowstormgradient","title":"<code>SnowStormGradient</code>","text":"<p>Additive per-bin systematic reweighting using a precomputed gradient pickle.</p> Attribute Value Exposed parameters Whatever you pass as <code>parameters</code> Required variables (none \u2014 purely additive over the histogram) YAML keys <code>name</code>, <code>parameters</code> (list), <code>gradient_names</code> (list, same length), <code>default</code> (list), <code>split_values</code> (list), <code>gradient_pickle</code> (path), optional <code>param_mapping</code> <p>The pickle is expected to be a dict with keys <code>binning</code>, <code>livetime</code>, and one entry per <code>gradient_name</code> containing <code>{\"gradient\": ndarray, \"gradient_error\": ndarray}</code> with shapes matching the binning.</p> <pre><code>- name: snowstorm\n  type: SnowStormGradient\n  parameters: [ice_absorption, dom_efficiency]\n  gradient_names: [abs, qeff]\n  default: [1.0, 1.0]\n  split_values: [1.0, 1.0]\n  gradient_pickle: gradients/snowstorm_ic86.pkl\n</code></pre>"},{"location":"reference/factors/#scaledtemplate","title":"<code>ScaledTemplate</code>","text":"<p>A per-bin template (loaded from a pickle) scaled by a single parameter <code>template_norm</code>. The pickle must contain <code>{\"template\": ndarray}</code> and optionally <code>{\"template_fluctuation\": ndarray}</code> for the per-bin MC variance.</p> Attribute Value Exposed parameters <code>template_norm</code> Required variables (none) YAML keys <code>name</code>, <code>template_file</code>, optional <code>param_mapping</code> <pre><code>- name: muon_template\n  type: ScaledTemplate\n  template_file: templates/muons_ic86.pkl\n  param_mapping:\n    template_norm: muon_norm\n</code></pre>"},{"location":"reference/factors/#perbinpolynomial","title":"<code>PerBinPolynomial</code>","text":"<p>Per-bin polynomial in a single parameter <code>scale</code>. The coefficients pickle is an array of shape <code>(order+1, *hist_dims)</code> where order is 1 or 2 (i.e. only 2nd- and 3rd-order polynomials are supported).</p> Attribute Value Exposed parameters <code>scale</code> Required variables (none) YAML keys <code>name</code>, <code>coefficients_file</code>, optional <code>param_mapping</code> <pre><code>- name: binned_systematic\n  type: PerBinPolynomial\n  coefficients_file: systematics/poly_coeffs.pkl\n</code></pre>"},{"location":"reference/factors/#quick-lookup-table","title":"Quick lookup table","text":"Factor Exposed parameters Required variables <code>PowerLawFlux</code> <code>flux_norm</code>, <code>spectral_index</code> <code>true_energy</code> <code>BrokenPowerLawFlux</code> <code>flux_norm</code>, <code>spectral_index_1</code>, <code>spectral_index_2</code>, <code>logEbreak</code> <code>true_energy</code> <code>FluxNorm</code> <code>flux_norm</code> \u2014 <code>GaisserZenithFactor</code> <code>kaon_pion_ratio</code> <code>true_energy</code>, <code>true_zenith</code> <code>FlavorRatio</code> <code>nue_ratio</code>, <code>nutau_ratio</code> <code>true_type</code> <code>SegmentedPlane</code> <code>galactic_norm_{i}</code>, <code>galactic_gamma_{i}</code> <code>true_energy</code>, <code>true_lat</code>, <code>true_lon</code> <code>GalacticPlaneBox</code> \u2014 <code>true_lat</code> <code>SnowstormGauss</code> <code>scale</code> <code>&lt;req_variable_name&gt;</code> <code>DeltaGamma</code> <code>delta_gamma</code> <code>true_energy</code> <code>ModelInterpolator</code> <code>lambda_int</code> <code>&lt;baseline_weight&gt;</code>, <code>&lt;alternative_weight&gt;</code> <code>GradientReweight</code> keys of <code>gradient_key_mapping</code> values of <code>gradient_key_mapping</code>, <code>&lt;baseline_weight&gt;</code> <code>ClassifierGradientReweight</code> unique <code>param</code>s in <code>poly_features</code> one g-column per <code>(param, order)</code> <code>VetoThreshold</code> <code>e_threshold</code> <code>threshold_a</code>, <code>threshold_b</code>, <code>threshold_c</code> columns <code>FixedVeto</code> \u2014 <code>&lt;passing_fraction&gt;</code> <code>SoftCut</code> \u2014 <code>&lt;cut_variable&gt;</code> <code>SnowStormGradient</code> (binned) from YAML <code>parameters</code> \u2014 (binned-only) <code>ScaledTemplate</code> (binned) <code>template_norm</code> \u2014 (binned-only) <code>PerBinPolynomial</code> (binned) <code>scale</code> \u2014 (binned-only)"},{"location":"reference/likelihoods/","title":"Likelihood reference","text":""},{"location":"reference/likelihoods/#likelihood-classes","title":"Likelihood classes","text":"Class Use when <code>PoissonLikelihood</code> Standard binned Poisson likelihood <code>SAYLikelihood</code> MC statistics are limited (arXiv:1901.04645) <p>Both accept <code>(analysis: Analysis, priors: List[AbstractPrior])</code> and expose</p> <pre><code>llh.llh(observed_data, datasets, parameter_values, empty_bins=\"skip\")\n</code></pre> <p><code>empty_bins</code> can be <code>\"skip\"</code> (default \u2014 substitute \\(10^{-8}\\) when the expectation is zero) or <code>\"throw\"</code> (raise <code>ValueError</code>).</p>"},{"location":"reference/likelihoods/#poisson-formula","title":"Poisson \u2014 formula","text":"\\[ \\log\\mathcal L = \\sum_A \\big[-\\mu_A + n_A \\log\\mu_A - \\log(n_A!)\\big] \\;+\\; \\sum_p \\log p_\\text{prior}. \\] <p>The factorial term is computed via <code>gammaln(n+1)</code> and so stays differentiable.</p>"},{"location":"reference/likelihoods/#say-formula","title":"SAY \u2014 formula","text":"<p>For each bin:</p> \\[ \\alpha = \\tfrac{\\mu^2}{\\sigma_\\mu^2} + 1, \\quad \\beta = \\tfrac{\\mu}{\\sigma_\\mu^2} \\] \\[ \\log \\mathcal L_\\text{eff} = \\alpha\\log\\beta + \\log\\Gamma(n+\\alpha) - \\log\\Gamma(n+1) - (n+\\alpha)\\log(1+\\beta) - \\log\\Gamma(\\alpha) \\] <p>When \\(\\sigma_\\mu^2 = 0\\) (no MC variance information) the bin falls back to the Poisson term. The implementation also clips \\(\\sigma_\\mu^2 \\le \\mu^2\\) to keep \\(\\alpha, \\beta\\) well-defined under nuisance-parameter induced negative-weight contributions.</p>"},{"location":"reference/likelihoods/#prior-classes","title":"Prior classes","text":"Class Notes <code>UniformPrior</code> Dummy prior. Bounds + seeds, but zero log-PDF contribution. <code>GaussianUnivariatePrior</code> Product of independent Gaussians (un-normalised; only the \u2212\u00bd z\u00b2 term is added). <p>Every parameter exposed by the analysis must appear in at least one prior \u2014 the likelihood constructor checks this and raises <code>ValueError</code> on a mismatch.</p> <pre><code>from pyForwardFolding.likelihood import UniformPrior, GaussianUnivariatePrior\n\nuniform = UniformPrior(\n    prior_seeds={\"astro_norm\": 1.0, \"astro_index\": 2.0},\n    prior_bounds={\"astro_norm\": (0.0, float(\"inf\")), \"astro_index\": (-float(\"inf\"), float(\"inf\"))},\n)\n\ngauss = GaussianUnivariatePrior(\n    prior_params={\"kaon_pion_ratio\": (0.213, 0.05)},\n    prior_seeds={\"kaon_pion_ratio\": 0.213},\n    prior_bounds={\"kaon_pion_ratio\": (0.0, 1.0)},\n)\n</code></pre>"},{"location":"reference/likelihoods/#helpers","title":"Helpers","text":"<ul> <li><code>likelihood.get_analysis()</code> \u2014 returns the wrapped <code>Analysis</code>.</li> <li><code>likelihood.get_seeds()</code> \u2014 concatenates <code>prior_seeds</code> across all priors.</li> <li><code>likelihood.get_bounds()</code> \u2014 concatenates <code>prior_bounds</code> across all priors.</li> </ul> <p>These are what the minimizers use to assemble flat seed / bound vectors automatically.</p>"},{"location":"reference/api/analysis/","title":"<code>pyForwardFolding.analysis</code>","text":"<p>The <code>Analysis</code> class is the top-level container that ties multiple <code>BinnedExpectation</code> objects together for joint evaluation and fitting.</p>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis","title":"pyForwardFolding.analysis","text":""},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis","title":"Analysis","text":"<pre><code>Analysis(expectations: Dict[str, BinnedExpectation])\n</code></pre> <p>Represents a complete analysis consisting of multiple binned expectations.</p> <p>Parameters:</p> Name Type Description Default <code>expectations</code> <code>Dict[str, BinnedExpectation]</code> <p>Dictionary mapping expectation names to their objects.</p> required Source code in <code>pyForwardFolding/analysis.py</code> <pre><code>def __init__(self, expectations: Dict[str, BinnedExpectation]):\n    self.expectations = expectations\n</code></pre>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.required_variables","title":"required_variables  <code>property</code>","text":"<pre><code>required_variables: Set[str]\n</code></pre> <p>Get all variables required by any component in the analysis.</p> <p>Returns:</p> Type Description <code>Set[str]</code> <p>Set[str]: A set containing all required variables.</p>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.exposed_parameters","title":"exposed_parameters  <code>property</code>","text":"<pre><code>exposed_parameters: Set[str]\n</code></pre> <p>Get all parameters exposed by the analysis expectations.</p> <p>Returns:</p> Type Description <code>Set[str]</code> <p>Set[str]: A set of all exposed parameters from all expectations.</p>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.evaluate","title":"evaluate","text":"<pre><code>evaluate(\n    datasets: Dict[str, Dict[str, Union[Array, float]]],\n    parameter_values: Dict[str, float],\n) -&gt; Tuple[Dict[str, Array], Dict[str, Array]]\n</code></pre> <p>Evaluate all expectations in the analysis.</p> <p>Parameters:</p> Name Type Description Default <code>datasets</code> <code>Dict[str, Dict[str, Union[Array, float]]]</code> <p>A dictionary mapping component names to their input variables.</p> required <code>parameter_values</code> <code>Dict[str, float]</code> <p>Variables exposed by previously evaluated expectations.</p> required <p>Returns:</p> Type Description <code>Tuple[Dict[str, Array], Dict[str, Array]]</code> <p>Tuple[Dict[str, Array], Dict[str, Array]]: A tuple containing: - A dictionary mapping component names to their evaluation results (histograms). - A dictionary mapping component names to their squared evaluation results (histograms).</p> Source code in <code>pyForwardFolding/analysis.py</code> <pre><code>def evaluate(\n    self,\n    datasets: Dict[str, Dict[str, Union[Array, float]]],\n    parameter_values: Dict[str, float],\n) -&gt; Tuple[Dict[str, Array], Dict[str, Array]]:\n    \"\"\"\n    Evaluate all expectations in the analysis.\n\n    Args:\n        datasets (Dict[str, Dict[str, Union[Array, float]]]): A dictionary mapping component names to their input variables.\n        parameter_values (Dict[str, float]): Variables exposed by previously evaluated expectations.\n\n    Returns:\n        Tuple[Dict[str, Array], Dict[str, Array]]: A tuple containing:\n            - A dictionary mapping component names to their evaluation results (histograms).\n            - A dictionary mapping component names to their squared evaluation results (histograms).\n    \"\"\"\n    output_dict = {}\n    output_ssq_dict = {}\n\n    for comp_name, comp in self.expectations.items():\n        # Evaluate the component\n        hist, hist_ssq = comp.evaluate(\n            datasets,\n            parameter_values,\n        )\n\n        # Store results\n        output_dict[comp_name] = hist\n        output_ssq_dict[comp_name] = hist_ssq\n\n    return output_dict, output_ssq_dict\n</code></pre>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.evaluate_per_component","title":"evaluate_per_component","text":"<pre><code>evaluate_per_component(\n    datasets: Dict[str, Dict[str, Union[Array, float]]],\n    parameter_values: Dict[str, float],\n) -&gt; Tuple[\n    Dict[str, Dict[str, Array]], Dict[str, Dict[str, Array]]\n]\n</code></pre> <p>Evaluate all expectations in the analysis, broken down per model component (e.g. astro, atmo).</p> <p>Parameters:</p> Name Type Description Default <code>datasets</code> <code>Dict[str, Dict[str, Union[Array, float]]]</code> <p>A dictionary mapping dataset names to their input variables.</p> required <code>parameter_values</code> <code>Dict[str, float]</code> <p>Variables exposed by previously evaluated expectations.</p> required <p>Returns:</p> Type Description <code>Dict[str, Dict[str, Array]]</code> <p>Tuple of two dicts shaped {expectation_name: {model_component_name: histogram}}</p> <code>Dict[str, Dict[str, Array]]</code> <p>\u2014 the first holds histograms, the second the squared-weights histograms.</p> Source code in <code>pyForwardFolding/analysis.py</code> <pre><code>def evaluate_per_component(\n    self,\n    datasets: Dict[str, Dict[str, Union[Array, float]]],\n    parameter_values: Dict[str, float],\n) -&gt; Tuple[Dict[str, Dict[str, Array]], Dict[str, Dict[str, Array]]]:\n    \"\"\"\n    Evaluate all expectations in the analysis, broken down per model component\n    (e.g. astro, atmo).\n\n    Args:\n        datasets (Dict[str, Dict[str, Union[Array, float]]]): A dictionary mapping\n            dataset names to their input variables.\n        parameter_values (Dict[str, float]): Variables exposed by previously\n            evaluated expectations.\n\n    Returns:\n        Tuple of two dicts shaped\n            {expectation_name: {model_component_name: histogram}}\n        \u2014 the first holds histograms, the second the squared-weights histograms.\n    \"\"\"\n    output_dict: Dict[str, Dict[str, Array]] = {}\n    output_ssq_dict: Dict[str, Dict[str, Array]] = {}\n\n    for exp_name, exp in self.expectations.items():\n        per_comp_hist: Dict[str, Array] = {}\n        per_comp_hist_ssq: Dict[str, Array] = {}\n\n        for model_dskey, model in exp.dskey_model_pairs:\n            if model_dskey not in datasets:\n                raise ValueError(\n                    f\"Dataset '{model_dskey}' not found in provided datasets.\"\n                )\n            input_variables = datasets[model_dskey]\n\n            # Per-ModelComponent weights (astro, atmo, ...)\n            per_comp_weights = model.evaluate_per_component(\n                input_variables, parameter_values\n            )\n\n            # Binning variables only need to be extracted once per dataset\n            binning_variables = tuple(\n                backend.asarray(input_variables[var])\n                for var in exp.binning.required_variables\n            )\n\n            for model_comp_name, weights in per_comp_weights.items():\n                weight_sq = backend.power(weights, 2)\n\n                hist_add = (\n                    exp.binning.build_histogram(\n                        model_dskey, weights, binning_variables\n                    )\n                    * exp.lifetime\n                )\n                hist_ssq_add = (\n                    exp.binning.build_histogram(\n                        model_dskey, weight_sq, binning_variables\n                    )\n                    * exp.lifetime ** 2\n                )\n\n                if model_comp_name in per_comp_hist:\n                    per_comp_hist[model_comp_name] = per_comp_hist[model_comp_name] + hist_add\n                    per_comp_hist_ssq[model_comp_name] = per_comp_hist_ssq[model_comp_name] + hist_ssq_add\n                else:\n                    per_comp_hist[model_comp_name] = hist_add\n                    per_comp_hist_ssq[model_comp_name] = hist_ssq_add\n\n        # Clamp once, after accumulating across datasets\n        for model_comp_name in per_comp_hist:\n            per_comp_hist[model_comp_name] = backend.clip(\n                per_comp_hist[model_comp_name], 0, float(\"inf\")\n            )\n            per_comp_hist_ssq[model_comp_name] = backend.clip(\n                per_comp_hist_ssq[model_comp_name], 0, float(\"inf\")\n            )\n\n        output_dict[exp_name] = per_comp_hist\n        output_ssq_dict[exp_name] = per_comp_hist_ssq\n\n    return output_dict, output_ssq_dict\n</code></pre>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.fisher_information","title":"fisher_information","text":"<pre><code>fisher_information(\n    datasets: Dict[str, Dict[str, Union[ndarray, float]]],\n    parameter_values: Dict[str, Union[ndarray, float]],\n    per_bin=False,\n) -&gt; jnp.ndarray\n</code></pre> <p>Calculate the Fisher Information matrix at the given parameter values.</p> <p>The Fisher Information quantifies the amount of information that the observed data carries about the model parameters. It is computed by evaluating the gradient of the expected bin counts with respect to the parameters and applying the standard Fisher Information formula when assuming a Poisson likelihood:</p> <pre><code>I_ij = \u03a3_k (\u2202\u03bc_k/\u2202\u03b8_i \u2202\u03bc_k/\u2202\u03b8_j) / \u03bc_k\n</code></pre> <p>where \u03bc_k is the expected count in bin k, and \u03b8_i, \u03b8_j are parameters.</p> <p>Parameters:</p> Name Type Description Default <code>datasets</code> <code>Dict[str, Dict[str, Union[ndarray, float]]]</code> <p>A dictionary mapping  component names to their input variables.</p> required <code>parameter_values</code> <code>Dict[str, Union[ndarray, float]]</code> <p>A dictionary mapping  parameter names to their values.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>jnp.ndarray: The Fisher Information matrix (shape: [n_params, n_params]).</p> Source code in <code>pyForwardFolding/analysis.py</code> <pre><code>def fisher_information(\n    self,\n    datasets: Dict[str, Dict[str, Union[np.ndarray, float]]],\n    parameter_values: Dict[str, Union[np.ndarray, float]],\n    per_bin=False,\n) -&gt; jnp.ndarray:\n    \"\"\"\n    Calculate the Fisher Information matrix at the given parameter values.\n\n    The Fisher Information quantifies the amount of information that the observed data\n    carries about the model parameters. It is computed by evaluating the gradient of\n    the expected bin counts with respect to the parameters and applying the standard\n    Fisher Information formula when assuming a Poisson likelihood:\n\n        I_ij = \u03a3_k (\u2202\u03bc_k/\u2202\u03b8_i \u2202\u03bc_k/\u2202\u03b8_j) / \u03bc_k\n\n    where \u03bc_k is the expected count in bin k, and \u03b8_i, \u03b8_j are parameters.\n\n    Args:\n        datasets (Dict[str, Dict[str, Union[np.ndarray, float]]]): A dictionary mapping \n            component names to their input variables.\n        parameter_values (Dict[str, Union[np.ndarray, float]]): A dictionary mapping \n            parameter names to their values.\n\n    Returns:\n        jnp.ndarray: The Fisher Information matrix (shape: [n_params, n_params]).\n    \"\"\"\n    fisher_dict = {}\n    for comp_name, comp in self.expectations.items():\n        # Bad to evaluate comp.evaluate twice, but unfortunately there is no jacfwd_and_value...\n        grads, _ = jacfwd(comp.evaluate, argnums=1, has_aux=True)(\n            datasets,\n            parameter_values,\n        )\n        hist, _ = comp.evaluate(\n            datasets,\n            parameter_values,\n        )\n\n        # As jacfwd does destroy the key ordering, we need to loop over the keys of parameters values\n        # This will later help to keep track of which variance belongs to which parameter\n        flat_grads = [grads[k].flatten() for k in parameter_values]\n        hist = hist.flatten()\n        information = [jnp.where(hist == 0, 0.0, g / jnp.sqrt(hist)) for g in flat_grads]\n        values = jnp.stack(information)\n        fisher_per_bin = jnp.einsum(\"pk,qk-&gt;kpq\", values, values)\n\n        fisher_dict[comp_name] = fisher_per_bin\n        # else:\n        #     fisher_dict[comp_name] = jnp.sum(fisher_per_bin, axis=0)  # [P, P]\n\n    if per_bin:\n        return fisher_dict \n    else:\n        return {hkey: jnp.sum(fisher_dict[hkey], axis=0) for hkey in fisher_dict.keys()}  # [P, P]\n</code></pre>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.covariance","title":"covariance","text":"<pre><code>covariance(\n    datasets: Dict[str, Dict[str, Union[ndarray, float]]],\n    parameter_values: Dict[str, Union[ndarray, float]],\n    per_hist=False,\n) -&gt; jnp.ndarray\n</code></pre> <p>Compute the covariance matrix from a Fisher Information Matrix directly calculated from the dataset and parameter values. </p> <p>This matrix represents the best-case lower bound on the covariance of any unbiased estimator of the parameters (the Cram\u00e9r\u2013Rao bound).</p> <p>Parameters:</p> Name Type Description Default <code>datasets</code> <code>Dict[str, Dict[str, Union[ndarray, float]]]</code> <p>A dictionary mapping  component names to their input variables.</p> required <code>parameter_values</code> <code>Dict[str, Union[ndarray, float]]</code> <p>A dictionary mapping  parameter names to their values.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>jnp.ndarray: The parameter covariance matrix (shape: [n_params, n_params]).</p> Source code in <code>pyForwardFolding/analysis.py</code> <pre><code>def covariance(\n        self,\n        datasets: Dict[str, Dict[str, Union[np.ndarray, float]]],\n        parameter_values: Dict[str, Union[np.ndarray, float]],\n        per_hist=False,\n) -&gt; jnp.ndarray:\n    \"\"\"\n    Compute the covariance matrix from a Fisher Information Matrix directly calculated\n    from the dataset and parameter values. \n\n    This matrix represents the best-case lower bound on the covariance of any unbiased\n    estimator of the parameters (the Cram\u00e9r\u2013Rao bound).\n\n    Args:\n        datasets (Dict[str, Dict[str, Union[np.ndarray, float]]]): A dictionary mapping \n            component names to their input variables.\n        parameter_values (Dict[str, Union[np.ndarray, float]]): A dictionary mapping \n            parameter names to their values.\n\n    Returns:\n        jnp.ndarray: The parameter covariance matrix (shape: [n_params, n_params]).\n    \"\"\"\n    fisher_information = self.fisher_information(datasets,parameter_values)\n    fim = jnp.sum(jnp.array([v for v in fisher_information.values()]),axis=0)\n    cov = {\"total\": self.covariance_from_fisherinformation(fim)}\n    for hkey, fim in fisher_information.items():\n        cov[hkey] = self.covariance_from_fisherinformation(fim)\n\n\n    return cov\n</code></pre>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.variance","title":"variance","text":"<pre><code>variance(\n    datasets: Dict[str, Dict[str, Union[ndarray, float]]],\n    parameter_values: Dict[str, Union[ndarray, float]],\n) -&gt; Dict[str, jnp.ndarray]\n</code></pre> <p>Extract the variances (diagonal elements) from the parameter covariance matrix.</p> <p>Each variance corresponds to the square of the minimal achievable standard deviation of an unbiased estimator for that parameter.</p> <p>Parameters:</p> Name Type Description Default <code>datasets</code> <code>Dict[str, Dict[str, Union[ndarray, float]]]</code> <p>A dictionary mapping  component names to their input variables.</p> required <code>parameter_values</code> <code>Dict[str, Union[ndarray, float]]</code> <p>A dictionary mapping  parameter names to their values.</p> required <p>Returns:</p> Type Description <code>Dict[str, ndarray]</code> <p>Dict[str, jnp.ndarray]: A dictionary mapping parameter names to their variances.</p> Source code in <code>pyForwardFolding/analysis.py</code> <pre><code>def variance(\n    self,\n    datasets: Dict[str, Dict[str, Union[np.ndarray, float]]],\n    parameter_values: Dict[str, Union[np.ndarray, float]],\n) -&gt; Dict[str, jnp.ndarray]:\n    \"\"\"\n    Extract the variances (diagonal elements) from the parameter covariance matrix.\n\n    Each variance corresponds to the square of the minimal achievable standard deviation\n    of an unbiased estimator for that parameter.\n\n    Args:\n        datasets (Dict[str, Dict[str, Union[np.ndarray, float]]]): A dictionary mapping \n            component names to their input variables.\n        parameter_values (Dict[str, Union[np.ndarray, float]]): A dictionary mapping \n            parameter names to their values.\n\n    Returns:\n        Dict[str, jnp.ndarray]: A dictionary mapping parameter names to their variances.\n    \"\"\"\n    cov = self.covariance(datasets, parameter_values)\n\n    keys = parameter_values.keys()\n    variance = {}\n    for key, c in cov.items():\n        vari = self.variance_from_covariance(c)\n        variance[key] = {k: v for k, v in zip(keys, vari)}\n\n\n\n    return variance\n</code></pre>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.covariance_from_fisherinformation","title":"covariance_from_fisherinformation  <code>staticmethod</code>","text":"<pre><code>covariance_from_fisherinformation(\n    fisher_information: ndarray,\n) -&gt; jnp.ndarray\n</code></pre> <p>Compute the covariance matrix of the parameters by inverting the Fisher Information matrix.</p> <p>This matrix represents the best-case lower bound on the covariance of any unbiased estimator of the parameters (the Cram\u00e9r\u2013Rao bound).</p> <p>Parameters:</p> Name Type Description Default <code>fisher_information</code> <code>ndarray</code> <p>The Fisher Information matrix</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>jnp.ndarray: The parameter covariance matrix (shape: [n_params, n_params]).</p> Source code in <code>pyForwardFolding/analysis.py</code> <pre><code>@staticmethod\ndef covariance_from_fisherinformation(\n    fisher_information: jnp.ndarray\n) -&gt; jnp.ndarray:\n    \"\"\"\n    Compute the covariance matrix of the parameters by inverting the Fisher Information matrix.\n\n    This matrix represents the best-case lower bound on the covariance of any unbiased\n    estimator of the parameters (the Cram\u00e9r\u2013Rao bound).\n\n    Args:\n        fisher_information (jnp.ndarray): The Fisher Information matrix\n\n    Returns:\n        jnp.ndarray: The parameter covariance matrix (shape: [n_params, n_params]).\n    \"\"\"\n    cov = jnp.linalg.inv(fisher_information)\n    return cov\n</code></pre>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.variance_from_covariance","title":"variance_from_covariance  <code>staticmethod</code>","text":"<pre><code>variance_from_covariance(cov: ndarray) -&gt; jnp.ndarray\n</code></pre> <p>Extract the variances (diagonal elements) from the parameter covariance matrix.</p> <p>Each variance corresponds to the square of the minimal achievable standard deviation of an unbiased estimator for that parameter.</p> <p>Parameters:</p> Name Type Description Default <code>cov</code> <code>ndarray</code> <p>The covariance matrix</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>jnp.ndarray: The variances for each parameters (shape: [n_params]).</p> Source code in <code>pyForwardFolding/analysis.py</code> <pre><code>@staticmethod\ndef variance_from_covariance(\n    cov: jnp.ndarray\n) -&gt; jnp.ndarray:\n    \"\"\"\n    Extract the variances (diagonal elements) from the parameter covariance matrix.\n\n    Each variance corresponds to the square of the minimal achievable standard deviation\n    of an unbiased estimator for that parameter.\n\n    Args:\n        cov (jnp.ndarray): The covariance matrix\n\n    Returns:\n        jnp.ndarray: The variances for each parameters (shape: [n_params]).\n    \"\"\"\n    var = jnp.diag(cov)\n    return var\n</code></pre>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>String representation of the Analysis object.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>A comprehensive string representation of the analysis.</p> Source code in <code>pyForwardFolding/analysis.py</code> <pre><code>def __repr__(self):\n    \"\"\"\n    String representation of the Analysis object.\n\n    Returns:\n        str: A comprehensive string representation of the analysis.\n    \"\"\"\n    lines = []\n    lines.append(f\"Analysis with {len(self.expectations)} expectations\")\n    lines.append(\"=\" * 50)\n\n    # Overall summary\n    lines.append(f\"Required variables: {sorted(self.required_variables)}\")\n    lines.append(f\"Exposed parameters: {sorted(self.exposed_parameters)}\")\n    lines.append(\"\")\n\n    # Detailed expectation information\n    for exp_name, expectation in self.expectations.items():\n        lines.append(f\"Expectation: {exp_name}\")\n        lines.append(\"-\" * 30)\n\n        # Dataset keys and models\n        dataset_keys = [dskey for dskey, _ in expectation.dskey_model_pairs]\n        lines.append(f\"  Dataset keys: {dataset_keys}\")\n\n        # Models and components with detailed information\n        lines.append(f\"  Models ({len(expectation.models)}):\")\n        for model in expectation.models:\n            # Use the model's __repr__ method and indent it\n            model_repr = str(model).replace(\"\\n\", \"\\n    \")\n            lines.append(f\"    {model_repr}\")\n\n        # Binning information using its __repr__ method\n        lines.append(\"  Binning:\")\n        binning_repr = str(expectation.binning).replace(\"\\n\", \"\\n    \")\n        lines.append(f\"    {binning_repr}\")\n\n        # Binned factors with detailed information\n        if expectation.binned_factors:\n            lines.append(f\"  Binned factors ({len(expectation.binned_factors)}):\")\n            for factor in expectation.binned_factors:\n                factor_repr = str(factor).replace(\"\\n\", \"\\n    \")\n                lines.append(f\"    {factor_repr}\")\n\n        # Variables and parameters for this expectation\n        lines.append(\n            f\"  Required variables: {sorted(expectation.required_variables)}\"\n        )\n        lines.append(\n            f\"  Exposed parameters: {sorted(expectation.exposed_parameters)}\"\n        )\n        lines.append(f\"  Lifetime: {expectation.lifetime}\")\n        lines.append(\"\")\n\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.repr_markdown","title":"repr_markdown","text":"<pre><code>repr_markdown(\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_summary: bool = True,\n    show_expectation_details: bool = True,\n    show_model_details: bool = True,\n    show_binning_details: bool = True,\n) -&gt; str\n</code></pre> <p>Configurable markdown representation of the Analysis object.</p> <p>Parameters:</p> Name Type Description Default <code>indent_level</code> <code>int</code> <p>The level of indentation (each level adds 2 spaces). Default is 0.</p> <code>0</code> <code>bullet_style</code> <code>str</code> <p>Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".</p> <code>'-'</code> <code>include_summary</code> <code>bool</code> <p>Whether to include overall summary information. Default is True.</p> <code>True</code> <code>show_expectation_details</code> <code>bool</code> <p>Whether to show detailed expectation information. Default is True.</p> <code>True</code> <code>show_model_details</code> <code>bool</code> <p>Whether to show detailed model information. Default is True.</p> <code>True</code> <code>show_binning_details</code> <code>bool</code> <p>Whether to show detailed binning information. Default is True.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A configurable markdown representation of the analysis.</p> Source code in <code>pyForwardFolding/analysis.py</code> <pre><code>def repr_markdown(\n    self,\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_summary: bool = True,\n    show_expectation_details: bool = True,\n    show_model_details: bool = True,\n    show_binning_details: bool = True,\n) -&gt; str:\n    \"\"\"\n    Configurable markdown representation of the Analysis object.\n\n    Args:\n        indent_level (int): The level of indentation (each level adds 2 spaces). Default is 0.\n        bullet_style (str): Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".\n        include_summary (bool): Whether to include overall summary information. Default is True.\n        show_expectation_details (bool): Whether to show detailed expectation information. Default is True.\n        show_model_details (bool): Whether to show detailed model information. Default is True.\n        show_binning_details (bool): Whether to show detailed binning information. Default is True.\n\n    Returns:\n        str: A configurable markdown representation of the analysis.\n    \"\"\"\n    indent = \"  \" * indent_level\n    sub_indent = \"  \" * (indent_level + 1)\n\n    lines = []\n\n    # Header with appropriate level based on indent\n    if indent_level == 0:\n        lines.append(\"# Analysis Configuration\")\n        lines.append(f\"**{len(self.expectations)} expectations configured**\")\n    else:\n        lines.append(f\"{indent}{bullet_style} **Analysis Configuration**\")\n        lines.append(\n            f\"{sub_indent}{bullet_style} {len(self.expectations)} expectations configured\"\n        )\n\n    lines.append(\"\")\n\n    # Overall summary\n    if include_summary:\n        if indent_level == 0:\n            lines.append(\"## Summary\")\n            lines.append(\"| Aspect | Details |\")\n            lines.append(\"|--------|---------|\")\n            lines.append(\n                f\"| Required variables | `{', '.join(sorted(self.required_variables))}` |\"\n            )\n            lines.append(\n                f\"| Exposed parameters | `{', '.join(sorted(self.exposed_parameters))}` |\"\n            )\n        else:\n            lines.append(f\"{sub_indent}{bullet_style} Summary:\")\n            summary_indent = \"  \" * (indent_level + 2)\n            lines.append(\n                f\"{summary_indent}{bullet_style} Required variables: `{', '.join(sorted(self.required_variables))}`\"\n            )\n            lines.append(\n                f\"{summary_indent}{bullet_style} Exposed parameters: `{', '.join(sorted(self.exposed_parameters))}`\"\n            )\n        lines.append(\"\")\n\n    # Detailed expectation information\n    if show_expectation_details:\n        for exp_name, expectation in self.expectations.items():\n            exp_md = expectation.repr_markdown(\n                indent_level=indent_level + 1\n                if indent_level == 0\n                else indent_level + 2,\n                bullet_style=bullet_style,\n                include_summary=include_summary,\n                show_model_details=show_model_details,\n                show_binning_details=show_binning_details,\n            )\n            lines.append(exp_md)\n            lines.append(\"\")\n    else:\n        # Just list expectation names\n        if indent_level == 0:\n            lines.append(\"## Expectations:\")\n        else:\n            lines.append(f\"{sub_indent}{bullet_style} Expectations:\")\n\n        for exp_name in self.expectations.keys():\n            exp_indent = (\n                sub_indent if indent_level == 0 else \"  \" * (indent_level + 2)\n            )\n            lines.append(f\"{exp_indent}{bullet_style} `{exp_name}`\")\n        lines.append(\"\")\n\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.__getitem__","title":"__getitem__","text":"<pre><code>__getitem__(item: str) -&gt; BinnedExpectation\n</code></pre> <p>Get a specific expectation by name.</p> <p>Parameters:</p> Name Type Description Default <code>item</code> <code>str</code> <p>The name of the expectation.</p> required <p>Returns:</p> Name Type Description <code>BinnedExpectation</code> <code>BinnedExpectation</code> <p>The corresponding expectation object.</p> Source code in <code>pyForwardFolding/analysis.py</code> <pre><code>def __getitem__(self, item: str) -&gt; BinnedExpectation:\n    \"\"\"\n    Get a specific expectation by name.\n\n    Args:\n        item (str): The name of the expectation.\n\n    Returns:\n        BinnedExpectation: The corresponding expectation object.\n    \"\"\"\n    return self.expectations[item]\n</code></pre>"},{"location":"reference/api/analysis/#pyForwardFolding.analysis.Analysis.render_graph","title":"render_graph","text":"<pre><code>render_graph(\n    figsize: Tuple[int, int] = (12, 8),\n    node_size: int = 3000,\n    font_size: int = 10,\n    show_datasets: bool = True,\n    show_parameters: bool = True,\n    layout: str = \"spring\",\n    engine: str = \"networkx\",\n)\n</code></pre> <p>Render the analysis object and its subcomponents as a graph visualization. Uses NetworkX for graph creation and either NetworkX/matplotlib or Graphviz for rendering.</p> <p>Parameters:</p> Name Type Description Default <code>figsize</code> <code>Tuple[int, int]</code> <p>Figure size as (width, height) tuple</p> <code>(12, 8)</code> <code>node_size</code> <code>int</code> <p>Base size of nodes in the graph (used when auto_size_nodes=False and engine='networkx')</p> <code>3000</code> <code>font_size</code> <code>int</code> <p>Font size for node labels</p> <code>10</code> <code>show_datasets</code> <code>bool</code> <p>Whether to show dataset nodes</p> <code>True</code> <code>show_parameters</code> <code>bool</code> <p>Whether to show parameter nodes</p> <code>True</code> <code>layout</code> <code>str</code> <p>Layout algorithm ('spring', 'hierarchical', 'circular' for NetworkX; 'dot', 'neato', 'fdp', 'sfdp', 'circo', 'twopi' for Graphviz)</p> <code>'spring'</code> <code>engine</code> <code>str</code> <p>Rendering engine ('networkx' or 'graphviz')</p> <code>'networkx'</code> <p>Returns:</p> Type Description <p>matplotlib figure object (NetworkX) or Graphviz AGraph object (Graphviz)</p> Source code in <code>pyForwardFolding/analysis.py</code> <pre><code>def render_graph(\n    self,\n    figsize: Tuple[int, int] = (12, 8),\n    node_size: int = 3000,\n    font_size: int = 10,\n    show_datasets: bool = True,\n    show_parameters: bool = True,\n    layout: str = \"spring\",\n    engine: str = \"networkx\",\n):\n    \"\"\"\n    Render the analysis object and its subcomponents as a graph visualization.\n    Uses NetworkX for graph creation and either NetworkX/matplotlib or Graphviz for rendering.\n\n    Args:\n        figsize: Figure size as (width, height) tuple\n        node_size: Base size of nodes in the graph (used when auto_size_nodes=False and engine='networkx')\n        font_size: Font size for node labels\n        show_datasets: Whether to show dataset nodes\n        show_parameters: Whether to show parameter nodes\n        layout: Layout algorithm ('spring', 'hierarchical', 'circular' for NetworkX; 'dot', 'neato', 'fdp', 'sfdp', 'circo', 'twopi' for Graphviz)\n        engine: Rendering engine ('networkx' or 'graphviz')\n\n    Returns:\n        matplotlib figure object (NetworkX) or Graphviz AGraph object (Graphviz)\n    \"\"\"\n    # Create the NetworkX graph structure (shared by both engines)\n    G = self._create_networkx_graph(\n        show_datasets=show_datasets, show_parameters=show_parameters\n    )\n\n    if engine.lower() == \"graphviz\":\n        return self._render_with_graphviz(\n            G,\n            figsize=figsize,\n            font_size=font_size,\n            layout=layout,\n        )\n    else:\n        return self._render_with_networkx(\n            G,\n            figsize=figsize,\n            node_size=node_size,\n            font_size=font_size,\n            layout=layout,\n            show_parameters=show_parameters,\n        )\n</code></pre>"},{"location":"reference/api/backend/","title":"<code>pyForwardFolding.backend</code>","text":"<p>The JAX-based numerical backend. The <code>Backend</code> protocol defines the operations that factors, binnings, and likelihoods rely on; <code>JAXBackend</code> is the concrete implementation. The default instance is exposed as <code>pyForwardFolding.backend.backend</code>.</p> <p>If you write custom factors, prefer <code>backend.&lt;op&gt;</code> over importing JAX or NumPy directly \u2014 it keeps your code differentiable and JIT-compatible.</p>"},{"location":"reference/api/backend/#pyForwardFolding.backend","title":"pyForwardFolding.backend","text":""},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend","title":"Backend","text":"<pre><code>Backend(rng_seed: int = 0)\n</code></pre> <p>               Bases: <code>Protocol</code></p> <p>Protocol for backend interface for numerical operations.</p> <p>Initialize the backend with an optional random seed.</p> <p>Parameters:</p> Name Type Description Default <code>rng_seed</code> <code>int</code> <p>Random seed for reproducibility.</p> <code>0</code> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def __init__(self, rng_seed: int = 0):\n    \"\"\"\n    Initialize the backend with an optional random seed.\n\n    Args:\n        rng_seed (int): Random seed for reproducibility.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.array","title":"array","text":"<pre><code>array(data: Any, dtype: Any = None) -&gt; Array\n</code></pre> <p>Create an array from data.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def array(self, data: Any, dtype: Any = None) -&gt; Array:\n    \"\"\"Create an array from data.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.asarray","title":"asarray","text":"<pre><code>asarray(data: Any, dtype: Any = None) -&gt; Array\n</code></pre> <p>Create an array from data without copying if possible.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def asarray(self, data: Any, dtype: Any = None) -&gt; Array:\n    \"\"\"Create an array from data without copying if possible.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.zeros","title":"zeros","text":"<pre><code>zeros(shape: Any, dtype: Any = None) -&gt; Array\n</code></pre> <p>Create an array of zeros.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def zeros(self, shape: Any, dtype: Any = None) -&gt; Array:\n    \"\"\"Create an array of zeros.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.abs","title":"abs","text":"<pre><code>abs(x: Array) -&gt; Array\n</code></pre> <p>Element-wise absolute</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def abs(self, x: Array) -&gt; Array:\n    \"\"\"Element-wise absolute\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.power","title":"power","text":"<pre><code>power(a: Any, b: Any) -&gt; Array\n</code></pre> <p>Element-wise power operation.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def power(self, a: Any, b: Any) -&gt; Array:\n    \"\"\"Element-wise power operation.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.exp","title":"exp","text":"<pre><code>exp(a: Any) -&gt; Array\n</code></pre> <p>Element-wise exponential.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def exp(self, a: Any) -&gt; Array:\n    \"\"\"Element-wise exponential.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.sqrt","title":"sqrt","text":"<pre><code>sqrt(a: Any) -&gt; Array\n</code></pre> <p>Element-wise square root.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def sqrt(self, a: Any) -&gt; Array:\n    \"\"\"Element-wise square root.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.erf","title":"erf","text":"<pre><code>erf(a: Any) -&gt; Array\n</code></pre> <p>Element-wise error function.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def erf(self, a: Any) -&gt; Array:\n    \"\"\"Element-wise error function.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.fasterf","title":"fasterf","text":"<pre><code>fasterf(x: Any) -&gt; Array\n</code></pre> <p>Fast approximation of error function.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def fasterf(self, x: Any) -&gt; Array:\n    \"\"\"Fast approximation of error function.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.gauss_pdf","title":"gauss_pdf","text":"<pre><code>gauss_pdf(x: Any, mu: Any, sigma: Any) -&gt; Array\n</code></pre> <p>Gaussian probability density function.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def gauss_pdf(self, x: Any, mu: Any, sigma: Any) -&gt; Array:\n    \"\"\"Gaussian probability density function.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.gauss_cdf","title":"gauss_cdf","text":"<pre><code>gauss_cdf(x: Any, mu: Any, sigma: Any) -&gt; Array\n</code></pre> <p>Gaussian cumulative distribution function.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def gauss_cdf(self, x: Any, mu: Any, sigma: Any) -&gt; Array:\n    \"\"\"Gaussian cumulative distribution function.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.uniform_pdf","title":"uniform_pdf","text":"<pre><code>uniform_pdf(x: Any, lo: Any, hi: Any) -&gt; Array\n</code></pre> <p>Uniform probability density function.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def uniform_pdf(self, x: Any, lo: Any, hi: Any) -&gt; Array:\n    \"\"\"Uniform probability density function.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.set_index","title":"set_index","text":"<pre><code>set_index(x: ArrayLike, index: Any, values: Any) -&gt; Array\n</code></pre> <p>Set values at specified indices.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def set_index(self, x: ArrayLike, index: Any, values: Any) -&gt; Array:\n    \"\"\"Set values at specified indices.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.fill","title":"fill","text":"<pre><code>fill(x: ArrayLike, value: Any) -&gt; Array\n</code></pre> <p>Fill array with a value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def fill(self, x: ArrayLike, value: Any) -&gt; Array:\n    \"\"\"Fill array with a value.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.histogram","title":"histogram","text":"<pre><code>histogram(x: Any, bins: Any, weights: Any) -&gt; Array\n</code></pre> <p>Compute histogram.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def histogram(self, x: Any, bins: Any, weights: Any) -&gt; Array:\n    \"\"\"Compute histogram.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.bincount","title":"bincount","text":"<pre><code>bincount(x: Array, weights: Array, length: int) -&gt; Array\n</code></pre> <p>Count occurrences of each value in array.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def bincount(self, x: Array, weights: Array, length: int) -&gt; Array:\n    \"\"\"Count occurrences of each value in array.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.reshape","title":"reshape","text":"<pre><code>reshape(x: Array, shape: Any) -&gt; Array\n</code></pre> <p>Reshape array.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def reshape(self, x: Array, shape: Any) -&gt; Array:\n    \"\"\"Reshape array.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.set_index_add","title":"set_index_add","text":"<pre><code>set_index_add(x: Array, index: Any, values: Any) -&gt; Array\n</code></pre> <p>Add values at specified indices.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def set_index_add(self, x: Array, index: Any, values: Any) -&gt; Array:\n    \"\"\"Add values at specified indices.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.sort","title":"sort","text":"<pre><code>sort(x: Array) -&gt; Array\n</code></pre> <p>Sort an array</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def sort(self, x: Array) -&gt; Array:\n    \"\"\"Sort an array\"\"\" \n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.searchsorted","title":"searchsorted","text":"<pre><code>searchsorted(\n    a: Array, v: Array, side: str = \"left\"\n) -&gt; Array\n</code></pre> <p>Find indices where elements should be inserted to maintain order.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def searchsorted(self, a: Array, v: Array, side: str = \"left\") -&gt; Array:\n    \"\"\"Find indices where elements should be inserted to maintain order.\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.ravel_multi_index","title":"ravel_multi_index","text":"<pre><code>ravel_multi_index(multi_index: Any, dims: Any) -&gt; Array\n</code></pre> <p>Converts a multi-dimensional index into a flat index.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def ravel_multi_index(self, multi_index: Any, dims: Any) -&gt; Array:\n    \"\"\"\n    Converts a multi-dimensional index into a flat index.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.clip","title":"clip","text":"<pre><code>clip(x: Array, low: Any, high: Any) -&gt; Array\n</code></pre> <p>Clamps the values in x to be within the range [low, high].</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def clip(self, x: Array, low: Any, high: Any) -&gt; Array:\n    \"\"\"\n    Clamps the values in x to be within the range [low, high].\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.linspace","title":"linspace","text":"<pre><code>linspace(start: float, stop: float, num: int) -&gt; Array\n</code></pre> <p>Returns evenly spaced numbers over a specified interval.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def linspace(self, start: float, stop: float, num: int) -&gt; Array:\n    \"\"\"\n    Returns evenly spaced numbers over a specified interval.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.where_sum","title":"where_sum","text":"<pre><code>where_sum(\n    condition: Array, x: Array, y: ArrayLike\n) -&gt; ArrayLike\n</code></pre> <p>Returns the sum of an array with elements from x where condition is True, and from y otherwise.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def where_sum(self, condition: Array, x: Array, y: ArrayLike) -&gt; ArrayLike:\n    \"\"\"\n    Returns the sum of an array with elements from x where condition is True, and from y otherwise.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.log","title":"log","text":"<pre><code>log(x: Any) -&gt; Array\n</code></pre> <p>Computes the natural logarithm of x.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def log(self, x: Any) -&gt; Array:\n    \"\"\"\n    Computes the natural logarithm of x.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.log10","title":"log10","text":"<pre><code>log10(x: Any) -&gt; Array\n</code></pre> <p>Computes the base-10 logarithm of x.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def log10(self, x: Any) -&gt; Array:\n    \"\"\"\n    Computes the base-10 logarithm of x.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.diff","title":"diff","text":"<pre><code>diff(x: Any) -&gt; Array\n</code></pre> <p>Computes the discrete difference along the specified axis.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def diff(self, x: Any) -&gt; Array:\n    \"\"\"\n    Computes the discrete difference along the specified axis.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.allclose","title":"allclose","text":"<pre><code>allclose(a: Any, b: Any, atol: float = 1e-08) -&gt; Array\n</code></pre> <p>Checks if two arrays are element-wise equal within a tolerance.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def allclose(self, a: Any, b: Any, atol: float = 1e-8) -&gt; Array:\n    \"\"\"\n    Checks if two arrays are element-wise equal within a tolerance.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.sum","title":"sum","text":"<pre><code>sum(x: Array, axis: Any = None) -&gt; Array\n</code></pre> <p>Computes the sum of array elements over a given axis.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def sum(self, x: Array, axis: Any = None) -&gt; Array:\n    \"\"\"\n    Computes the sum of array elements over a given axis.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.tanh","title":"tanh","text":"<pre><code>tanh(x: Any) -&gt; Any\n</code></pre> <p>Computes the hyperbolic tangent of x.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def tanh(self, x: Any) -&gt; Any:\n    \"\"\"\n    Computes the hyperbolic tangent of x.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.digitize","title":"digitize","text":"<pre><code>digitize(x: Array, bins: Array) -&gt; Array\n</code></pre> <p>Returns the indices of the bins to which each value in x belongs.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def digitize(self, x: Array, bins: Array) -&gt; Array:\n    \"\"\"\n    Returns the indices of the bins to which each value in x belongs.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.any","title":"any","text":"<pre><code>any(x: Array) -&gt; Array\n</code></pre> <p>Returns True if any element in x is True</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def any(self, x: Array) -&gt; Array:\n    \"\"\"\n    Returns True if any element in x is True\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.where","title":"where","text":"<pre><code>where(cond: Any, x: Any, y: Any) -&gt; Array\n</code></pre> <p>Depending on cond return value from x or y</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def where(self, cond: Any, x: Any, y: Any) -&gt; Array:\n    \"\"\"\n    Depending on cond return value from x or y\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.sigmoid","title":"sigmoid","text":"<pre><code>sigmoid(x: Array) -&gt; Array\n</code></pre> <p>Sigmoid function</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def sigmoid(self, x: Array) -&gt; Array:\n    \"\"\"\n    Sigmoid function\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.select","title":"select","text":"<pre><code>select(\n    condition: Array, x: ArrayLike, y: ArrayLike\n) -&gt; Array\n</code></pre> <p>Select elements from x or y depending on condition.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def select(self, condition: Array, x: ArrayLike, y: ArrayLike) -&gt; Array:\n    \"\"\"\n    Select elements from x or y depending on condition.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.gammaln","title":"gammaln","text":"<pre><code>gammaln(x: Array) -&gt; Array\n</code></pre> <p>Natural logarithm of the gamma function.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def gammaln(self, x: Array) -&gt; Array:\n    \"\"\"\n    Natural logarithm of the gamma function.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.func_and_grad","title":"func_and_grad","text":"<pre><code>func_and_grad(func: Any) -&gt; Any\n</code></pre> <p>Return a function that computes both value and gradient.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def func_and_grad(self, func: Any) -&gt; Any:\n    \"\"\"\n    Return a function that computes both value and gradient.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.compile","title":"compile","text":"<pre><code>compile(func: Any) -&gt; Any\n</code></pre> <p>Compile a function for faster execution.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def compile(self, func: Any) -&gt; Any:\n    \"\"\"\n    Compile a function for faster execution.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.grad","title":"grad","text":"<pre><code>grad(func: Any) -&gt; Any\n</code></pre> <p>Return the gradient of a function.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def grad(self, func: Any) -&gt; Any:\n    \"\"\"\n    Return the gradient of a function.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.logspace","title":"logspace","text":"<pre><code>logspace(start: float, stop: float, num: int) -&gt; Array\n</code></pre> <p>Return numbers spaced evenly on a log scale.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def logspace(self, start: float, stop: float, num: int) -&gt; Array:\n    \"\"\"\n    Return numbers spaced evenly on a log scale.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.sin","title":"sin","text":"<pre><code>sin(x: Array) -&gt; Array\n</code></pre> <p>Trigonometric sine, element-wise.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def sin(self, x: Array) -&gt; Array:\n    \"\"\"\n    Trigonometric sine, element-wise.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.cos","title":"cos","text":"<pre><code>cos(x: Array) -&gt; Array\n</code></pre> <p>Trigonometric cosine, element-wise.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def cos(self, x: Array) -&gt; Array:\n    \"\"\"\n    Trigonometric cosine, element-wise.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.arcsin","title":"arcsin","text":"<pre><code>arcsin(x: Array) -&gt; Array\n</code></pre> <p>Trigonometric inverse sine, element-wise.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def arcsin(self, x: Array) -&gt; Array:\n    \"\"\"\n    Trigonometric inverse sine, element-wise.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.arccos","title":"arccos","text":"<pre><code>arccos(x: Array) -&gt; Array\n</code></pre> <p>Trigonometric inverse cosine, element-wise.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def arccos(self, x: Array) -&gt; Array:\n    \"\"\"\n    Trigonometric inverse cosine, element-wise.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.arg_weighted_quantile","title":"arg_weighted_quantile","text":"<pre><code>arg_weighted_quantile(\n    x: Array, weights: Array, quantile: float\n) -&gt; Array\n</code></pre> <p>Return the index of the weighted quantile.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def arg_weighted_quantile(self, x: Array, weights: Array, quantile: float) -&gt; Array:\n    \"\"\"\n    Return the index of the weighted quantile.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.weighted_quantile","title":"weighted_quantile","text":"<pre><code>weighted_quantile(\n    x: Array, weights: Array, quantile: float\n) -&gt; Array\n</code></pre> <p>Return the weighted quantile.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def weighted_quantile(self, x: Array, weights: Array, quantile: float) -&gt; Array:\n    \"\"\"\n    Return the weighted quantile.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.weighted_median","title":"weighted_median","text":"<pre><code>weighted_median(x: Array, weights: Array) -&gt; Array\n</code></pre> <p>Return the weighted median.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def weighted_median(self, x: Array, weights: Array) -&gt; Array:\n    \"\"\"\n    Return the weighted median.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.quantile","title":"quantile","text":"<pre><code>quantile(x: Array, q: Array) -&gt; Array\n</code></pre> <p>Compute the quantile of an array.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Array</code> <p>Input array.</p> required <code>q</code> <code>float</code> <p>Quantile to compute (0 &lt;= q &lt;= 1).</p> required <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>The computed quantile value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def quantile(self, x: Array, q: Array) -&gt; Array:\n    \"\"\"\n    Compute the quantile of an array.\n\n    Args:\n        x (Array): Input array.\n        q (float): Quantile to compute (0 &lt;= q &lt;= 1).\n\n    Returns:\n        Array: The computed quantile value.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.repeat","title":"repeat","text":"<pre><code>repeat(\n    x: Array, repeats, axis=None, total_repeat_length=None\n) -&gt; Array\n</code></pre> <p>Repeat elements of an array.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def repeat(self, x: Array, repeats, axis=None, total_repeat_length=None) -&gt; Array:\n    \"\"\"\n    Repeat elements of an array.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.poiss_rng","title":"poiss_rng","text":"<pre><code>poiss_rng(lam: Array) -&gt; Array\n</code></pre> <p>Generate random numbers from a Poisson distribution.</p> <p>Parameters:</p> Name Type Description Default <code>lam</code> <code>Array</code> <p>The rate parameter (lambda) of the Poisson distribution.</p> required <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>Random numbers drawn from the Poisson distribution.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def poiss_rng(self, lam: Array) -&gt; Array:\n    \"\"\"\n    Generate random numbers from a Poisson distribution.\n\n    Args:\n        lam (Array): The rate parameter (lambda) of the Poisson distribution.\n\n    Returns:\n        Array: Random numbers drawn from the Poisson distribution.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.chi2_logsf","title":"chi2_logsf","text":"<pre><code>chi2_logsf(x: ArrayLike, df: int) -&gt; Array\n</code></pre> <p>Compute the log survival function of the chi-squared distribution.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Array</code> <p>The value at which to evaluate the log survival function.</p> required <code>df</code> <code>int</code> <p>Degrees of freedom of the chi-squared distribution.</p> required <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>The log survival function value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def chi2_logsf(self, x: ArrayLike, df: int) -&gt; Array:\n    \"\"\"\n    Compute the log survival function of the chi-squared distribution.\n\n    Args:\n        x (Array): The value at which to evaluate the log survival function.\n        df (int): Degrees of freedom of the chi-squared distribution.\n\n    Returns:\n        Array: The log survival function value.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.norm_ppf","title":"norm_ppf","text":"<pre><code>norm_ppf(p: ArrayLike) -&gt; Array\n</code></pre> <p>Compute the percent point function (inverse CDF) of the normal distribution.</p> <p>Parameters:</p> Name Type Description Default <code>p</code> <code>float</code> <p>The probability value for which to compute the PPF.</p> required <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>The PPF value corresponding to the given probability.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def norm_ppf(self, p: ArrayLike) -&gt; Array:\n    \"\"\"\n    Compute the percent point function (inverse CDF) of the normal distribution.\n\n    Args:\n        p (float): The probability value for which to compute the PPF.\n\n    Returns:\n        Array: The PPF value corresponding to the given probability.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.norm_sf","title":"norm_sf","text":"<pre><code>norm_sf(x: ArrayLike) -&gt; Array\n</code></pre> <p>Compute the survival function (1 - CDF) of the normal distribution.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ArrayLike</code> <p>The value at which to evaluate the survival function.</p> required <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>The survival function value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def norm_sf(self, x: ArrayLike) -&gt; Array:\n    \"\"\"\n    Compute the survival function (1 - CDF) of the normal distribution.\n\n    Args:\n        x (ArrayLike): The value at which to evaluate the survival function.\n\n    Returns:\n        Array: The survival function value.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.median","title":"median","text":"<pre><code>median(x: ArrayLike) -&gt; Array\n</code></pre> <p>Compute the median of an array.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ArrayLike</code> <p>Input array.</p> required <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>The median value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def median(self, x: ArrayLike) -&gt; Array:\n    \"\"\"\n    Compute the median of an array.\n\n    Args:\n        x (ArrayLike): Input array.\n\n    Returns:\n        Array: The median value.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.mean","title":"mean","text":"<pre><code>mean(x: ArrayLike) -&gt; Array\n</code></pre> <p>Compute the mean of an array.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ArrayLike</code> <p>Input array.</p> required <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>The mean value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def mean(self, x: ArrayLike) -&gt; Array:\n    \"\"\"\n    Compute the mean of an array.\n\n    Args:\n        x (ArrayLike): Input array.\n\n    Returns:\n        Array: The mean value.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.mod","title":"mod","text":"<pre><code>mod(a: ArrayLike, b: ArrayLike) -&gt; Array\n</code></pre> <p>Calculate the remainder</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def mod(self, a: ArrayLike, b: ArrayLike) -&gt; Array:\n    \"\"\"Calculate the remainder\"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.Backend.max","title":"max","text":"<pre><code>max(x: ArrayLike) -&gt; Array\n</code></pre> <p>Compute the maximum value of an array.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ArrayLike</code> <p>Input array.</p> required <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>The maximum value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def max(self, x: ArrayLike) -&gt; Array:\n    \"\"\"\n    Compute the maximum value of an array.\n\n    Args:\n        x (ArrayLike): Input array.\n\n    Returns:\n        Array: The maximum value.\n    \"\"\"\n    ...\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.JAXBackend","title":"JAXBackend","text":"<pre><code>JAXBackend(rng_seed: int = 0)\n</code></pre> <p>JAX implementation of the backend interface.</p> <p>Note: This class doesn't need to inherit from Backend protocol because it uses structural typing - any class that implements all the required methods automatically satisfies the protocol.</p> <p>Initialize the JAX backend with an optional random seed.</p> <p>Parameters:</p> Name Type Description Default <code>rng_seed</code> <code>int</code> <p>Random seed for reproducibility.</p> <code>0</code> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def __init__(self, rng_seed: int = 0):\n    \"\"\"\n    Initialize the JAX backend with an optional random seed.\n\n    Args:\n        rng_seed (int): Random seed for reproducibility.\n    \"\"\"\n    self.rng_key = jax.random.PRNGKey(rng_seed)\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.JAXBackend.quantile","title":"quantile","text":"<pre><code>quantile(x: ArrayLike, q: ArrayLike) -&gt; JAXArray\n</code></pre> <p>Compute the quantile of an array.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ArrayLike</code> <p>Input array.</p> required <code>q</code> <code>float</code> <p>Quantile to compute (0 &lt;= q &lt;= 1).</p> required <p>Returns:</p> Name Type Description <code>JAXArray</code> <code>Array</code> <p>The computed quantile value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def quantile(self, x: ArrayLike, q: ArrayLike) -&gt; JAXArray:\n    \"\"\"\n    Compute the quantile of an array.\n\n    Args:\n        x (ArrayLike): Input array.\n        q (float): Quantile to compute (0 &lt;= q &lt;= 1).\n\n    Returns:\n        JAXArray: The computed quantile value.\n    \"\"\"\n    x = jnp.asarray(x)\n    return jnp.quantile(x, q)\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.JAXBackend.repeat","title":"repeat","text":"<pre><code>repeat(\n    x: ArrayLike,\n    repeats,\n    axis=None,\n    total_repeat_length=None,\n) -&gt; JAXArray\n</code></pre> <p>Repeat elements of an array.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def repeat(\n    self, x: ArrayLike, repeats, axis=None, total_repeat_length=None\n) -&gt; JAXArray:\n    \"\"\"\n    Repeat elements of an array.\n    \"\"\"\n    x = jnp.asarray(x)\n    return jnp.repeat(\n        x, repeats, axis=axis, total_repeat_length=total_repeat_length\n    )\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.JAXBackend.poiss_rng","title":"poiss_rng","text":"<pre><code>poiss_rng(lam: ArrayLike) -&gt; JAXArray\n</code></pre> <p>Generate random numbers from a Poisson distribution.</p> <p>Parameters:</p> Name Type Description Default <code>lam</code> <code>ArrayLike</code> <p>The rate parameter (lambda) of the Poisson distribution.</p> required <code>seed</code> <code>int</code> <p>Random seed for reproducibility.</p> required <p>Returns:</p> Name Type Description <code>JAXArray</code> <code>Array</code> <p>Random numbers drawn from the Poisson distribution.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def poiss_rng(self, lam: ArrayLike) -&gt; JAXArray:\n    \"\"\"\n    Generate random numbers from a Poisson distribution.\n\n    Args:\n        lam (ArrayLike): The rate parameter (lambda) of the Poisson distribution.\n        seed (int): Random seed for reproducibility.\n\n    Returns:\n        JAXArray: Random numbers drawn from the Poisson distribution.\n    \"\"\"\n    key, new_key = jax.random.split(self.rng_key)\n    self.rng_key = new_key\n    lam = jnp.asarray(lam)\n    return jax.random.poisson(key, jnp.asarray(lam))\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.JAXBackend.chi2_logsf","title":"chi2_logsf","text":"<pre><code>chi2_logsf(x: ArrayLike, df: int) -&gt; JAXArray\n</code></pre> <p>Compute the log survival function of the chi-squared distribution.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ArrayLike</code> <p>The value at which to evaluate the log survival function.</p> required <code>df</code> <code>int</code> <p>Degrees of freedom of the chi-squared distribution.</p> required <p>Returns:</p> Name Type Description <code>JAXArray</code> <code>Array</code> <p>The log survival function value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def chi2_logsf(self, x: ArrayLike, df: int) -&gt; JAXArray:\n    \"\"\"\n    Compute the log survival function of the chi-squared distribution.\n\n    Args:\n        x (ArrayLike): The value at which to evaluate the log survival function.\n        df (int): Degrees of freedom of the chi-squared distribution.\n\n    Returns:\n        JAXArray: The log survival function value.\n    \"\"\"\n    x = jnp.asarray(x)\n    return jax.scipy.stats.chi2.logsf(x, df)\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.JAXBackend.norm_ppf","title":"norm_ppf","text":"<pre><code>norm_ppf(p: ArrayLike) -&gt; JAXArray\n</code></pre> <p>Compute the percent point function (inverse CDF) of the normal distribution.</p> <p>Parameters:</p> Name Type Description Default <code>p</code> <code>ArrayLike</code> <p>The probability value for which to compute the PPF.</p> required <p>Returns:</p> Name Type Description <code>JAXArray</code> <code>Array</code> <p>The PPF value corresponding to the given probability.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def norm_ppf(self, p: ArrayLike) -&gt; JAXArray:\n    \"\"\"\n    Compute the percent point function (inverse CDF) of the normal distribution.\n\n    Args:\n        p (ArrayLike): The probability value for which to compute the PPF.\n\n    Returns:\n        JAXArray: The PPF value corresponding to the given probability.\n    \"\"\"\n    p = jnp.asarray(p)\n    return jax.scipy.stats.norm.ppf(p)\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.JAXBackend.norm_sf","title":"norm_sf","text":"<pre><code>norm_sf(x: ArrayLike) -&gt; JAXArray\n</code></pre> <p>Compute the survival function (1 - CDF) of the normal distribution.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ArrayLike</code> <p>The value at which to evaluate the survival function.</p> required <p>Returns:</p> Name Type Description <code>JAXArray</code> <code>Array</code> <p>The survival function value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def norm_sf(self, x: ArrayLike) -&gt; JAXArray:\n    \"\"\"\n    Compute the survival function (1 - CDF) of the normal distribution.\n\n    Args:\n        x (ArrayLike): The value at which to evaluate the survival function.\n\n    Returns:\n        JAXArray: The survival function value.\n    \"\"\"\n    x = jnp.asarray(x)\n    return jax.scipy.stats.norm.sf(x)\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.JAXBackend.median","title":"median","text":"<pre><code>median(x: ArrayLike) -&gt; JAXArray\n</code></pre> <p>Compute the median of an array.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ArrayLike</code> <p>Input array.</p> required <p>Returns:</p> Name Type Description <code>JAXArray</code> <code>Array</code> <p>The median value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def median(self, x: ArrayLike) -&gt; JAXArray:\n    \"\"\"\n    Compute the median of an array.\n\n    Args:\n        x (ArrayLike): Input array.\n\n    Returns:\n        JAXArray: The median value.\n    \"\"\"\n    x = jnp.asarray(x)\n    return jnp.median(x)\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.JAXBackend.mean","title":"mean","text":"<pre><code>mean(x: ArrayLike) -&gt; JAXArray\n</code></pre> <p>Compute the mean of an array.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ArrayLike</code> <p>Input array.</p> required <p>Returns:</p> Name Type Description <code>JAXArray</code> <code>Array</code> <p>The mean value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def mean(self, x: ArrayLike) -&gt; JAXArray:\n    \"\"\"\n    Compute the mean of an array.\n\n    Args:\n        x (ArrayLike): Input array.\n\n    Returns:\n        JAXArray: The mean value.\n    \"\"\"\n    x = jnp.asarray(x)\n    return jnp.mean(x)\n</code></pre>"},{"location":"reference/api/backend/#pyForwardFolding.backend.JAXBackend.max","title":"max","text":"<pre><code>max(x: ArrayLike) -&gt; JAXArray\n</code></pre> <p>Compute the maximum value of an array.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>ArrayLike</code> <p>Input array.</p> required <p>Returns:</p> Name Type Description <code>JAXArray</code> <code>Array</code> <p>The maximum value.</p> Source code in <code>pyForwardFolding/backend.py</code> <pre><code>def max(self, x: ArrayLike) -&gt; JAXArray:\n    \"\"\"\n    Compute the maximum value of an array.\n\n    Args:\n        x (ArrayLike): Input array.\n\n    Returns:\n        JAXArray: The maximum value.\n    \"\"\"\n    x = jnp.asarray(x)\n    return jnp.max(x)\n</code></pre>"},{"location":"reference/api/binned_expectation/","title":"<code>pyForwardFolding.binned_expectation</code>","text":"<p>A <code>BinnedExpectation</code> pairs one or more <code>(dataset_key, Model)</code> tuples with a binning, a lifetime, and (optionally) a list of binned factors.</p>"},{"location":"reference/api/binned_expectation/#pyForwardFolding.binned_expectation","title":"pyForwardFolding.binned_expectation","text":""},{"location":"reference/api/binned_expectation/#pyForwardFolding.binned_expectation.BinnedExpectation","title":"BinnedExpectation","text":"<pre><code>BinnedExpectation(\n    name: str,\n    dskey_model_pairs: List[Tuple[str, Model]],\n    binning: AbstractBinning,\n    binned_factors: Optional[\n        List[AbstractBinnedFactor]\n    ] = None,\n    lifetime: float = 1.0,\n)\n</code></pre> <p>Represents an expectation calculated over binned data.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the BinnedExpectation.</p> required <code>dskey_model_pairs</code> <code>List[Tuple[str, Model]]</code> <p>List of tuples where each tuple contains a dataset key and a model.</p> required <code>binning</code> <code>AbstractBinning</code> <p>The binning used to create the histogram.</p> required <code>binned_factors</code> <code>Optional[List[AbstractBinnedFactor]]</code> <p>Factors to be added to the histogram.</p> <code>None</code> <code>lifetime</code> <code>float</code> <p>Lifetime of the binned expectation.</p> <code>1.0</code> Source code in <code>pyForwardFolding/binned_expectation.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    dskey_model_pairs: List[Tuple[str, Model]],\n    binning: AbstractBinning,\n    binned_factors: Optional[List[AbstractBinnedFactor]] = None,\n    lifetime: float = 1.0,\n):\n    self.name = name\n    self.dskey_model_pairs = dskey_model_pairs\n    self.models = [model for _, model in dskey_model_pairs]\n    self.binning = binning\n    self.binned_factors = binned_factors if binned_factors is not None else []\n    self.lifetime = lifetime\n</code></pre>"},{"location":"reference/api/binned_expectation/#pyForwardFolding.binned_expectation.BinnedExpectation.required_variables","title":"required_variables  <code>property</code>","text":"<pre><code>required_variables: Set[str]\n</code></pre> <p>Get all variables required by the BinnedExpectation.</p> <p>Returns:</p>"},{"location":"reference/api/binned_expectation/#pyForwardFolding.binned_expectation.BinnedExpectation.exposed_parameters","title":"exposed_parameters  <code>property</code>","text":"<pre><code>exposed_parameters: Set[str]\n</code></pre> <p>Get parameters exposed by the BinnedExpectation.</p> <p>Returns:</p> Type Description <code>Set[str]</code> <p>Set[str]: A set of all exposed parameters from models and binned factors.</p>"},{"location":"reference/api/binned_expectation/#pyForwardFolding.binned_expectation.BinnedExpectation.evaluate","title":"evaluate","text":"<pre><code>evaluate(\n    datasets: Dict[str, Dict[str, Union[Array, float]]],\n    parameter_values: Dict[str, float],\n) -&gt; Tuple[Array, Array]\n</code></pre> <p>Evaluate a binned expectation by creating a weighted histogram.</p> <p>Parameters:</p> Name Type Description Default <code>datasets</code> <code>Dict[str, Dict[str, Union[Array, float]]]</code> <p>A dictionary where keys are dataset names and values are dictionaries of input variables.</p> required <code>parameter_values</code> <code>Dict[str, float]</code> <p>A dictionary of parameter values, where keys are parameter names and values are floats.</p> required <p>Returns:</p> Type Description <code>Tuple[Array, Array]</code> <p>Tuple[Array, Array]: A tuple containing: - The histogram weights (Array). - The squared weights (Array) representing the binned expectation.</p> Source code in <code>pyForwardFolding/binned_expectation.py</code> <pre><code>def evaluate(\n    self,\n    datasets: Dict[str, Dict[str, Union[Array, float]]],\n    parameter_values: Dict[str, float],\n) -&gt; Tuple[Array, Array]:\n    \"\"\"\n    Evaluate a binned expectation by creating a weighted histogram.\n\n    Args:\n        datasets (Dict[str, Dict[str, Union[Array, float]]]): A dictionary where keys are dataset names and values are dictionaries of input variables.\n        parameter_values (Dict[str, float]): A dictionary of parameter values, where keys are parameter names and values are floats.\n\n    Returns:\n        Tuple[Array, Array]: A tuple containing:\n            - The histogram weights (Array).\n            - The squared weights (Array) representing the binned expectation.\n    \"\"\"\n\n    hist = backend.zeros(self.binning.hist_dims)\n    hist_ssq = backend.zeros(self.binning.hist_dims)\n\n    for model_dskey, model in self.dskey_model_pairs:\n        if model_dskey not in datasets:\n            raise ValueError(\n                f\"Dataset '{model_dskey}' not found in provided datasets.\"\n            )\n        input_variables = datasets[model_dskey]\n\n        # Evaluate the model to get weights\n        weights = model.evaluate(\n            input_variables,\n            parameter_values,\n        )\n        weight_sq = backend.power(weights, 2)\n        # Extract binning variables\n        binning_variables = tuple(\n            backend.asarray(input_variables[var])\n            for var in self.binning.required_variables\n        )\n\n        # Build histograms\n        hist += (\n            self.binning.build_histogram(model_dskey, weights, binning_variables)\n            * self.lifetime\n        )\n        hist_ssq += (\n            self.binning.build_histogram(model_dskey, weight_sq, binning_variables)\n            * self.lifetime**2\n        )\n\n        # Add contributions from binned factors\n        for factor in self.binned_factors:\n            # TODO: Update logic. Do we want to store the bin edges in the factor?\n\n            # if isinstance(self.binning, RectangularBinning):\n            #     if factor.bin_edges is None:\n            #         raise ValueError(\"Binned factors must have bin_edges defined.\")\n            #     else:\n            #         for j, bin_edge in enumerate(self.binning.bin_edges):\n            #             print(\"Set binning\", bin_edge)\n            #             print(\"Loaded Binning\", factor.bin_edges[self.det_config][j])\n            #             np.testing.assert_array_almost_equal(\n            #                 bin_edge,\n            #                 factor.bin_edges[self.det_config][j],\n            #                 err_msg=f\"Binned factor {factor.name} has different bin edges than the binning.\"\n            #             )\n            hist_add, hist_ssq_add = factor.evaluate(\n                input_variables,\n                parameter_values,\n            )\n            hist += hist_add * self.lifetime\n            if hist_ssq_add is not None:\n                hist_ssq += hist_ssq_add * self.lifetime**2\n\n    # Clamp values to avoid negative or infinite values\n    hist = backend.clip(hist, 0, float(\"inf\"))\n    hist_ssq = backend.clip(hist_ssq, 0, float(\"inf\"))\n\n    return hist, hist_ssq\n</code></pre>"},{"location":"reference/api/binned_expectation/#pyForwardFolding.binned_expectation.BinnedExpectation.repr_markdown","title":"repr_markdown","text":"<pre><code>repr_markdown(\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_summary: bool = True,\n    show_model_details: bool = True,\n    show_binning_details: bool = True,\n) -&gt; str\n</code></pre> <p>Configurable markdown representation of the BinnedExpectation object.</p> <p>Parameters:</p> Name Type Description Default <code>indent_level</code> <code>int</code> <p>The level of indentation (each level adds 2 spaces). Default is 0.</p> <code>0</code> <code>bullet_style</code> <code>str</code> <p>Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".</p> <code>'-'</code> <code>include_summary</code> <code>bool</code> <p>Whether to include summary information. Default is True.</p> <code>True</code> <code>show_model_details</code> <code>bool</code> <p>Whether to show detailed model information. Default is True.</p> <code>True</code> <code>show_binning_details</code> <code>bool</code> <p>Whether to show detailed binning information. Default is True.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A configurable markdown representation of the binned expectation.</p> Source code in <code>pyForwardFolding/binned_expectation.py</code> <pre><code>def repr_markdown(\n    self,\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_summary: bool = True,\n    show_model_details: bool = True,\n    show_binning_details: bool = True,\n) -&gt; str:\n    \"\"\"\n    Configurable markdown representation of the BinnedExpectation object.\n\n    Args:\n        indent_level (int): The level of indentation (each level adds 2 spaces). Default is 0.\n        bullet_style (str): Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".\n        include_summary (bool): Whether to include summary information. Default is True.\n        show_model_details (bool): Whether to show detailed model information. Default is True.\n        show_binning_details (bool): Whether to show detailed binning information. Default is True.\n\n    Returns:\n        str: A configurable markdown representation of the binned expectation.\n    \"\"\"\n    indent = \"  \" * indent_level\n    sub_indent = \"  \" * (indent_level + 1)\n\n    lines = []\n\n    # Header with appropriate level based on indent\n    if indent_level == 0:\n        lines.append(f\"## Expectation: `{self.name}`\")\n    else:\n        lines.append(f\"{indent}{bullet_style} **Expectation:** `{self.name}`\")\n\n    # Dataset keys\n    dataset_keys = [dskey for dskey, _ in self.dskey_model_pairs]\n    if indent_level == 0:\n        lines.append(f\"**Datasets:** `{dataset_keys}`\")\n    else:\n        lines.append(f\"{sub_indent}{bullet_style} Datasets: `{dataset_keys}`\")\n\n    lines.append(\"\")\n\n    # Models section\n    if indent_level == 0:\n        lines.append(f\"**Models ({len(self.models)}):**\")\n    else:\n        lines.append(f\"{sub_indent}{bullet_style} Models ({len(self.models)}):\")\n\n    if show_model_details:\n        # Create a mapping from model to datasets\n        model_to_datasets = {}\n        for dskey, model in self.dskey_model_pairs:\n            if model.name not in model_to_datasets:\n                model_to_datasets[model.name] = []\n            model_to_datasets[model.name].append(dskey)\n\n        for i, model in enumerate(self.models, 1):\n            datasets_used = model_to_datasets.get(model.name, [])\n            datasets_str = ', '.join(f\"`{ds}`\" for ds in datasets_used)\n\n            model_indent = \"\" if indent_level == 0 else sub_indent\n            lines.append(f\"{model_indent}{i}. **{model.name}** ({len(model.components)} components) - Uses datasets: {datasets_str}\")\n\n\n            # Use compact model representation\n            model_md = model.repr_markdown(\n                indent_level=indent_level + 1 if indent_level == 0 else indent_level + 2,\n                bullet_style=bullet_style,\n                include_summary=False,  # Skip model summary to avoid redundancy\n                show_component_details=False,  # Keep it compact\n            )\n            lines.append(model_md)           \n    else:\n        # Just list model names\n        for model in self.models:\n            model_indent = sub_indent if indent_level == 0 else \"  \" * (indent_level + 2)\n            lines.append(f\"{model_indent}{bullet_style} {model.name}\")\n\n    lines.append(\"\")\n\n    # Binning information\n    if show_binning_details:\n        if indent_level == 0:\n            lines.append(\"**Binning:**\")\n        else:\n            lines.append(f\"{sub_indent}{bullet_style} Binning:\")\n\n        binning_indent = sub_indent if indent_level == 0 else \"  \" * (indent_level + 2)\n        lines.append(f\"{binning_indent}{bullet_style} Type: `{type(self.binning).__name__}`\")\n        lines.append(f\"{binning_indent}{bullet_style} Variables: `{self.binning.required_variables}`\")\n        lines.append(f\"{binning_indent}{bullet_style} Dimensions: `{self.binning.hist_dims}` ({self.binning.nbins} bins)\")\n        lines.append(\"\")\n\n    # Binned factors if any\n    if self.binned_factors:\n        factor_names = [f.name for f in self.binned_factors]\n        if indent_level == 0:\n            lines.append(f\"**Binned Factors:** {', '.join(factor_names)}\")\n        else:\n            lines.append(f\"{sub_indent}{bullet_style} Binned Factors: {', '.join(factor_names)}\")\n        lines.append(\"\")\n\n    # Summary information\n    if include_summary:\n        if indent_level == 0:\n            lines.append(\"**Configuration Summary:**\")\n            lines.append(\"| Aspect | Count/Details |\")\n            lines.append(\"|--------|---------------|\")\n            lines.append(f\"| Required variables | {len(self.required_variables)} |\")\n            lines.append(f\"| Exposed parameters | {len(self.exposed_parameters)} |\")\n            lines.append(f\"| Lifetime | `{self.lifetime}` |\")\n        else:\n            lines.append(f\"{sub_indent}{bullet_style} Required variables: {len(self.required_variables)}\")\n            lines.append(f\"{sub_indent}{bullet_style} Exposed parameters: {len(self.exposed_parameters)}\")\n            lines.append(f\"{sub_indent}{bullet_style} Lifetime: `{self.lifetime}`\")\n\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/binning/","title":"<code>pyForwardFolding.binning</code>","text":"<p>Binning strategies (<code>AbstractBinning</code>, <code>RectangularBinning</code>, <code>RectangularBinning2DTo3D</code>, <code>RelaxedBinning</code>).</p>"},{"location":"reference/api/binning/#pyForwardFolding.binning","title":"pyForwardFolding.binning","text":""},{"location":"reference/api/binning/#pyForwardFolding.binning.AbstractBinning","title":"AbstractBinning","text":"<pre><code>AbstractBinning(\n    bin_indices_dict: Optional[Dict[str, List]] = None,\n    mask_dict: Optional[Dict[str, Array]] = None,\n)\n</code></pre> <p>Abstract base class for binning strategies.</p> Source code in <code>pyForwardFolding/binning.py</code> <pre><code>def __init__(\n    self,\n    bin_indices_dict: Optional[Dict[str, List]] = None,\n    mask_dict: Optional[Dict[str, Array]] = None,\n):\n    if bin_indices_dict is None:\n        bin_indices_dict = {}\n    if mask_dict is None:\n        mask_dict = {}\n    self.bin_indices_dict = bin_indices_dict\n    self.mask_dict = mask_dict\n    self._hist_dims: Tuple[int, ...] = ()\n</code></pre>"},{"location":"reference/api/binning/#pyForwardFolding.binning.AbstractBinning.construct_from","title":"construct_from  <code>classmethod</code>","text":"<pre><code>construct_from(config: Dict[str, Any]) -&gt; AbstractBinning\n</code></pre> <p>Construct a binning object from a configuration dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Dict[str, Any]</code> <p>Configuration dictionary.</p> required <p>Returns:</p> Name Type Description <code>AbstractBinning</code> <code>AbstractBinning</code> <p>An instance of a subclass of AbstractBinning.</p> Source code in <code>pyForwardFolding/binning.py</code> <pre><code>@classmethod\ndef construct_from(\n    cls: Type[\"AbstractBinning\"], config: Dict[str, Any]\n) -&gt; \"AbstractBinning\":\n    \"\"\"\n    Construct a binning object from a configuration dictionary.\n\n    Args:\n        config (Dict[str, Any]): Configuration dictionary.\n\n    Returns:\n        AbstractBinning: An instance of a subclass of AbstractBinning.\n    \"\"\"\n    binning_type = config.get(\"type\")\n    if binning_type == \"RelaxedBinning\":\n        return RelaxedBinning.construct_from(config)\n    elif binning_type == \"RectangularBinning\":\n        return RectangularBinning.construct_from(config)\n    else:\n        raise ValueError(f\"Unknown binning type: {binning_type}\")\n</code></pre>"},{"location":"reference/api/binning/#pyForwardFolding.binning.AbstractBinning.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>String representation of the AbstractBinning object.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>A string representation of the binning.</p> Source code in <code>pyForwardFolding/binning.py</code> <pre><code>def __repr__(self):\n    \"\"\"\n    String representation of the AbstractBinning object.\n\n    Returns:\n        str: A string representation of the binning.\n    \"\"\"\n    binning_type = type(self).__name__\n    lines = []\n    lines.append(f\"{binning_type}\")\n    if hasattr(self, 'required_variables') and self.required_variables:\n        lines.append(f\"  Variables: {self.required_variables}\")\n    lines.append(f\"  Histogram dimensions: {self.hist_dims}\")\n    lines.append(f\"  Number of bins: {self.nbins}\")\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/binning/#pyForwardFolding.binning.RelaxedBinning","title":"RelaxedBinning","text":"<pre><code>RelaxedBinning(\n    bin_variable: str,\n    bin_edges: Array,\n    slope: float,\n    mask: Any = None,\n)\n</code></pre> <p>               Bases: <code>AbstractBinning</code></p> <p>Relaxed binning strategy using a tanh kernel.</p> <p>Parameters:</p> Name Type Description Default <code>bin_variable</code> <code>str</code> <p>The variable used for binning.</p> required <code>bin_edges</code> <code>List[float]</code> <p>The edges of the bins.</p> required <code>slope</code> <code>float</code> <p>The slope parameter for the tanh kernel.</p> required Source code in <code>pyForwardFolding/binning.py</code> <pre><code>def __init__(\n    self, bin_variable: str, bin_edges: Array, slope: float, mask: Any = None\n):\n    super().__init__(None, mask)\n    self.bin_variable = bin_variable\n    self.bin_edges = (backend.array(bin_edges),)\n    self.slope = slope\n\n    bin_width = backend.diff(self.bin_edges[0])\n    if not backend.allclose(bin_width, bin_width[0]):\n        raise ValueError(\"Bin widths must be uniform\")\n    # Extract scalar value from array for type safety\n    self.bin_width: float = float(bin_width[0])\n    self._hist_dims = tuple(len(edges) - 1 for edges in self.bin_edges)\n    raise NotImplementedError(\"RelaxedBinning is currently not implemented\")\n</code></pre>"},{"location":"reference/api/binning/#pyForwardFolding.binning.RectangularBinning","title":"RectangularBinning","text":"<pre><code>RectangularBinning(\n    bin_variables: Tuple[str, ...],\n    bin_edges: Tuple[Array, ...],\n    bin_indices_dict: Optional[\n        Dict[str, List[Tuple[int]]]\n    ] = None,\n    mask_dict: Optional[Dict[str, Any]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractBinning</code></p> <p>Rectangular binning strategy for multi-dimensional data.</p> <p>Parameters:</p> Name Type Description Default <code>bin_variables</code> <code>Tuple[str]</code> <p>The variables used for binning.</p> required <code>bin_edges</code> <code>List[Array]</code> <p>The edges of the bins for each variable.</p> required <code>bin_indices</code> <code>List[Tuple[int]]</code> <p>Precomputed bin indices.</p> required <code>mask_dict</code> <code>Dict[str, Any]</code> <p>Masks for the binning variables.</p> <code>None</code> Source code in <code>pyForwardFolding/binning.py</code> <pre><code>def __init__(\n    self,\n    bin_variables: Tuple[str, ...],\n    bin_edges: Tuple[Array, ...],\n    bin_indices_dict: Optional[Dict[str, List[Tuple[int]]]] = None,\n    mask_dict: Optional[Dict[str, Any]] = None,\n):\n    super().__init__(bin_indices_dict, mask_dict)\n    self.bin_variables = bin_variables\n    self.bin_edges = tuple(backend.array(edges) for edges in bin_edges)\n    self._hist_dims = tuple(len(edges) - 1 for edges in self.bin_edges)\n</code></pre>"},{"location":"reference/api/binning/#pyForwardFolding.binning.RectangularBinning.clear_bin_indices","title":"clear_bin_indices","text":"<pre><code>clear_bin_indices(ds_key: Optional[str] = None) -&gt; None\n</code></pre> <p>Clear the bin indices and mask for a specific dataset key, or all if ds_key is None.</p> <p>Parameters:</p> Name Type Description Default <code>ds_key</code> <code>str</code> <p>The key for the dataset. If None, clears all bin indices and masks.</p> <code>None</code> Source code in <code>pyForwardFolding/binning.py</code> <pre><code>def clear_bin_indices(self, ds_key: Optional[str] = None) -&gt; None:\n    \"\"\"\n    Clear the bin indices and mask for a specific dataset key, or all if ds_key is None.\n\n    Args:\n        ds_key (str, optional): The key for the dataset. If None, clears all bin indices and masks.\n    \"\"\"\n    if ds_key is None:\n        self.bin_indices_dict.clear()\n        self.mask_dict.clear()\n    else:\n        if ds_key in self.bin_indices_dict:\n            del self.bin_indices_dict[ds_key]\n        if ds_key in self.mask_dict:\n            del self.mask_dict[ds_key]\n</code></pre>"},{"location":"reference/api/binning/#pyForwardFolding.binning.RectangularBinning2DTo3D","title":"RectangularBinning2DTo3D","text":"<pre><code>RectangularBinning2DTo3D(\n    bin_variables: Tuple[str, ...],\n    bin_edges: Tuple[Array, ...],\n    bin_edges_3d: Array,\n    bin_indices_dict: Optional[\n        Dict[str, List[Tuple[int]]]\n    ] = None,\n    mask_dict: Optional[Dict[str, Any]] = None,\n)\n</code></pre> <p>               Bases: <code>RectangularBinning</code></p> <p>Rectangular binning strategy for converting 2D data to 3D histograms.</p> <p>Parameters:</p> Name Type Description Default <code>bin_variables</code> <code>Tuple[str]</code> <p>The variables used for the 2D histogram.</p> required <code>bin_edges</code> <code>List[Array]</code> <p>The edges of the bins for each variable.</p> required <code>bin_edges_3d</code> <code>int</code> <p>Bin edges for the third dimension.</p> required <code>bin_indices_dict</code> <code>Optional[Dict[str, List[Tuple[int]]]]</code> <p>Precomputed bin indices for the 2D histogram</p> <code>None</code> <code>mask_dict</code> <code>Dict[str, Any]</code> <p>Masks for the binning variables.</p> <code>None</code> Source code in <code>pyForwardFolding/binning.py</code> <pre><code>def __init__(\n    self,\n    bin_variables: Tuple[str, ...],\n    bin_edges: Tuple[Array, ...],\n    bin_edges_3d: Array,\n    bin_indices_dict: Optional[Dict[str, List[Tuple[int]]]] = None,\n    mask_dict: Optional[Dict[str, Any]] = None,\n):\n    super().__init__(bin_variables, bin_edges, bin_indices_dict, mask_dict)\n    if len(self.hist_dims) != 2:\n        raise ValueError(\"RectangularBinning2DTo3D requires exactly two dimensions\")\n\n    self.bin_edges_3d = bin_edges_3d\n</code></pre>"},{"location":"reference/api/binning/#pyForwardFolding.binning.RectangularBinning2DTo3D.build_histogram","title":"build_histogram","text":"<pre><code>build_histogram(\n    ds_key: str,\n    weights: Array,\n    binning_variables: Tuple[Array, ...],\n) -&gt; Array\n</code></pre> <p>Build a 3D histogram from 2D data by adding a third dimension of size 1.</p> <p>Parameters:</p> Name Type Description Default <code>ds_key</code> <code>str</code> <p>The dataset key.</p> required <code>weights</code> <code>Array</code> <p>Weights for the histogram.</p> required <code>binning_variables</code> <code>Tuple[Array, ...]</code> <p>The variables used for binning.</p> required <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>A 3D histogram with the third dimension of size 1.</p> Source code in <code>pyForwardFolding/binning.py</code> <pre><code>def build_histogram(\n    self,\n    ds_key: str,\n    weights: Array,\n    binning_variables: Tuple[Array, ...],\n) -&gt; Array:\n    \"\"\"\n    Build a 3D histogram from 2D data by adding a third dimension of size 1.\n\n    Args:\n        ds_key (str): The dataset key.\n        weights (Array): Weights for the histogram.\n        binning_variables (Tuple[Array, ...]): The variables used for binning.\n\n    Returns:\n        Array: A 3D histogram with the third dimension of size 1.\n    \"\"\"\n    histogram_2d = super().build_histogram(ds_key, weights, binning_variables)\n\n    histogram_2d /= (\n        self.bin_edges_3d.shape[0] - 1\n    )  # Normalize by the number of bins in the third dimension\n\n    histogram_3d = backend.repeat(\n        histogram_2d[..., None],\n        repeats=self.bin_edges_3d.shape[0] - 1,\n        axis=-1,\n        total_repeat_length=self.bin_edges_3d.shape[0] - 1,\n    )\n\n    return histogram_3d\n</code></pre>"},{"location":"reference/api/config/","title":"<code>pyForwardFolding.config</code>","text":"<p>YAML loader entry points: <code>analysis_from_config</code>, <code>params_from_config</code>, <code>dataset_from_config</code>, <code>models_from_config</code>, and <code>load_dataframe</code>.</p> <p>See Configuration overview for the YAML schema.</p>"},{"location":"reference/api/config/#pyForwardFolding.config","title":"pyForwardFolding.config","text":""},{"location":"reference/api/config/#pyForwardFolding.config.analysis_from_config","title":"analysis_from_config","text":"<pre><code>analysis_from_config(config: Union[str, dict]) -&gt; Analysis\n</code></pre> <p>Load an analysis configuration from a YAML file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to the YAML configuration file.</p> required <p>Returns:</p> Name Type Description <code>Analysis</code> <code>Analysis</code> <p>The constructed analysis object.</p> Source code in <code>pyForwardFolding/config.py</code> <pre><code>def analysis_from_config(config: Union[str, dict]) -&gt; Analysis:\n    \"\"\"\n    Load an analysis configuration from a YAML file.\n\n    Args:\n        path (str): Path to the YAML configuration file.\n\n    Returns:\n        Analysis: The constructed analysis object.\n    \"\"\"\n    conf = _load_config(config)\n    factors = _build_factors(conf)\n    components = _build_components(conf, factors)\n    models = _build_models(conf, components)\n\n    binned_expectations = {}\n\n    for hist_config in conf[\"histograms\"]:\n        binning = AbstractBinning.construct_from(hist_config[\"binning\"])\n        lifetime = hist_config.get(\"lifetime\", 1.0)\n        hist_factors = [\n            AbstractBinnedFactor.construct_from(f, binning)\n            for f in hist_config.get(\"hist_factors\", [])\n        ]\n\n        dskey_model_name_pairs = hist_config[\"models\"]\n        dskey_model_pairs = [\n            (dskey, models[model_name]) for model_name, dskey in dskey_model_name_pairs\n        ]\n\n        binned_expectations[hist_config[\"name\"]] = BinnedExpectation(\n            name=hist_config[\"name\"],\n            dskey_model_pairs=dskey_model_pairs,\n            binning=binning,\n            binned_factors=hist_factors,\n            lifetime=lifetime,\n        )\n\n    return Analysis(binned_expectations)\n</code></pre>"},{"location":"reference/api/config/#pyForwardFolding.config.models_from_config","title":"models_from_config","text":"<pre><code>models_from_config(\n    config: Union[str, dict],\n) -&gt; Dict[str, Dict[str, Model]]\n</code></pre> <p>Load models per dataset from a YAML file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to the YAML configuration file.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>Dict[str, Dict[str, Model]]</code> <p>model for each dataset.</p> Source code in <code>pyForwardFolding/config.py</code> <pre><code>def models_from_config(config: Union[str, dict]) -&gt; Dict[str, Dict[str, Model]]:\n    \"\"\"\n    Load models per dataset from a YAML file.\n\n    Args:\n        path (str): Path to the YAML configuration file.\n\n    Returns:\n        dict: model for each dataset.\n    \"\"\"\n    conf = _load_config(config)\n    factors = _build_factors(conf)\n    components = _build_components(conf, factors)\n    models = _build_models(conf, components)\n\n    output: Dict[str, Dict[str, Model]] = {}\n    for hist in conf[\"histograms\"]:\n        output[hist[\"name\"]] = {}\n        for model in hist[\"models\"]:\n            output[hist[\"name\"]][model[0]] = models[model[0]]\n\n    return output\n</code></pre>"},{"location":"reference/api/config/#pyForwardFolding.config.load_dataframe","title":"load_dataframe","text":"<pre><code>load_dataframe(path: str) -&gt; pd.DataFrame\n</code></pre> <p>Loads a Pandas DataFrame from a given file path. Automatically detects the file format based on its extension.</p> <p>Supported formats: CSV, Parquet, Feather, HDF5, Excel, Pickle.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to the data file.</p> required <p>Returns:</p> Type Description <code>DataFrame</code> <p>pd.DataFrame: Loaded DataFrame.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If the file does not exist.</p> <code>ValueError</code> <p>If the file extension is unsupported.</p> Source code in <code>pyForwardFolding/config.py</code> <pre><code>def load_dataframe(path: str) -&gt; pd.DataFrame:\n    \"\"\"\n    Loads a Pandas DataFrame from a given file path.\n    Automatically detects the file format based on its extension.\n\n    Supported formats: CSV, Parquet, Feather, HDF5, Excel, Pickle.\n\n    Args:\n        path (str): Path to the data file.\n\n    Returns:\n        pd.DataFrame: Loaded DataFrame.\n\n    Raises:\n        FileNotFoundError: If the file does not exist.\n        ValueError: If the file extension is unsupported.\n    \"\"\"\n    import pandas as pd\n\n    if not os.path.exists(path):\n        raise FileNotFoundError(f\"Data file not found: {path}\")\n\n    ext = os.path.splitext(path)[-1].lower()\n\n    if ext in [\".csv\", \".txt\"]:\n        return pd.read_csv(path)\n    elif ext in [\".parquet\"]:\n        return pd.read_parquet(path)\n    elif ext in [\".feather\", \".ft\"]:\n        return pd.read_feather(path)\n    elif ext in [\".h5\", \".hdf\", \".hdf5\"]:\n        return pd.read_hdf(path)\n    elif ext in [\".xlsx\", \".xls\"]:\n        return pd.read_excel(path)\n    elif ext in [\".pkl\", \".pickle\"]:\n        return pd.read_pickle(path)\n    else:\n        raise ValueError(\n            f\"Unsupported file extension '{ext}' for file: {path}. \"\n            \"Supported formats: CSV, Parquet, Feather, HDF5, Excel, Pickle.\"\n        )\n</code></pre>"},{"location":"reference/api/config/#pyForwardFolding.config.dataset_from_config","title":"dataset_from_config","text":"<pre><code>dataset_from_config(\n    config: Union[str, dict],\n) -&gt; Dict[str, Dict[str, float]]\n</code></pre> <p>Creates a dataset from a yaml config.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to the YAML configuration file.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>Dict[str, Dict[str, float]]</code> <p>dataset to be used as analysis input.</p> Source code in <code>pyForwardFolding/config.py</code> <pre><code>def dataset_from_config(config: Union[str, dict]) -&gt; Dict[str, Dict[str, float]]:\n    \"\"\"\n    Creates a dataset from a yaml config.\n\n    Args:\n        path (str): Path to the YAML configuration file.\n\n    Returns:\n        dict: dataset to be used as analysis input.\n    \"\"\"\n\n    conf = _load_config(config)\n    dataset = {}\n\n    for subconf in conf[\"datasets\"]:\n        df = load_dataframe(subconf[\"path\"])\n        subdataset = {}\n        for out_key, entry in subconf[\"param_mapping\"].items():\n            in_key = entry[\"df_key\"]\n            vals = df[in_key]\n            trafo = entry.get(\"transform\")\n            if trafo:\n                vals = getattr(backend, trafo)(vals)\n            subdataset[out_key] = backend.asarray(vals)\n\n        if \"median_energy\" in subconf:\n            energies = subconf[\"median_energy\"][\"energy_key\"]\n            weights = subconf[\"median_energy\"][\"weight\"]\n            median_energy = backend.weighted_median(df[energies], df[weights])\n            subdataset[\"median_energy\"] = backend.asarray([median_energy])\n        dataset[subconf[\"name\"]] = subdataset\n\n    return dataset\n</code></pre>"},{"location":"reference/api/config/#pyForwardFolding.config.params_from_config","title":"params_from_config","text":"<pre><code>params_from_config(\n    config: Union[str, dict],\n) -&gt; Tuple[Dict[str, float], list]\n</code></pre> <p>Creates a dict for model_parameters and priors from a config.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to the YAML configuration file.</p> required <p>Returns:</p> Name Type Description <code>model_parameters</code> <code>Dict[str, float]</code> <p>dict containing model parameters extracted from the config</p> <code>priors</code> <code>list</code> <p>list of prior objects extracted from the config</p> Source code in <code>pyForwardFolding/config.py</code> <pre><code>def params_from_config(config: Union[str, dict]) -&gt; Tuple[Dict[str, float],list]:\n    \"\"\"\n    Creates a dict for model_parameters and priors from a config.\n\n    Args:\n        path (str): Path to the YAML configuration file.\n\n    Returns:\n        model_parameters: dict containing model parameters extracted from the config\n        priors: list of prior objects extracted from the config\n    \"\"\"\n    from .likelihood import UniformPrior, GaussianUnivariatePrior\n\n    conf = _load_config(config)\n\n    prior_bounds = {\n        k: (float(v[0]), float(v[1]))\n        for k, v in conf[\"prior_bounds\"].items()\n    }\n    prior_seeds = conf[\"prior_seeds\"]\n\n    if \"prior_bounds_gauss\" in conf and conf[\"prior_bounds_gauss\"]:\n        prior_bounds_gauss = {\n            k: (float(v[0]), float(v[1]))\n            for k, v in conf[\"prior_bounds_gauss\"].items()\n        }\n        prior_params_gauss = {\n            k: (float(v[0]), float(v[1]))\n            for k, v in conf[\"prior_params_gauss\"].items()\n        }\n        prior_seeds_gauss = conf[\"prior_seeds_gauss\"]\n        priors= [\n            UniformPrior(prior_seeds, prior_bounds),\n            GaussianUnivariatePrior(prior_params_gauss, prior_seeds_gauss, prior_bounds_gauss)\n        ]\n    else:\n        priors = [\n            UniformPrior(prior_seeds, prior_bounds)\n        ]\n\n    model_parameters = conf[\"model_parameters\"]\n\n    return model_parameters, priors\n</code></pre>"},{"location":"reference/api/factor/","title":"<code>pyForwardFolding.factor</code>","text":"<p>All per-event (<code>AbstractUnbinnedFactor</code>) and per-bin (<code>AbstractBinnedFactor</code>) factor classes, plus the <code>FACTORSTR_CLASS_MAPPING</code> registry used by YAML config dispatch.</p> <p>See the Factor reference for a per-factor summary of parameters and required variables.</p>"},{"location":"reference/api/factor/#pyForwardFolding.factor","title":"pyForwardFolding.factor","text":""},{"location":"reference/api/factor/#pyForwardFolding.factor.AbstractFactor","title":"AbstractFactor","text":"<pre><code>AbstractFactor(\n    name: str,\n    param_mapping: Optional[\n        Dict[str, Union[str, float]]\n    ] = None,\n)\n</code></pre> <p>Abstract class representing a per-event factor.</p> <p>Initialize the factor with a name and parameter mapping.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>param_mapping</code> <code>dict</code> <p>Dictionary keyed by internal factor parameter name. Each value can be either:</p> <ul> <li>a <code>str</code>: rename \u2014 the parameter is exposed under this   name in the global parameter dictionary.</li> <li>a numeric (<code>int</code> or <code>float</code>, but not <code>bool</code>): fix \u2014   the parameter is held at this constant value and is not   exposed in the analysis.</li> </ul> <p>Internal parameters not listed in the mapping keep their original name and remain exposed. If <code>None</code> (default), all factor parameters are exposed under their original names.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    param_mapping: Optional[Dict[str, Union[str, float]]] = None,\n):\n    \"\"\"\n    Initialize the factor with a name and parameter mapping.\n\n    Args:\n        name (str): Identifier for the factor.\n        param_mapping (dict, optional): Dictionary keyed by internal\n            factor parameter name. Each value can be either:\n\n            - a ``str``: rename \u2014 the parameter is exposed under this\n              name in the global parameter dictionary.\n            - a numeric (``int`` or ``float``, but not ``bool``): fix \u2014\n              the parameter is held at this constant value and is *not*\n              exposed in the analysis.\n\n            Internal parameters not listed in the mapping keep their\n            original name and remain exposed. If ``None`` (default), all\n            factor parameters are exposed under their original names.\n    \"\"\"\n    self.name = name\n    self.param_mapping = param_mapping\n    self.factor_parameters: List[str] = []\n\n    # Split param_mapping entries into renames (str values) and fixed\n    # constants (numeric values). Bools are explicitly excluded so that\n    # accidental boolean entries are caught rather than silently\n    # interpreted as 0/1 constants.\n    if param_mapping is None:\n        self._renames: Dict[str, str] = {}\n        self.fixed_factor_params: Dict[str, float] = {}\n    else:\n        self._renames = {\n            k: v for k, v in param_mapping.items() if isinstance(v, str)\n        }\n        self.fixed_factor_params = {\n            k: float(v)\n            for k, v in param_mapping.items()\n            if isinstance(v, (int, float)) and not isinstance(v, bool)\n        }\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.AbstractFactor.parameter_mapping","title":"parameter_mapping  <code>property</code>","text":"<pre><code>parameter_mapping: Dict[str, str]\n</code></pre> <p>Mapping of fittable factor parameters (internal name -&gt; exposed name). Parameters that were fixed via <code>param_mapping</code> (numeric value) are excluded; parameters not listed in the mapping default to identity (internal name == exposed name).</p>"},{"location":"reference/api/factor/#pyForwardFolding.factor.AbstractFactor.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>String representation of the AbstractFactor object.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>A string representation of the factor.</p> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __repr__(self):\n    \"\"\"\n    String representation of the AbstractFactor object.\n\n    Returns:\n        str: A string representation of the factor.\n    \"\"\"\n    factor_type = type(self).__name__\n    lines = []\n    lines.append(f\"{factor_type}: {self.name}\")\n    if self.factor_parameters:\n        lines.append(f\"  Parameters: {self.parameter_mapping}\")\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.AbstractFactor.repr_markdown","title":"repr_markdown","text":"<pre><code>repr_markdown(\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_type_in_name: bool = True,\n) -&gt; str\n</code></pre> <p>Configurable markdown representation of the AbstractFactor object as a list.</p> <p>Parameters:</p> Name Type Description Default <code>indent_level</code> <code>int</code> <p>The level of indentation (each level adds 2 spaces). Default is 0.</p> <code>0</code> <code>bullet_style</code> <code>str</code> <p>Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".</p> <code>'-'</code> <code>include_type_in_name</code> <code>bool</code> <p>Whether to include factor type in the name. Default is True.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A configurable markdown representation of the factor as a list.</p> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def repr_markdown(\n    self,\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_type_in_name: bool = True,\n) -&gt; str:\n    \"\"\"\n    Configurable markdown representation of the AbstractFactor object as a list.\n\n    Args:\n        indent_level (int): The level of indentation (each level adds 2 spaces). Default is 0.\n        bullet_style (str): Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".\n        include_type_in_name (bool): Whether to include factor type in the name. Default is True.\n\n    Returns:\n        str: A configurable markdown representation of the factor as a list.\n    \"\"\"\n    factor_type = type(self).__name__\n\n    indent = \"  \" * indent_level\n    sub_indent = \"  \" * (indent_level + 1)\n    sub_sub_indent = \"  \" * (indent_level + 2)\n\n    lines = []\n    if include_type_in_name:\n        lines.append(f\"{indent}{bullet_style} **{factor_type}** (`{self.name}`)\")\n    else:\n        lines.append(f\"{indent}{bullet_style} `{self.name}`\")\n\n    if self.factor_parameters:\n        lines.append(f\"{sub_indent}{bullet_style} Parameters:\")\n        for factor_param, exposed_param in self.parameter_mapping.items():\n            lines.append(f\"{sub_sub_indent}{bullet_style} `{factor_param}` \u2192 `{exposed_param}`\")\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.AbstractUnbinnedFactor","title":"AbstractUnbinnedFactor","text":"<pre><code>AbstractUnbinnedFactor(\n    name: str,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractFactor</code></p> <p>Abstract class for factors that operate on unbinned data.</p> <p>Initialize the unbinned factor with a name and parameter mapping. Args:     name (str): Identifier for the factor.     param_mapping (dict): Dictionary mapping factor parameter names to names in the parameter dictionary.</p> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(self, name: str, param_mapping: Optional[Dict[str, str]] = None):\n    \"\"\"\n    Initialize the unbinned factor with a name and parameter mapping.\n    Args:\n        name (str): Identifier for the factor.\n        param_mapping (dict): Dictionary mapping factor parameter names to names in the parameter dictionary.\n    \"\"\"\n    super().__init__(name, param_mapping)\n    self.req_vars: List[str] = []\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.AbstractUnbinnedFactor.required_variables","title":"required_variables  <code>property</code>","text":"<pre><code>required_variables: List[str]\n</code></pre> <p>Get the required variables for the unbinned factor.</p> <p>Returns:</p> Type Description <code>List[str]</code> <p>List[str]: A list of required variables.</p>"},{"location":"reference/api/factor/#pyForwardFolding.factor.AbstractUnbinnedFactor.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>String representation of the AbstractUnbinnedFactor object.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>A string representation of the unbinned factor.</p> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __repr__(self):\n    \"\"\"\n    String representation of the AbstractUnbinnedFactor object.\n\n    Returns:\n        str: A string representation of the unbinned factor.\n    \"\"\"\n\n    lines = super().__repr__().splitlines()  \n    if self.required_variables:\n        lines.append(f\"  Required variables: {self.required_variables}\") # type: ignore\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.AbstractUnbinnedFactor.repr_markdown","title":"repr_markdown","text":"<pre><code>repr_markdown(\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_type_in_name: bool = True,\n) -&gt; str\n</code></pre> <p>Configurable markdown representation of the AbstractUnbinnedFactor object as a list.</p> <p>Parameters:</p> Name Type Description Default <code>indent_level</code> <code>int</code> <p>The level of indentation (each level adds 2 spaces). Default is 0.</p> <code>0</code> <code>bullet_style</code> <code>str</code> <p>Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".</p> <code>'-'</code> <code>include_type_in_name</code> <code>bool</code> <p>Whether to include factor type in the name. Default is True.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A configurable markdown representation of the unbinned factor as a list.</p> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def repr_markdown(\n    self,\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_type_in_name: bool = True,\n) -&gt; str:\n    \"\"\"\n    Configurable markdown representation of the AbstractUnbinnedFactor object as a list.\n\n    Args:\n        indent_level (int): The level of indentation (each level adds 2 spaces). Default is 0.\n        bullet_style (str): Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".\n        include_type_in_name (bool): Whether to include factor type in the name. Default is True.\n\n    Returns:\n        str: A configurable markdown representation of the unbinned factor as a list.\n    \"\"\"\n\n    sub_indent = \"  \" * (indent_level + 1)\n    sub_sub_indent = \"  \" * (indent_level + 2)\n\n    lines = [AbstractFactor.repr_markdown(self, indent_level, bullet_style, include_type_in_name)]\n    if self.required_variables:\n        lines.append(f\"{sub_indent}{bullet_style} Required variables:\")\n        for var in self.required_variables:\n            lines.append(f\"{sub_sub_indent}{bullet_style} `{var}`\")\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.AbstractBinnedFactor","title":"AbstractBinnedFactor","text":"<pre><code>AbstractBinnedFactor(\n    name: str,\n    binning: AbstractBinning,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractFactor</code></p> <p>Abstract base class for factors that contribute to a binned expectation. This class should be inherited by specific implementations of binned factors.</p> <p>Initialize the AbstractBinnedFactor with a name, binning, and parameter mapping.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>binning</code> <code>AbstractBinning</code> <p>The binning strategy for this factor.</p> required <code>param_mapping</code> <code>Optional[Dict[str, str]]</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    binning: AbstractBinning,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    \"\"\"\n    Initialize the AbstractBinnedFactor with a name, binning, and parameter mapping.\n\n    Args:\n        name (str): Identifier for the factor.\n        binning (AbstractBinning): The binning strategy for this factor.\n        param_mapping (Optional[Dict[str, str]]): Dictionary mapping factor parameter names to names in the parameter dictionary.\n    \"\"\"\n    super().__init__(name, param_mapping)\n    self.binning = binning\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.AbstractBinnedFactor.repr_markdown","title":"repr_markdown","text":"<pre><code>repr_markdown(\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_type_in_name: bool = True,\n) -&gt; str\n</code></pre> <p>Configurable markdown representation of the AbstractBinnedFactor object as a list.</p> <p>Parameters:</p> Name Type Description Default <code>indent_level</code> <code>int</code> <p>The level of indentation (each level adds 2 spaces). Default is 0.</p> <code>0</code> <code>bullet_style</code> <code>str</code> <p>Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".</p> <code>'-'</code> <code>include_type_in_name</code> <code>bool</code> <p>Whether to include factor type in the name. Default is True.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A configurable markdown representation of the binned factor as a list.</p> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def repr_markdown(\n    self,\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_type_in_name: bool = True,\n) -&gt; str:\n    \"\"\"\n    Configurable markdown representation of the AbstractBinnedFactor object as a list.\n\n    Args:\n        indent_level (int): The level of indentation (each level adds 2 spaces). Default is 0.\n        bullet_style (str): Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".\n        include_type_in_name (bool): Whether to include factor type in the name. Default is True.\n\n    Returns:\n        str: A configurable markdown representation of the binned factor as a list.\n    \"\"\"\n\n    indent = \"  \" * indent_level\n    sub_indent = \"  \" * (indent_level + 1)\n\n    lines = [AbstractFactor.repr_markdown(self, indent_level, bullet_style, include_type_in_name)]\n    lines.append(f\"{indent}{bullet_style} Binning: {type(self.binning).__name__}\")\n    lines.append(f\"{sub_indent}{bullet_style} Dimensions: `{self.binning.hist_dims}`\")\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.PowerLawFlux","title":"PowerLawFlux","text":"<pre><code>PowerLawFlux(\n    name: str,\n    pivot_energy: float,\n    baseline_norm: float,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Factor that applies a power law flux model.</p> <p>Parameters required by this factor are: <code>flux_norm</code> and <code>spectral_index</code>. Variables required by this factor are: <code>true_energy</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>pivot_energy</code> <code>float</code> <p>Reference energy for the power law.</p> required <code>baseline_norm</code> <code>float</code> <p>Baseline normalization factor.</p> required <code>param_mapping</code> <code>dict</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    pivot_energy: float,\n    baseline_norm: float,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    super().__init__(name, param_mapping)\n\n    self.pivot_energy = pivot_energy\n    self.baseline_norm = baseline_norm\n\n    self.factor_parameters: List[str] = [\"flux_norm\", \"spectral_index\"]\n    self.req_vars: List[str] = [\"true_energy\"]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.BrokenPowerLawFlux","title":"BrokenPowerLawFlux","text":"<pre><code>BrokenPowerLawFlux(\n    name: str,\n    pivot_energy: float,\n    baseline_norm: float,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Factor that applies a broken power law flux model.</p> <p>Parameters required by this factor are: <code>flux_norm</code>, <code>spectral_index_1</code>, <code>spectral_index_2</code> and <code>logEbreak</code>. Variables required by this factor are: <code>true_energy</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>pivot_energy</code> <code>float</code> <p>Reference energy for the power law.</p> required <code>baseline_norm</code> <code>float</code> <p>Baseline normalization factor.</p> required <code>param_mapping</code> <code>dict</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    pivot_energy: float,\n    baseline_norm: float,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    super().__init__(name, param_mapping)\n\n    self.pivot_energy = pivot_energy\n    self.baseline_norm = baseline_norm\n\n    self.factor_parameters: List[str] = [\"flux_norm\", \"spectral_index_1\", \"spectral_index_2\", \"logEbreak\"]\n    self.req_vars: List[str] = [\"true_energy\"]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.GaisserZenithFactor","title":"GaisserZenithFactor","text":"<pre><code>GaisserZenithFactor(\n    name: str,\n    pivot_energy: float,\n    epsilon_pi: float = 115.0,\n    epsilon_K: float = 850.0,\n    a_pi: float = 1.0,\n    b_K: float = 1.0,\n    earth_curvature: bool = True,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Factor that applies the zenith-dependent piece of the Gaisser atmospheric neutrino flux parametrization, excluding the E^{-gamma} baseline shape.</p> <p>Intended to be multiplied with a separate spectral factor (e.g. a spline or <code>PowerLawFlux</code>) and a <code>FluxNorm</code> factor. This factor only contributes the pi/K bracket</p> <pre><code>B(E, theta) = 1 / (1 + a * E * cos(theta*) / eps_pi)\n            + R_K * 1 / (1 + b * E * cos(theta*) / eps_K)\n</code></pre> <p>normalized so that B(pivot_energy, vertical) = 1. Here <code>cos(theta*)</code> is the Chirkin (2004) Earth-curvature-corrected effective cosine (if <code>earth_curvature=True</code>), or plain <code>cos(theta)</code> otherwise. The formula is applied to the atmospheric zenith angle, which is symmetric in up/down, so the absolute value of <code>cos(true_zenith)</code> is used internally.</p> <p>Physical effect: the bracket is ~constant at low energies and adds an additional power of E that depends on <code>cos(theta*)</code> above the critical energies. Horizontal showers transition later, producing a relatively harder spectrum at the horizon (the \"secant-theta\" enhancement).</p> <p>Parameters required by this factor are: <code>kaon_pion_ratio</code>. Variables required by this factor are: <code>true_energy</code> and <code>true_zenith</code> (the zenith angle in radians).</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>pivot_energy</code> <code>float</code> <p>Reference energy [GeV] at which the bracket is normalized to 1 (at vertical incidence). This factor evaluates to 1 at (pivot_energy, vertical).</p> required <code>epsilon_pi</code> <code>float</code> <p>Pion critical energy [GeV]. Default 115 GeV.</p> <code>115.0</code> <code>epsilon_K</code> <code>float</code> <p>Kaon critical energy [GeV]. Default 850 GeV.</p> <code>850.0</code> <code>a_pi</code> <code>float</code> <p>Pion interaction coefficient inside the pion term. Default 1.0.</p> <code>1.0</code> <code>b_K</code> <code>float</code> <p>Kaon interaction coefficient inside the kaon term. Default 1.0.</p> <code>1.0</code> <code>earth_curvature</code> <code>bool</code> <p>If True, apply the Chirkin (2004) effective cosine to prevent the unphysical sec(theta) divergence at the horizon. Default True.</p> <code>True</code> <code>param_mapping</code> <code>dict</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    pivot_energy: float,\n    epsilon_pi: float = 115.0,\n    epsilon_K: float = 850.0,\n    a_pi: float = 1.0,\n    b_K: float = 1.0,\n    earth_curvature: bool = True,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    super().__init__(name, param_mapping)\n\n    self.pivot_energy = pivot_energy\n    self.epsilon_pi = epsilon_pi\n    self.epsilon_K = epsilon_K\n    self.a_pi = a_pi\n    self.b_K = b_K\n    self.earth_curvature = earth_curvature\n\n    self.factor_parameters: List[str] = [\"kaon_pion_ratio\"]\n    self.req_vars: List[str] = [\"true_energy\", \"true_zenith\"]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.FlavorRatio","title":"FlavorRatio","text":"<pre><code>FlavorRatio(\n    name: str,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Factor that applies a power law flux model and scales each neutrino flavor</p> <p>Parameters required by this factor are: <code>flux_norm</code> and <code>spectral_index</code>. Variables required by this factor are: <code>true_energy</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>pivot_energy</code> <code>float</code> <p>Reference energy for the power law.</p> required <code>baseline_norm</code> <code>float</code> <p>Baseline normalization factor.</p> required <code>param_mapping</code> <code>dict</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    super().__init__(name, param_mapping)\n\n    self.factor_parameters: List[str] = [\"nue_ratio\",\"nutau_ratio\"]\n    self.req_vars: List[str] = [\"true_type\"]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.FluxNorm","title":"FluxNorm","text":"<pre><code>FluxNorm(\n    name: str,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Factor that applies a simple flux normalization.</p> <p>Parameters required by this factor are: <code>flux_norm</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>param_mapping</code> <code>dict</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(self, name: str, param_mapping: Optional[Dict[str, str]] = None):\n    super().__init__(name, param_mapping)\n\n    self.factor_parameters = [\"flux_norm\"]\n    self.req_vars = []\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.SegmentedPlane","title":"SegmentedPlane","text":"<pre><code>SegmentedPlane(\n    name,\n    param_mapping: Optional[Dict[str, str]] = None,\n    reference_energy: float = 1000.0,\n    baseline_flux: float = 1.0,\n    segment_edges: list = [0.0],\n    height: float = 3.14159265359,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Factor that applies a segment-wise delta gamma scaling and a flux norm to a precalculated Galactic Plane weight.</p> <p>Parameters required by this factor are: <code>segmented_norm_{i}</code> and <code>segmented_gamma_{i}</code> where i corresponds to each segment index. Variables required by this factor are: <code>true_energy</code>, <code>true_lat</code> and <code>true_lon</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>reference_energy</code> <code>float</code> <p>Reference energy for scaling.</p> <code>1000.0</code> <code>baseline_flux</code> <code>float</code> <p>Overall scaling of the factor.</p> <code>1.0</code> <code>segment_edges</code> <code>list</code> <p>List of all segment edges.</p> <code>[0.0]</code> <code>height</code> <code>float</code> <p>Height cut around the galactic horizon.</p> <code>3.14159265359</code> <code>param_mapping</code> <code>dict</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name,\n    param_mapping: Optional[Dict[str, str]] = None,\n    reference_energy: float = 1e3,\n    baseline_flux: float = 1.0,\n    segment_edges: list = [0.],\n    height: float = 3.14159265359,\n):\n    super().__init__(name, param_mapping)\n    self.reference_energy = reference_energy\n    self.baseline_flux = baseline_flux\n    self.segment_edges = segment_edges\n    self.num_segments = len(segment_edges)\n    self.height = height\n\n    self.factor_parameters = [f\"galactic_norm_{i}\" for i in range(self.num_segments)]\n    self.factor_parameters += [f\"galactic_gamma_{i}\" for i in range(self.num_segments)]\n    self.req_vars = [\"true_energy\", \"true_lat\", \"true_lon\"]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.GalacticPlaneBox","title":"GalacticPlaneBox","text":"<pre><code>GalacticPlaneBox(\n    name: str,\n    height: float,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Box-shaped analytical galactic-plane selector.</p> <p>Returns 1.0 for events inside the latitude band |true_lat| &lt;= height (in radians) and 0.0 outside. Pure geometry \u2014 no fittable parameters and no longitude dependence (the box is a band in latitude only, spanning all longitudes).</p> <p>Intended to be multiplied with a spectral factor (e.g. <code>PowerLawFlux</code> or a spline) and a <code>FluxNorm</code> to model a galactic-plane flux component. Edges are hard; this introduces a non-differentiability in <code>true_lat</code> but <code>true_lat</code> is per-event input data, not a fit parameter, so gradient-based fits are unaffected.</p> <p>No parameters required by this factor. Variables required by this factor are: <code>true_lat</code> (in radians).</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>height</code> <code>float</code> <p>Latitude half-width of the box in radians. Events with |true_lat| &lt;= height return 1.0, others return 0.0.</p> required <code>param_mapping</code> <code>dict</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    height: float,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    super().__init__(name, param_mapping)\n    self.height = height\n    self.factor_parameters: List[str] = []\n    self.req_vars: List[str] = [\"true_lat\"]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.SnowstormGauss","title":"SnowstormGauss","text":"<pre><code>SnowstormGauss(\n    name,\n    sys_gauss_width,\n    sys_sim_bounds,\n    req_variable_name,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Factor that implements a Gaussian reweighting scheme for systematic uncertainty modeling.</p> <p>Parameters required by this factor are: <code>scale</code>. Variables required by this factor are specified by <code>req_variable_name</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>sys_gauss_width</code> <code>float</code> <p>Width of the Gaussian distribution.</p> required <code>sys_sim_bounds</code> <code>tuple</code> <p>Bounds for the simulated parameter space (min, max).</p> required <code>req_variable_name</code> <code>str</code> <p>Name of the required variable for reweighting.</p> required <code>param_mapping</code> <code>dict</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name,\n    sys_gauss_width,\n    sys_sim_bounds,\n    req_variable_name,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    super().__init__(name, param_mapping)\n\n    self.sys_gauss_width = sys_gauss_width\n    self.sys_sim_bounds = sys_sim_bounds\n    self.req_vars = [req_variable_name]\n    self.factor_parameters = [\"scale\"]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.DeltaGamma","title":"DeltaGamma","text":"<pre><code>DeltaGamma(\n    name,\n    param_mapping: Optional[Dict[str, str]] = None,\n    reference_energy: float = 1.0,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Factor that applies a delta gamma scaling.</p> <p>Parameters required by this factor are: <code>delta_gamma</code>. Variables required by this factor are: <code>true_energy</code> and <code>median_energy</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>reference_energy</code> <code>float</code> <p>Reference energy for scaling.</p> <code>1.0</code> <code>param_mapping</code> <code>dict</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name,\n    param_mapping: Optional[Dict[str, str]] = None,\n    reference_energy: float = 1.0,\n):\n    super().__init__(name, param_mapping)\n    self.reference_energy = reference_energy\n\n    self.factor_parameters = [\"delta_gamma\"]\n    self.req_vars = [\"true_energy\"]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.ModelInterpolator","title":"ModelInterpolator","text":"<pre><code>ModelInterpolator(\n    name: str,\n    baseline_weight: str,\n    alternative_weight: str,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Interpolation between two models.</p> <p>Parameters required by this factor are: <code>lambda_int</code>. Variables required by this factor are: <code>baseline_weight</code> and <code>alternative_weight</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>baseline_weight</code> <code>str</code> <p>Name of the baseline weight variable.</p> required <code>alternative_weight</code> <code>str</code> <p>Name of the alternative weight variable.</p> required <code>param_mapping</code> <code>dict</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    baseline_weight: str,\n    alternative_weight: str,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    super().__init__(name, param_mapping)\n    self.base_key = baseline_weight\n    self.alt_key = alternative_weight\n    self.req_vars = [self.base_key, self.alt_key]\n    self.factor_parameters: List[str] = [\"lambda_int\"]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.GradientReweight","title":"GradientReweight","text":"<pre><code>GradientReweight(\n    name: str,\n    gradient_key_mapping: Dict[str, str],\n    baseline_weight: str,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Gradient reweight application. (e.g barr parameters) Requires precalculated gradients.</p> <p>Parameters required by this factor are the keys of <code>gradient_key_mapping</code>. Variables required by this factor are the values of <code>gradient_key_mapping</code> and <code>baseline_weight</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>gradient_key_mapping</code> <code>dict</code> <p>Dictionary mapping exposed variable names to gradient variable names.</p> required <code>baseline_weight</code> <code>str</code> <p>Name of the baseline weight variable.</p> required Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    gradient_key_mapping: Dict[str, str],\n    baseline_weight: str,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    super().__init__(name, param_mapping)\n    self.baseline_weight = baseline_weight\n    self.grad_key_map = gradient_key_mapping\n    self.req_vars = list(self.grad_key_map.values()) + [self.baseline_weight]\n    self.factor_parameters = list(self.grad_key_map.keys())\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.ClassifierGradientReweight","title":"ClassifierGradientReweight","text":"<pre><code>ClassifierGradientReweight(\n    name: str,\n    poly_features: List[Tuple[str, int]],\n    nominal_values: Dict[str, float],\n    gradient_col_template: str = \"g_{param}_{order}\",\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Per-event reweighting using classifier-fitted polynomial coefficients in log-weight space. Implements:</p> <pre><code>r_j(alpha) = exp( sum_{param,p} g_{param,p,j} * (alpha_param - alpha_nom_param)^p )\n</code></pre> <p>The g columns are expected to be present in the dataset, written by gradients.py with the naming convention g_{param}_{order} (e.g. g_abs_1, g_qeff_2).</p> <p>Parameters required by this factor are the unique parameter names in <code>poly_features</code>. Variables required by this factor are the g columns derived from <code>poly_features</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>poly_features</code> <code>list</code> <p>List of (param, order) tuples matching those used in training,                   e.g. [(\"abs\", 1), (\"abs\", 2), (\"qeff\", 1), (\"qeff\", 2)].</p> required <code>nominal_values</code> <code>dict</code> <p>Nominal systematic values, e.g. {\"abs\": 1.0, \"qeff\": 1.0}.</p> required <code>gradient_col_template</code> <code>str</code> <p>Format string for gradient column names.                          Defaults to \"g_{param}_{order}\".</p> <code>'g_{param}_{order}'</code> <code>param_mapping</code> <code>dict</code> <p>Parameter name remapping passed to base class.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    poly_features: List[Tuple[str, int]],\n    nominal_values: Dict[str, float],\n    gradient_col_template: str = \"g_{param}_{order}\",\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    super().__init__(name, param_mapping)\n    self.poly_features          = poly_features\n    self.nominal_values         = nominal_values\n    self.gradient_col_template  = gradient_col_template\n\n    # One input variable per polynomial term (the g columns in the dataset)\n    self.req_vars = [\n        gradient_col_template.format(param=param, order=order)\n        for param, order in poly_features\n    ]\n    # One free parameter per unique systematic (duplicates collapsed)\n    self.factor_parameters = list(dict.fromkeys(param for param, _ in poly_features))\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.VetoThreshold","title":"VetoThreshold","text":"<pre><code>VetoThreshold(\n    name,\n    threshold_a,\n    threshold_b,\n    threshold_c,\n    rescale_energy,\n    anchor_energy,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Changes the atm. passing fraction according to a second-order expansion of log10(splined_passing_fraction)</p> <p>Parameters required by this factor are: <code>e_threshold</code>. Variables required by this factor are the coefficients of the expansion.</p> <p>Read fir coefficients as well as anchor energy [GeV]</p> <p>Parameters:</p> Name Type Description Default <code>threshold_</code> <p>coefficients of second-order expansion</p> required <code>anchor_energy</code> <p>energy at which log10(PF) was expanded</p> required <code>rescale_energy</code> <p>scale 10**(fit parameter) to energy # TODO</p> required Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name,\n    threshold_a,\n    threshold_b,\n    threshold_c,\n    rescale_energy,\n    anchor_energy,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    \"\"\"\n    Read fir coefficients as well as anchor energy [GeV]\n\n    Args:\n        threshold_: coefficients of second-order expansion\n        anchor_energy: energy at which log10(PF) was expanded\n        rescale_energy: scale 10**(fit parameter) to energy # TODO\n    \"\"\"\n\n    super().__init__(name, param_mapping)\n\n    self.a = threshold_a\n    self.b = threshold_b\n    self.c = threshold_c\n    self.e_rescale = rescale_energy\n    self.e_anchor = anchor_energy\n    self.req_vars = [self.a, self.b, self.c]\n    self.factor_parameters: List[str] = [\"e_threshold\"]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.FixedVeto","title":"FixedVeto","text":"<pre><code>FixedVeto(\n    name: str,\n    passing_fraction: str,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Applies a fixed per-event passing fraction to the component weights.</p> <p>No parameters required. Variables required are the per-event passing fractions.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>param_mapping</code> <code>dict</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(self, \n             name: str, \n             passing_fraction: str,\n             param_mapping: Optional[Dict[str, str]] = None):\n    super().__init__(name, param_mapping)\n\n    self.factor_parameters = []\n    self.pf_key = passing_fraction\n    self.req_vars = [self.pf_key]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.SoftCut","title":"SoftCut","text":"<pre><code>SoftCut(\n    name: str,\n    cut_variable: str,\n    slope: float,\n    cut_value: float,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractUnbinnedFactor</code></p> <p>Factor for a soft cut on a specific variable.</p> <p>Parameters required by this factor are: <code>soft_cut</code>. Variables required by this factor are specified by <code>cut_variable</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>cut_variable</code> <code>str</code> <p>Name of the variable to apply the soft cut on.</p> required <code>slope</code> <code>float</code> <p>Slope of the sigmoid function defining the softness of the cut.</p> required <code>param_mapping</code> <code>dict</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    cut_variable: str,\n    slope: float,\n    cut_value: float,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    super().__init__(name, param_mapping)\n\n    self.cut_variable = cut_variable\n    self.slope = slope\n    self.cut_value = cut_value\n    self.factor_parameters = []#[\"soft_cut\"]\n    self.req_vars = [self.cut_variable]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.PerBinPolynomial","title":"PerBinPolynomial","text":"<pre><code>PerBinPolynomial(\n    name: str,\n    binning: AbstractBinning,\n    coefficients: Array,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractBinnedFactor</code></p> <p>Factor that applies a polynomial reweighting per bin. The polynomial coefficients are provided as a list of lists, where each inner list corresponds to a bin and contains the coefficients for the polynomial in that bin.</p> <p>Parameters required by this factor are: <code>scale</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Identifier for the factor.</p> required <code>binning</code> <code>AbstractBinning</code> <p>Binning object for the factor.</p> required <code>coefficients</code> <code>List[List[float]]</code> <p>List of lists containing polynomial coefficients for each bin.</p> required <code>param_mapping</code> <code>Optional[Dict[str, str]]</code> <p>Dictionary mapping factor parameter names to names in the parameter dictionary.</p> <code>None</code> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    binning: AbstractBinning,\n    coefficients: Array,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    super().__init__(name, binning, param_mapping)\n\n    if coefficients.shape[0] not in [2, 3]:\n        raise ValueError(\n            \"PerBinPolynomial only supports up to 2nd and 3rd order polynomials.\"\n        )\n\n    if self.binning.hist_dims != coefficients.shape[1:]:\n        raise ValueError(\n            f\"Shape of coefficients {coefficients.shape[1:]} does not match binning dimensions {self.binning.hist_dims}.\"\n        )\n\n    self.coefficients = coefficients\n    self.factor_parameters = [\"scale\"]\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.PerBinPolynomial.evaluate","title":"evaluate","text":"<pre><code>evaluate(\n    input_variables, parameter_values: Dict[str, float]\n) -&gt; Tuple[Array, Optional[Array]]\n</code></pre> <p>Evaluate the polynomial reweighting for each bin based on the input variables.</p> <p>Parameters:</p> Name Type Description Default <code>input_variables</code> <code>dict</code> <p>Dictionary containing input variables.</p> required <code>parameter_values</code> <code>dict</code> <p>Dictionary containing parameter values.</p> required <p>Returns:</p> Type Description <code>Tuple[Array, Optional[Array]]</code> <p>Tuple[Array, Array]: Mean and variance of the polynomial reweighting per bin.</p> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def evaluate(\n    self, input_variables, parameter_values: Dict[str, float]\n) -&gt; Tuple[Array, Optional[Array]]:\n    \"\"\"\n    Evaluate the polynomial reweighting for each bin based on the input variables.\n\n    Args:\n        input_variables (dict): Dictionary containing input variables.\n        parameter_values (dict): Dictionary containing parameter values.\n\n    Returns:\n        Tuple[Array, Array]: Mean and variance of the polynomial reweighting per bin.\n    \"\"\"\n\n    exposed_values = get_parameter_values(self, parameter_values)\n    scale = exposed_values[\"scale\"]\n\n\n    if self.coefficients.shape[0] == 2:\n        mu_add = scale * self.coefficients[0] + self.coefficients[1]\n\n    elif self.coefficients.shape[0] == 3:\n        mu_add = (\n            scale**2 * self.coefficients[0]\n            + scale * self.coefficients[1] \n            + self.coefficients[2]\n        )\n    else:\n        raise RuntimeError(\n            \"PerBinPolynomial only supports 2nd and 3rd order polynomials\")\n\n    return mu_add, None\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.SnowStormGradient","title":"SnowStormGradient","text":"<pre><code>SnowStormGradient(\n    name: str,\n    binning: AbstractBinning,\n    parameters: List[str],\n    gradient_names: List[str],\n    default: List[float],\n    split_values: List[float],\n    gradient_pickle: str,\n    param_mapping: Optional[Dict[str, str]] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractBinnedFactor</code></p> <p>Factor that applies a systematic parameter gradient. Is applied additive to each detector histogram.</p>"},{"location":"reference/api/factor/#pyForwardFolding.factor.SnowStormGradient--parameters","title":"Parameters","text":"<p>name : str     Name of the factor binning : AbstractBinning     Binning object for the factor parameters : list     List of parameter names gradient_names : list     List of gradient dictionary keys for each parameter default : list     List of default parameter values split_values : list     List of split values for each parameter gradient_pickle : str     Path to pickle file containing the gradients</p> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    binning: AbstractBinning,\n    parameters: List[str],\n    gradient_names: List[str],\n    default: List[float],\n    split_values: List[float],\n    gradient_pickle: str,\n    param_mapping: Optional[Dict[str, str]] = None,\n):\n    \"\"\"\n    Parameters\n    ----------\n    name : str\n        Name of the factor\n    binning : AbstractBinning\n        Binning object for the factor\n    parameters : list\n        List of parameter names\n    gradient_names : list\n        List of gradient dictionary keys for each parameter\n    default : list\n        List of default parameter values\n    split_values : list\n        List of split values for each parameter\n    gradient_pickle : str\n        Path to pickle file containing the gradients\n\n    \"\"\"\n    super().__init__(name, binning, param_mapping)\n\n    self.defaults = default\n    self.split_values = split_values\n    self.gradient_names = gradient_names\n\n    with open(gradient_pickle, \"rb\") as f:\n        self.gradients = pickle.load(f)\n\n    self.factor_parameters = parameters\n\n    ndim_grads = [len(be) - 1 for be in self.gradients[\"binning\"]]\n\n    for gname in self.gradient_names:\n        grad = self.gradients[gname][\"gradient\"]\n        if grad.shape != tuple(ndim_grads):\n            raise ValueError(\n               f\"Gradient '{gname}' has shape {grad.shape}, expected {ndim_grads}\"\n            )\n\n    if list(self.binning.hist_dims) != ndim_grads:\n        raise ValueError(\n            f\"Mismatch between binning dimensions ({self.binning.hist_dims}) and gradient dimensions ({ndim_grads})\"\n        )\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.SnowStormGradient.evaluate","title":"evaluate","text":"<pre><code>evaluate(\n    input_variables, parameter_values: Dict[str, float]\n) -&gt; Tuple[Array, Array]\n</code></pre> <p>Evaluate the systematic parameter gradient for the given detector configuration and the given exposed variables.</p> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def evaluate(\n    self, input_variables, parameter_values: Dict[str, float]\n) -&gt; Tuple[Array, Array]:\n    \"\"\"\n    Evaluate the systematic parameter gradient for the given\n    detector configuration and the given exposed variables.\n\n    \"\"\"\n\n    t_gradients = float(self.gradients[\"livetime\"])\n    exposed_values = get_parameter_values(self, parameter_values)\n\n    # calculate variation of systematic parameters w.r.t. split\n    #   value in order to correctly relate to the gradient dict.\n    #   Overall parameter shifts are taken into account here so\n    #   that gradients are applied w.r.t. their split value\n    #   but the fit parameter itself corresponds to the shifted value\n    mu_add = backend.zeros(self.binning.hist_dims)\n    ssq_add = backend.zeros(self.binning.hist_dims)\n\n    for i, sys_par in enumerate(self.exposed_parameters):\n        sys_val = exposed_values[sys_par]\n        gradient: Array = self.gradients[self.gradient_names[i]]\n\n        mu_add += (sys_val - self.split_values[i]) * gradient[\"gradient\"]\n        ssq_add += (\n            (sys_val - self.split_values[i]) * gradient[\"gradient_error\"]\n        ) ** 2\n\n    return mu_add / t_gradients, ssq_add / t_gradients**2\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.get_required_variable_values","title":"get_required_variable_values","text":"<pre><code>get_required_variable_values(\n    factor: AbstractUnbinnedFactor,\n    input_variable_values: Dict[str, Union[Array, float]],\n) -&gt; Dict[str, Union[Array, float]]\n</code></pre> <p>Extract required variable values for a factor from the input dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>factor</code> <code>AbstractFactor</code> <p>The factor requesting variables.</p> required <code>input_variable_values</code> <code>dict</code> <p>Dictionary containing all available input variables.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>Dict[str, Union[Array, float]]</code> <p>Dictionary containing only the required variables for the factor.</p> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def get_required_variable_values(\n    factor: AbstractUnbinnedFactor,\n    input_variable_values: Dict[str, Union[Array, float]],\n) -&gt; Dict[str, Union[Array, float]]:\n    \"\"\"\n    Extract required variable values for a factor from the input dictionary.\n\n    Args:\n        factor (AbstractFactor): The factor requesting variables.\n        input_variable_values (dict): Dictionary containing all available input variables.\n\n    Returns:\n        dict: Dictionary containing only the required variables for the factor.\n    \"\"\"\n    req_vars = factor.required_variables\n    return {var: input_variable_values[var] for var in req_vars}\n</code></pre>"},{"location":"reference/api/factor/#pyForwardFolding.factor.get_parameter_values","title":"get_parameter_values","text":"<pre><code>get_parameter_values(\n    factor: AbstractFactor, parameter_dict: Dict[str, float]\n) -&gt; Dict[str, float]\n</code></pre> <p>Extract parameter values for a factor from the parameter dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>factor</code> <code>AbstractFactor</code> <p>The factor requesting variables.</p> required <code>parameter_dict</code> <code>dict</code> <p>Dictionary mapping parameter names to values.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>Dict[str, float]</code> <p>Dictionary containing only the exposed variables for the factor.</p> Source code in <code>pyForwardFolding/factor.py</code> <pre><code>def get_parameter_values(\n    factor: AbstractFactor, parameter_dict: Dict[str, float]\n) -&gt; Dict[str, float]:\n    \"\"\"\n    Extract parameter values for a factor from the parameter dictionary.\n\n    Args:\n        factor (AbstractFactor): The factor requesting variables.\n        parameter_dict (dict): Dictionary mapping parameter names to values.\n\n    Returns:\n        dict: Dictionary containing only the exposed variables for the factor.\n    \"\"\"\n\n    par_mapping = factor.parameter_mapping\n    parameter_values = {\n        factor_var_name: parameter_dict[par_name]\n        for factor_var_name, par_name in par_mapping.items()\n    }\n    # Inject factor-level fixed parameters so evaluate() still sees them.\n    parameter_values.update(factor.fixed_factor_params)\n    return parameter_values\n</code></pre>"},{"location":"reference/api/likelihood/","title":"<code>pyForwardFolding.likelihood</code>","text":"<p>Likelihood classes (<code>PoissonLikelihood</code>, <code>SAYLikelihood</code>) and prior classes (<code>UniformPrior</code>, <code>GaussianUnivariatePrior</code>).</p>"},{"location":"reference/api/likelihood/#pyForwardFolding.likelihood","title":"pyForwardFolding.likelihood","text":""},{"location":"reference/api/likelihood/#pyForwardFolding.likelihood.AbstractPrior","title":"AbstractPrior","text":"<pre><code>AbstractPrior(\n    prior_seeds: Dict[str, float],\n    prior_bounds: Optional[\n        Dict[str, Tuple[float, float]]\n    ] = None,\n)\n</code></pre> <p>Abstract base class for priors used in likelihood evaluations.</p> <p>Parameters:</p> Name Type Description Default <code>prior_seeds</code> <code>Dict[str, float]</code> <p>Initial values for the parameters.</p> required <code>prior_bounds</code> <code>Optional[Dict[str, Tuple[float, float]]]</code> <p>Optional bounds for the parameters.</p> <code>None</code> Source code in <code>pyForwardFolding/likelihood.py</code> <pre><code>def __init__(\n    self,\n    prior_seeds: Dict[str, float],\n    prior_bounds: Optional[Dict[str, Tuple[float, float]]] = None,\n):\n    \"\"\"\n    Abstract base class for priors used in likelihood evaluations.\n\n    Args:\n        prior_seeds (Dict[str, float]): Initial values for the parameters.\n        prior_bounds (Optional[Dict[str, Tuple[float, float]]]): Optional bounds for the parameters.\n    \"\"\"\n    self.prior_seeds = prior_seeds\n\n    if prior_bounds is None:\n        prior_bounds = {par: (-np.inf, np.inf) for par in prior_seeds.keys()}\n    self.prior_bounds = prior_bounds\n</code></pre>"},{"location":"reference/api/likelihood/#pyForwardFolding.likelihood.AbstractPrior.prior_variables","title":"prior_variables  <code>property</code>","text":"<pre><code>prior_variables: Set[str]\n</code></pre> <p>Get the set of prior variables.</p> <p>Returns:</p> Type Description <code>Set[str]</code> <p>Set[str]: A set of all prior variable names.</p>"},{"location":"reference/api/likelihood/#pyForwardFolding.likelihood.GaussianUnivariatePrior","title":"GaussianUnivariatePrior","text":"<pre><code>GaussianUnivariatePrior(\n    prior_params: Dict[str, Tuple[float, float]],\n    prior_seeds: Dict[str, float],\n    prior_bounds: Optional[\n        Dict[str, Tuple[float, float]]\n    ] = None,\n)\n</code></pre> <p>               Bases: <code>AbstractPrior</code></p> <p>A univariate Gaussian prior for parameters.</p> <p>Note, this prior is not normalized, it only provides a log likelihood contribution.</p> <p>Parameters:</p> Name Type Description Default <code>prior_params</code> <code>Dict[str, Tuple[float, float]]</code> <p>Dictionary mapping parameter names to (mean, std) tuples.</p> required <code>prior_seeds</code> <code>Dict[str, float]</code> <p>Initial values for the parameters.</p> required <code>prior_bounds</code> <code>Optional[Dict[str, Tuple[float, float]]]</code> <p>Optional bounds for the parameters.</p> <code>None</code> Source code in <code>pyForwardFolding/likelihood.py</code> <pre><code>def __init__(\n    self,\n    prior_params: Dict[str, Tuple[float, float]],\n    prior_seeds: Dict[str, float],\n    prior_bounds: Optional[Dict[str, Tuple[float, float]]] = None,\n):\n    \"\"\"\n    A univariate Gaussian prior for parameters.\n\n    Note, this prior is not normalized, it only provides a log likelihood contribution.\n\n    Args:\n        prior_params (Dict[str, Tuple[float, float]]): Dictionary mapping parameter names to (mean, std) tuples.\n        prior_seeds (Dict[str, float]): Initial values for the parameters.\n        prior_bounds (Optional[Dict[str, Tuple[float, float]]]): Optional bounds for the parameters.\n    \"\"\"\n    super().__init__(prior_seeds, prior_bounds)\n    self.prior_params = prior_params\n</code></pre>"},{"location":"reference/api/likelihood/#pyForwardFolding.likelihood.UniformPrior","title":"UniformPrior","text":"<pre><code>UniformPrior(\n    prior_seeds: Dict[str, float],\n    prior_bounds: Dict[str, Tuple[float, float]],\n)\n</code></pre> <p>               Bases: <code>AbstractPrior</code></p> <p>A uniform prior for parameters.</p> <p>Note, this is essentially a dummy prior. No bounds checkinf is performed.</p> <p>Parameters:</p> Name Type Description Default <code>prior_seeds</code> <code>Dict[str, float]</code> <p>Initial values for the parameters.</p> required <code>prior_bounds</code> <code>Optional[Dict[str, Tuple[float, float]]]</code> <p>Bounds for the parameters.</p> required Source code in <code>pyForwardFolding/likelihood.py</code> <pre><code>def __init__(\n    self,\n    prior_seeds: Dict[str, float],\n    prior_bounds: Dict[str, Tuple[float, float]],\n):\n    \"\"\"\n    A uniform prior for parameters.\n\n    Note, this is essentially a dummy prior. No bounds checkinf is performed.\n\n    Args:\n        prior_seeds (Dict[str, float]): Initial values for the parameters.\n        prior_bounds (Optional[Dict[str, Tuple[float, float]]]): Bounds for the parameters.\n    \"\"\"\n    super().__init__(prior_seeds, prior_bounds)\n</code></pre>"},{"location":"reference/api/likelihood/#pyForwardFolding.likelihood.AbstractLikelihood","title":"AbstractLikelihood","text":"<pre><code>AbstractLikelihood(analysis: Analysis, priors: List[T])\n</code></pre> <p>Abstract base class representing a likelihood function to be evaluated against observed data.</p> Source code in <code>pyForwardFolding/likelihood.py</code> <pre><code>def __init__(self, analysis: Analysis, priors: List[T]):\n    # Check that priors are set for all exposed parameters\n    exposed = analysis.exposed_parameters\n\n    prior_variables = set()\n    for prior in priors:\n        prior_variables.update(prior.prior_variables)\n\n    if exposed != prior_variables:\n        raise ValueError(\"Mismatch between exposed parameters and prior variables.\")\n\n    self.analysis = analysis\n    self.priors = priors\n</code></pre>"},{"location":"reference/api/likelihood/#pyForwardFolding.likelihood.AbstractLikelihood.get_analysis","title":"get_analysis","text":"<pre><code>get_analysis() -&gt; Analysis\n</code></pre> <p>Get the analysis associated with this likelihood.</p> <p>Returns:</p> Name Type Description <code>Analysis</code> <code>Analysis</code> <p>The associated analysis.</p> Source code in <code>pyForwardFolding/likelihood.py</code> <pre><code>def get_analysis(self) -&gt; Analysis:\n    \"\"\"\n    Get the analysis associated with this likelihood.\n\n    Returns:\n        Analysis: The associated analysis.\n    \"\"\"\n    return self.analysis\n</code></pre>"},{"location":"reference/api/likelihood/#pyForwardFolding.likelihood.AbstractLikelihood.get_seeds","title":"get_seeds","text":"<pre><code>get_seeds() -&gt; Dict[str, float]\n</code></pre> <p>Get the seeds for the parameters defined by the priors.</p> <p>Returns:</p> Type Description <code>Dict[str, float]</code> <p>Dict[str, float]: A dictionary mapping parameter names to their initial values.</p> Source code in <code>pyForwardFolding/likelihood.py</code> <pre><code>def get_seeds(self) -&gt; Dict[str, float]:\n    \"\"\"\n    Get the seeds for the parameters defined by the priors.\n\n    Returns:\n        Dict[str, float]: A dictionary mapping parameter names to their initial values.\n    \"\"\"\n    seeds = {}\n    for prior in self.priors:\n        seeds.update(prior.prior_seeds)\n    return seeds\n</code></pre>"},{"location":"reference/api/likelihood/#pyForwardFolding.likelihood.AbstractLikelihood.get_bounds","title":"get_bounds","text":"<pre><code>get_bounds() -&gt; Dict[str, Tuple[float, float]]\n</code></pre> <p>Get the bounds for the parameters defined by the priors.</p> <p>Returns:</p> Type Description <code>Dict[str, Tuple[float, float]]</code> <p>Dict[str, Tuple[float, float]]: A dictionary mapping parameter names to their bounds.</p> Source code in <code>pyForwardFolding/likelihood.py</code> <pre><code>def get_bounds(self) -&gt; Dict[str, Tuple[float, float]]:\n    \"\"\"\n    Get the bounds for the parameters defined by the priors.\n\n    Returns:\n        Dict[str, Tuple[float, float]]: A dictionary mapping parameter names to their bounds.\n    \"\"\"\n    bounds = {}\n    for prior in self.priors:\n        bounds.update(prior.prior_bounds)\n    return bounds\n</code></pre>"},{"location":"reference/api/likelihood/#pyForwardFolding.likelihood.PoissonLikelihood","title":"PoissonLikelihood","text":"<pre><code>PoissonLikelihood(analysis: Analysis, priors: List[T])\n</code></pre> <p>               Bases: <code>AbstractLikelihood</code></p> <p>A likelihood function that assumes Poisson-distributed data.</p> <p>Parameters:</p> Name Type Description Default <code>analysis</code> <code>Analysis</code> <p>The analysis to evaluate against observed data.</p> required Source code in <code>pyForwardFolding/likelihood.py</code> <pre><code>def __init__(self, analysis: Analysis, priors: List[T]):\n    super().__init__(analysis, priors)\n</code></pre>"},{"location":"reference/api/likelihood/#pyForwardFolding.likelihood.PoissonLikelihood.llh","title":"llh","text":"<pre><code>llh(\n    observed_data: Dict[str, Array],\n    datasets: Dict[str, Dict[str, Union[Array, float]]],\n    parameter_values: Dict[str, float],\n    empty_bins: str = \"skip\",\n) -&gt; Array\n</code></pre> <p>Compute the log-likelihood between model predictions and observed data assuming Poisson statistics.</p> <p>Parameters:</p> Name Type Description Default <code>observed_data</code> <code>Dict[str, Array]</code> <p>A dictionary mapping component names to observed data.</p> required <code>datasets</code> <code>Dict[str, Dict[str, Union[Array, float]]]</code> <p>Input datasets for the model evaluation.</p> required <code>parameter_values</code> <code>Dict[str, float]</code> <p>Variables exposed by previously evaluated components.</p> required <code>empty_bins</code> <code>str</code> <p>Strategy for handling empty bins (<code>\"skip\"</code> or <code>\"throw\"</code>).</p> <code>'skip'</code> <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>The log-likelihood value.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If empty bins are encountered and <code>empty_bins=\"throw\"</code>.</p> Source code in <code>pyForwardFolding/likelihood.py</code> <pre><code>def llh(\n    self,\n    observed_data: Dict[str, Array],\n    datasets: Dict[str, Dict[str, Union[Array, float]]],\n    parameter_values: Dict[str, float],\n    empty_bins: str = \"skip\",\n) -&gt; Array:\n    \"\"\"\n    Compute the log-likelihood between model predictions and observed data assuming Poisson statistics.\n\n    Args:\n        observed_data (Dict[str, Array]): A dictionary mapping component names to observed data.\n        datasets (Dict[str, Dict[str, Union[Array, float]]]): Input datasets for the model evaluation.\n        parameter_values (Dict[str, float]): Variables exposed by previously evaluated components.\n        empty_bins (str): Strategy for handling empty bins (`\"skip\"` or `\"throw\"`).\n\n    Returns:\n        Array: The log-likelihood value.\n\n    Raises:\n        ValueError: If empty bins are encountered and `empty_bins=\"throw\"`.\n    \"\"\"\n    # Evaluate the analysis\n    ana_eval, ana_eval_ssq = self.analysis.evaluate(datasets, parameter_values)\n    llh = backend.array(0.0)\n\n    for comp_name, comp_eval in ana_eval.items():\n        obs = observed_data.get(comp_name)\n        if obs is None:\n            raise ValueError(f\"No observed data for component '{comp_name}'\")\n        # Handle empty bins\n        non_empty_expectation = comp_eval &gt; 0\n        # non_empty_observations = obs &gt; 0\n        if empty_bins == \"skip\":\n            comp_eval_shift = backend.select(non_empty_expectation, comp_eval, 1e-8)\n            # obs_shift = backend.select(non_empty_observations,\n            #                           obs,\n            #                           obs + 1e-8)\n            llh_bins = backend.where_sum(\n                non_empty_expectation,\n                -comp_eval_shift\n                + obs * backend.log(comp_eval_shift)\n                - backend.gammaln(obs + 1),\n                0,\n            )\n            # llh_sat = backend.where_sum(non_empty_observations,\n            #                            -obs + obs * backend.log(obs_shift),\n            #                            0)\n            # llh += (llh_bins - llh_sat)\n            llh += llh_bins\n        elif empty_bins == \"throw\":\n            if not np.all(non_empty_expectation):\n                raise ValueError(f\"Empty bins in component '{comp_name}'\")\n\n    for p in self.priors:\n        llh += p.log_pdf(parameter_values)\n    return llh\n</code></pre>"},{"location":"reference/api/likelihood/#pyForwardFolding.likelihood.SAYLikelihood","title":"SAYLikelihood","text":"<pre><code>SAYLikelihood(analysis: Analysis, priors: List[T])\n</code></pre> <p>               Bases: <code>AbstractLikelihood</code></p> <p>Extension of the Poisson likelihood to account for limited MC statistics.</p> <p>https://doi.org/10.48550/arXiv.1901.04645</p> Source code in <code>pyForwardFolding/likelihood.py</code> <pre><code>def __init__(self, analysis: Analysis, priors: List[T]):\n    super().__init__(analysis, priors)\n</code></pre>"},{"location":"reference/api/minimizer/","title":"<code>pyForwardFolding.minimizer</code>","text":"<p><code>ScipyMinimizer</code> (L-BFGS-B via SciPy) and <code>MinuitMinimizer</code> (MIGRAD via iMinuit) implementations of <code>AbstractMinimizer</code>. Both wrap an <code>AbstractLikelihood</code> and flatten the parameter dictionary for the underlying solver.</p>"},{"location":"reference/api/minimizer/#pyForwardFolding.minimizer","title":"pyForwardFolding.minimizer","text":""},{"location":"reference/api/minimizer/#pyForwardFolding.minimizer.WrappedLLH","title":"WrappedLLH","text":"<pre><code>WrappedLLH(\n    likelihood: AbstractLikelihood,\n    obs: Dict,\n    datasets: Dict,\n    fixed_params: Dict,\n)\n</code></pre> <p>A wrapper for the likelihood function to flatten parameters for optimization.</p> <p>Parameters:</p> Name Type Description Default <code>likelihood</code> <code>Callable</code> <p>The likelihood function to wrap.</p> required <code>obs</code> <code>Dict</code> <p>Observed data.</p> required <code>datasets</code> <code>Dict</code> <p>Input datasets.</p> required <code>fixed_params</code> <code>Dict</code> <p>Parameters to keep fixed during optimization.</p> required Source code in <code>pyForwardFolding/minimizer.py</code> <pre><code>def __init__(\n    self,\n    likelihood: AbstractLikelihood,\n    obs: Dict,\n    datasets: Dict,\n    fixed_params: Dict,\n):\n    self.likelihood = likelihood\n    self.obs = obs\n    self.datasets = datasets\n    self.fixed_params = fixed_params\n    self.parameters = sorted(\n        list(self.likelihood.get_analysis().exposed_parameters)\n    )\n</code></pre>"},{"location":"reference/api/minimizer/#pyForwardFolding.minimizer.WrappedLLH.__call__","title":"__call__","text":"<pre><code>__call__(flat_params) -&gt; Array\n</code></pre> <p>Evaluate the likelihood with flattened parameters.</p> <p>Parameters:</p> Name Type Description Default <code>flat_params</code> <p>Flattened parameters for optimization.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>Array</code> <p>The negative likelihood value.</p> Source code in <code>pyForwardFolding/minimizer.py</code> <pre><code>def __call__(self, flat_params) -&gt; Array:\n    \"\"\"\n    Evaluate the likelihood with flattened parameters.\n\n    Args:\n        flat_params: Flattened parameters for optimization.\n\n    Returns:\n        float: The negative likelihood value.\n    \"\"\"\n    restructured_args = restructure_args(\n        flat_params, self.parameters, self.fixed_params\n    )\n    binned_llh = self.likelihood.llh(self.obs, self.datasets, restructured_args)\n    return -binned_llh\n</code></pre>"},{"location":"reference/api/minimizer/#pyForwardFolding.minimizer.AbstractMinimizer","title":"AbstractMinimizer","text":"<pre><code>AbstractMinimizer(llh: AbstractLikelihood)\n</code></pre> <p>Abstract class for minimizers.</p> <p>Parameters:</p> Name Type Description Default <code>llh</code> <code>AbstractLikelihood</code> <p>The likelihood function to minimize.</p> required <code>obs</code> <code>Dict</code> <p>Observed data.</p> required <code>dataset</code> <code>Dict</code> <p>Input datasets.</p> required <code>fixed_pars</code> <code>Dict</code> <p>Parameters to keep fixed during optimization.</p> required Source code in <code>pyForwardFolding/minimizer.py</code> <pre><code>def __init__(\n    self,\n    llh: AbstractLikelihood,\n):\n    self.llh = llh\n    self.exposed_vars = self.llh.get_analysis().exposed_parameters\n\n    self.parameters = sorted(list(self.exposed_vars))\n</code></pre>"},{"location":"reference/api/minimizer/#pyForwardFolding.minimizer.AbstractMinimizer.make_bounds","title":"make_bounds","text":"<pre><code>make_bounds(\n    fixed_pars: Optional[Dict[str, float]] = None,\n) -&gt; Bounds\n</code></pre> <p>Create bounds for the optimization.</p> Source code in <code>pyForwardFolding/minimizer.py</code> <pre><code>def make_bounds(self, fixed_pars: Optional[Dict[str, float]] = None) -&gt; Bounds:\n    \"\"\"\n    Create bounds for the optimization.\n    \"\"\"\n    bounds_dict = {}\n    for prior in self.llh.priors:\n        bounds_dict.update(prior.prior_bounds)\n\n    bounds_list = destructure_args(bounds_dict, self.parameters, fixed_pars)\n    bounds_lower = [bound[0] for bound in bounds_list]\n    bounds_upper = [bound[1] for bound in bounds_list]\n    return Bounds(bounds_lower, bounds_upper)  # type: ignore\n</code></pre>"},{"location":"reference/api/minimizer/#pyForwardFolding.minimizer.AbstractMinimizer.make_seeds","title":"make_seeds","text":"<pre><code>make_seeds(\n    fixed_pars: Optional[Dict[str, float]] = None,\n) -&gt; List[float]\n</code></pre> <p>Create seeds for the optimization.</p> Source code in <code>pyForwardFolding/minimizer.py</code> <pre><code>def make_seeds(self, fixed_pars: Optional[Dict[str, float]] = None) -&gt; List[float]:\n    \"\"\"\n    Create seeds for the optimization.\n    \"\"\"\n    seeds_dict = {}\n    for prior in self.llh.priors:\n        seeds_dict.update(prior.prior_seeds)\n\n    return destructure_args(seeds_dict, self.parameters, fixed_pars)\n</code></pre>"},{"location":"reference/api/minimizer/#pyForwardFolding.minimizer.MinuitMinimizer","title":"MinuitMinimizer","text":"<pre><code>MinuitMinimizer(\n    llh: AbstractLikelihood,\n    tol: float = 0.01,\n    strategy: int = 1,\n    simplex_prefit: bool = False,\n)\n</code></pre> <p>               Bases: <code>AbstractMinimizer</code></p> Source code in <code>pyForwardFolding/minimizer.py</code> <pre><code>def __init__(\n    self,\n    llh: AbstractLikelihood,\n    tol: float = 1e-2,\n    strategy: int = 1,\n    simplex_prefit: bool = False,\n):\n    super().__init__(\n        llh=llh,\n    )\n    self.tol = tol\n    self.strategy = strategy\n    self.simplex_prefit = simplex_prefit\n</code></pre>"},{"location":"reference/api/minimizer/#pyForwardFolding.minimizer.flat_index_dict_mapping","title":"flat_index_dict_mapping","text":"<pre><code>flat_index_dict_mapping(\n    exp_vars: Set[str],\n    fixed_params: Optional[Dict[str, Any]] = None,\n) -&gt; Dict[str, int]\n</code></pre> <p>Create a mapping of variable names to flat indices for optimization.</p> <p>Parameters:</p> Name Type Description Default <code>exp_vars</code> <code>Dict[str, List[str]]</code> <p>Exposed variables.</p> required <code>fixed_params</code> <code>Dict[str, Dict[str, Any]]</code> <p>Fixed parameters. Defaults to None.</p> <code>None</code> <p>Returns:</p> Type Description <code>Dict[str, int]</code> <p>Dict[str, Dict[str, int]]: Mapping of variable names to flat indices.</p> Source code in <code>pyForwardFolding/minimizer.py</code> <pre><code>def flat_index_dict_mapping(\n    exp_vars: Set[str], fixed_params: Optional[Dict[str, Any]] = None\n) -&gt; Dict[str, int]:\n    \"\"\"\n    Create a mapping of variable names to flat indices for optimization.\n\n    Args:\n        exp_vars (Dict[str, List[str]]): Exposed variables.\n        fixed_params (Dict[str, Dict[str, Any]], optional): Fixed parameters. Defaults to None.\n\n    Returns:\n        Dict[str, Dict[str, int]]: Mapping of variable names to flat indices.\n    \"\"\"\n    ix_dict = {}\n    for ix, var_name in enumerate(exp_vars):\n        if fixed_params and var_name in fixed_params:\n            continue\n        ix_dict[var_name] = ix\n\n    return ix_dict\n</code></pre>"},{"location":"reference/api/minimizer/#pyForwardFolding.minimizer.restructure_args","title":"restructure_args","text":"<pre><code>restructure_args(\n    flat_args,\n    exp_vars: List[str],\n    fixed_params: Optional[Dict[str, Any]] = None,\n) -&gt; Dict[str, Any]\n</code></pre> <p>Restructure flat arguments into the original parameter structure.</p> <p>Parameters:</p> Name Type Description Default <code>flat_args</code> <p>Flattened arguments.</p> required <code>exp_vars</code> <code>Dict[str, List[str]]</code> <p>Exposed variables.</p> required <code>fixed_params</code> <code>Dict[str, Dict[str, Any]]</code> <p>Fixed parameters. Defaults to None.</p> <code>None</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Dict[str, Any]]: Restructured arguments.</p> Source code in <code>pyForwardFolding/minimizer.py</code> <pre><code>def restructure_args(\n    flat_args, exp_vars: List[str], fixed_params: Optional[Dict[str, Any]] = None\n) -&gt; Dict[str, Any]:\n    \"\"\"\n    Restructure flat arguments into the original parameter structure.\n\n    Args:\n        flat_args: Flattened arguments.\n        exp_vars (Dict[str, List[str]]): Exposed variables.\n        fixed_params (Dict[str, Dict[str, Any]], optional): Fixed parameters. Defaults to None.\n\n    Returns:\n        Dict[str, Dict[str, Any]]: Restructured arguments.\n    \"\"\"\n    args = {}\n    ix = 0\n    for var_name in exp_vars:\n        if fixed_params and var_name in fixed_params:\n            args[var_name] = fixed_params[var_name]\n        else:\n            args[var_name] = flat_args[ix]\n            ix += 1\n    return args\n</code></pre>"},{"location":"reference/api/minimizer/#pyForwardFolding.minimizer.destructure_args","title":"destructure_args","text":"<pre><code>destructure_args(\n    params: Dict[str, T],\n    exp_vars: List[str],\n    fixed_params: Optional[Dict[str, Any]] = None,\n) -&gt; List[T]\n</code></pre> <p>Convert a dictionary of parameters into a flat list.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>Dict[str, Any]</code> <p>Parameters to flatten.</p> required <code>exp_vars</code> <code>Dict[str, List[str]]</code> <p>Exposed variables.</p> required <code>fixed_params</code> <code>Dict[str, Dict[str, Any]]</code> <p>Fixed parameters. Defaults to None.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[T]</code> <p>List[float]: Flattened parameters.</p> Source code in <code>pyForwardFolding/minimizer.py</code> <pre><code>def destructure_args(\n    params: Dict[str, T],\n    exp_vars: List[str],\n    fixed_params: Optional[Dict[str, Any]] = None,\n) -&gt; List[T]:\n    \"\"\"\n    Convert a dictionary of parameters into a flat list.\n\n    Args:\n        params (Dict[str, Any]): Parameters to flatten.\n        exp_vars (Dict[str, List[str]]): Exposed variables.\n        fixed_params (Dict[str, Dict[str, Any]], optional): Fixed parameters. Defaults to None.\n\n    Returns:\n        List[float]: Flattened parameters.\n    \"\"\"\n    flat_params = []\n    for var_name in exp_vars:\n        if fixed_params and var_name in fixed_params:\n            continue\n        flat_params.append(params[var_name])\n    return flat_params\n</code></pre>"},{"location":"reference/api/model/","title":"<code>pyForwardFolding.model</code>","text":"<p><code>Model</code>: a sum of <code>ModelComponent</code>s, each tied to a per-component baseline-weight key.</p>"},{"location":"reference/api/model/#pyForwardFolding.model","title":"pyForwardFolding.model","text":""},{"location":"reference/api/model/#pyForwardFolding.model.Model","title":"Model","text":"<pre><code>Model(\n    name: str,\n    components: List[ModelComponent],\n    baseline_weights: List[str],\n)\n</code></pre> <p>A forward-folding model consisting of multiple components, each with an associated baseline weight.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the model.</p> required <code>components</code> <code>List[ModelComponent]</code> <p>The components that make up the model.</p> required <code>baseline_weights</code> <code>List[str]</code> <p>Symbols (as strings) representing the baseline weight for each component.</p> required Notes <ul> <li>Component names must be unique within a model.</li> </ul> Source code in <code>pyForwardFolding/model.py</code> <pre><code>def __init__(\n    self, name: str, components: List[ModelComponent], baseline_weights: List[str]\n):\n    self.name = name\n    self.components = components\n    self.baseline_weights = baseline_weights\n\n    # Ensure component names are unique\n    component_names = [component.name for component in components]\n    if len(component_names) != len(set(component_names)):\n        raise ValueError(\"Model components must have unique names\")\n</code></pre>"},{"location":"reference/api/model/#pyForwardFolding.model.Model.required_variables","title":"required_variables  <code>property</code>","text":"<pre><code>required_variables: Set[str]\n</code></pre> <p>Get all variables required by any component in the model.</p> <p>Returns:</p> Type Description <code>Set[str]</code> <p>Set[str]: A set containing all required variables.</p>"},{"location":"reference/api/model/#pyForwardFolding.model.Model.exposed_parameters","title":"exposed_parameters  <code>property</code>","text":"<pre><code>exposed_parameters: Set[str]\n</code></pre> <p>Get all parameters exposed by the model's components.</p> <p>Returns:</p> Type Description <code>Set[str]</code> <p>Set[str]: A set of all exposed parameters from all components.</p>"},{"location":"reference/api/model/#pyForwardFolding.model.Model.parameter_mapping","title":"parameter_mapping  <code>property</code>","text":"<pre><code>parameter_mapping: Dict[str, Dict[str, Dict[str, str]]]\n</code></pre> <p>Get the mapping of model parameters to their respective components.</p> <p>Returns:</p> Type Description <code>Dict[str, Dict[str, Dict[str, str]]]</code> <p>Dict[str, Dict[str, Dict[str, str]]]: A dictionary mapping component names to their parameter names.</p>"},{"location":"reference/api/model/#pyForwardFolding.model.Model.from_pairs","title":"from_pairs  <code>classmethod</code>","text":"<pre><code>from_pairs(\n    name: str, components: List[Tuple[str, ModelComponent]]\n) -&gt; Model\n</code></pre> <p>Alternate constructor to create a Model from a list of (baseline_weight, component) pairs.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the model.</p> required <code>components</code> <code>List[Tuple[str, ModelComponent]]</code> <p>A list of (baseline_weight, component) pairs.</p> required <p>Returns:</p> Name Type Description <code>Model</code> <code>Model</code> <p>A new Model instance.</p> Source code in <code>pyForwardFolding/model.py</code> <pre><code>@classmethod\ndef from_pairs(\n    cls, name: str, components: List[Tuple[str, ModelComponent]]\n) -&gt; \"Model\":\n    \"\"\"\n    Alternate constructor to create a Model from a list of (baseline_weight, component) pairs.\n\n    Args:\n        name (str): The name of the model.\n        components (List[Tuple[str, ModelComponent]]): A list of (baseline_weight, component) pairs.\n\n    Returns:\n        Model: A new Model instance.\n    \"\"\"\n    baseline_weights = [pair[0] for pair in components]\n    component_list = [pair[1] for pair in components]\n    return cls(name, component_list, baseline_weights)\n</code></pre>"},{"location":"reference/api/model/#pyForwardFolding.model.Model.evaluate","title":"evaluate","text":"<pre><code>evaluate(\n    input_variables: Dict[str, Union[Array, float]],\n    parameter_values: Dict[str, float],\n) -&gt; Array\n</code></pre> <p>Evaluate the model by computing the sum of all components.</p> <p>Parameters:</p> Name Type Description Default <code>input_variables</code> <code>Dict[str, Union[Array, float]]</code> <p>Input variables for model evaluation.</p> required <code>parameter_values</code> <code>Dict[str, float]</code> <p>Variables exposed by previously evaluated components.</p> required <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>The modified <code>output</code> containing the sum of all components.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If any baseline weight is not found in the input variables.</p> Source code in <code>pyForwardFolding/model.py</code> <pre><code>def evaluate(\n    self,\n    input_variables: Dict[str, Union[Array, float]],\n    parameter_values: Dict[str, float],\n) -&gt; Array:\n    \"\"\"\n    Evaluate the model by computing the sum of all components.\n\n    Args:\n        input_variables (Dict[str, Union[Array, float]]): Input variables for model evaluation.\n        parameter_values (Dict[str, float]): Variables exposed by previously evaluated components.\n\n    Returns:\n        Array: The modified `output` containing the sum of all components.\n\n    Raises:\n        ValueError: If any baseline weight is not found in the input variables.\n    \"\"\"\n\n    output = backend.array(0.0)\n    for component, baseline_weight in zip(self.components, self.baseline_weights):\n        # Get the baseline weight value\n        baseline_weight_value = input_variables.get(baseline_weight, None)\n        if baseline_weight_value is None:\n            raise ValueError(\n                f\"Baseline weight '{baseline_weight}' not found in input variables\"\n            )\n\n        # Accumulate the result into the output\n        output += baseline_weight_value * component.evaluate(input_variables, parameter_values)\n\n    return output\n</code></pre>"},{"location":"reference/api/model/#pyForwardFolding.model.Model.evaluate_per_component","title":"evaluate_per_component","text":"<pre><code>evaluate_per_component(\n    input_variables: Dict[str, Union[Array, float]],\n    parameter_values: Dict[str, float],\n) -&gt; Dict[str, Union[Array, float]]\n</code></pre> <p>Evaluate each component of the model individually. Args:     input_variables (Dict[str, Union[Array, float]]): Input variables for model evaluation.     parameter_values (Dict[str, float]): Variables exposed by previously evaluated components. Returns:     Dict[str, Union[Array, float]]: A dictionary containing evaluation results for each component. Raises:     ValueError: If any baseline weight is not found in the input variables.</p> Source code in <code>pyForwardFolding/model.py</code> <pre><code>def evaluate_per_component(\n    self,\n    input_variables: Dict[str, Union[Array, float]],\n    parameter_values: Dict[str, float],\n) -&gt; Dict[str, Union[Array, float]]:\n    \"\"\"\n    Evaluate each component of the model individually.\n    Args:\n        input_variables (Dict[str, Union[Array, float]]): Input variables for model evaluation.\n        parameter_values (Dict[str, float]): Variables exposed by previously evaluated components.\n    Returns:\n        Dict[str, Union[Array, float]]: A dictionary containing evaluation results for each component.\n    Raises:\n        ValueError: If any baseline weight is not found in the input variables.\n    \"\"\"\n\n    # Ensure all input variables have the same length\n    input_var_lengths = [\n        len(value) if isinstance(value, Array) else 1\n        for value in input_variables.values()\n    ]\n    if not all(length == input_var_lengths[0] for length in input_var_lengths):\n        raise ValueError(\"All input variables must have the same length\")\n    output: Dict[str, Union[Array, float]] = {}\n    for component, baseline_weight in zip(self.components, self.baseline_weights):\n        # Get the baseline weight value\n        baseline_weight_value = input_variables.get(baseline_weight, None)\n        if baseline_weight_value is None:\n            raise ValueError(\n                f\"Baseline weight '{baseline_weight}' not found in input variables\"\n            )\n\n\n        # Accumulate the result into the output\n        output[component.name] = baseline_weight_value * component.evaluate(input_variables, parameter_values)\n\n    return output\n</code></pre>"},{"location":"reference/api/model/#pyForwardFolding.model.Model.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>String representation of the Model object.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>A string representation of the model.</p> Source code in <code>pyForwardFolding/model.py</code> <pre><code>def __repr__(self):\n    \"\"\"\n    String representation of the Model object.\n\n    Returns:\n        str: A string representation of the model.\n    \"\"\"\n    lines = []\n    lines.append(f\"Model: {self.name}\")\n    lines.append(f\"  Components ({len(self.components)}):\")\n    for comp, weight in zip(self.components, self.baseline_weights):\n        lines.append(f\"    \u2022 {comp.name} (weight: {weight})\")\n        # Indent the component representation\n        comp_repr = str(comp).replace('\\n', '\\n      ')\n        lines.append(f\"      {comp_repr}\")\n    lines.append(f\"  Required variables: {sorted(self.required_variables)}\")\n    lines.append(f\"  Exposed parameters: {sorted(self.exposed_parameters)}\")\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/model/#pyForwardFolding.model.Model.repr_markdown","title":"repr_markdown","text":"<pre><code>repr_markdown(\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_summary: bool = True,\n    show_component_details: bool = True,\n) -&gt; str\n</code></pre> <p>Configurable markdown representation of the Model object.</p> <p>Parameters:</p> Name Type Description Default <code>indent_level</code> <code>int</code> <p>The level of indentation (each level adds 2 spaces). Default is 0.</p> <code>0</code> <code>bullet_style</code> <code>str</code> <p>Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".</p> <code>'-'</code> <code>include_summary</code> <code>bool</code> <p>Whether to include required variables and exposed parameters summary. Default is True.</p> <code>True</code> <code>show_component_details</code> <code>bool</code> <p>Whether to show detailed component markdown. Default is True.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A configurable markdown representation of the model.</p> Source code in <code>pyForwardFolding/model.py</code> <pre><code>def repr_markdown(\n    self,\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_summary: bool = True,\n    show_component_details: bool = True,\n) -&gt; str:\n    \"\"\"\n    Configurable markdown representation of the Model object.\n\n    Args:\n        indent_level (int): The level of indentation (each level adds 2 spaces). Default is 0.\n        bullet_style (str): Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".\n        include_summary (bool): Whether to include required variables and exposed parameters summary. Default is True.\n        show_component_details (bool): Whether to show detailed component markdown. Default is True.\n\n    Returns:\n        str: A configurable markdown representation of the model.\n    \"\"\"\n    indent = \"  \" * indent_level\n    sub_indent = \"  \" * (indent_level + 1)\n\n    lines = []\n\n    # Header with appropriate level based on indent\n    if indent_level == 0:\n        lines.append(f\"### Model: {self.name}\")\n    else:\n        lines.append(f\"{indent}{bullet_style} **Model:** {self.name}\")\n\n    if include_summary:\n        lines.append(\"\")\n        if indent_level == 0:\n            lines.append(f\"**Required variables:** `{sorted(self.required_variables)}`\\n\")\n            lines.append(f\"**Exposed parameters:** `{sorted(self.exposed_parameters)}`\")\n        else:\n            lines.append(f\"{sub_indent}{bullet_style} Required variables: `{sorted(self.required_variables)}`\")\n            lines.append(f\"{sub_indent}{bullet_style} Exposed parameters: `{sorted(self.exposed_parameters)}`\")\n\n\n\n    # Components section\n    if indent_level == 0:\n        lines.append(f\"**Components ({len(self.components)}):**\")\n    else:\n        lines.append(f\"{sub_indent}{bullet_style} Components ({len(self.components)}):\")\n\n    for comp, weight in zip(self.components, self.baseline_weights):\n        comp_indent = \"\" if indent_level == 0 else sub_indent\n        lines.append(f\"{comp_indent}{bullet_style} **{comp.name}** (weight: {weight})\")\n\n        if show_component_details and hasattr(comp, 'repr_markdown'):\n            # Use the configurable rendering with proper indentation level\n            comp_md = comp.repr_markdown(\n                indent_level=indent_level + 1 if indent_level == 0 else indent_level + 2,\n                bullet_style=bullet_style,\n                include_summary=include_summary,\n            )\n            lines.append(comp_md)\n        elif show_component_details:\n            fallback_indent = sub_indent if indent_level == 0 else \"  \" * (indent_level + 2)\n            lines.append(f\"{fallback_indent}(No markdown representation available for {comp.name})\")\n\n\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/model_component/","title":"<code>pyForwardFolding.model_component</code>","text":"<p><code>ModelComponent</code>: a product of factors.</p>"},{"location":"reference/api/model_component/#pyForwardFolding.model_component","title":"pyForwardFolding.model_component","text":""},{"location":"reference/api/model_component/#pyForwardFolding.model_component.ModelComponent","title":"ModelComponent","text":"<pre><code>ModelComponent(\n    name: str, factors: List[AbstractUnbinnedFactor]\n)\n</code></pre> <p>A component of a model that contains multiple factors.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of the component.</p> required <code>factors</code> <code>List[AbstractUnbinnedFactor]</code> <p>A list of factors that make up this component.</p> required Notes <ul> <li>Factor names must be unique within a component.</li> </ul> Source code in <code>pyForwardFolding/model_component.py</code> <pre><code>def __init__(self, name: str, factors: List[AbstractUnbinnedFactor]):\n    self.name = name\n    self.factors = factors\n\n    # Ensure factor names are unique\n    factor_names = [factor.name for factor in factors]\n    if len(factor_names) != len(set(factor_names)):\n        raise ValueError(\"Factor names must be unique\")\n</code></pre>"},{"location":"reference/api/model_component/#pyForwardFolding.model_component.ModelComponent.exposed_parameters","title":"exposed_parameters  <code>property</code>","text":"<pre><code>exposed_parameters: Set[str]\n</code></pre> <p>Get the variables exposed by each factor in the model component.</p> <p>Returns:</p> Type Description <code>Set[str]</code> <p>Set[str]: A set of all exposed parameters from all factors.</p>"},{"location":"reference/api/model_component/#pyForwardFolding.model_component.ModelComponent.parameter_mapping","title":"parameter_mapping  <code>property</code>","text":"<pre><code>parameter_mapping: Dict[str, Dict[str, str]]\n</code></pre> <p>Get the mapping of parameters to their corresponding factor names.</p> <p>Returns:</p> Type Description <code>Dict[str, Dict[str, str]]</code> <p>Dict[str, str]: A dictionary mapping parameter names to their factor names.</p>"},{"location":"reference/api/model_component/#pyForwardFolding.model_component.ModelComponent.required_variables","title":"required_variables  <code>property</code>","text":"<pre><code>required_variables: Set[str]\n</code></pre> <p>Collect all variables required by any factor in the model component.</p> <p>Returns:</p> Type Description <code>Set[str]</code> <p>Set[str]: A set containing all required variables from all factors.</p>"},{"location":"reference/api/model_component/#pyForwardFolding.model_component.ModelComponent.evaluate","title":"evaluate","text":"<pre><code>evaluate(\n    input_variables: Dict[str, Union[Array, float]],\n    parameter_values: Dict[str, float],\n) -&gt; Array\n</code></pre> <p>Evaluate all factors in the model component in sequence, updating the output.</p> <p>Parameters:</p> Name Type Description Default <code>input_variables</code> <code>Dict[str, Union[Array, float]]</code> <p>Variables available as inputs to the factors.</p> required <code>parameter_values</code> <code>Dict[str, float]</code> <p>Variables exposed by previously evaluated factors.</p> required <p>Returns:</p> Name Type Description <code>Array</code> <code>Array</code> <p>The modified output vector.</p> Source code in <code>pyForwardFolding/model_component.py</code> <pre><code>def evaluate(\n    self,\n    input_variables: Dict[str, Union[Array, float]],\n    parameter_values: Dict[str, float],\n) -&gt; Array:\n    \"\"\"\n    Evaluate all factors in the model component in sequence, updating the output.\n\n    Args:\n        input_variables (Dict[str, Union[Array, float]]): Variables available as inputs to the factors.\n        parameter_values (Dict[str, float]): Variables exposed by previously evaluated factors.\n\n    Returns:\n        Array: The modified output vector.\n    \"\"\"\n\n    output = backend.array(1.0)\n\n    for factor in self.factors:\n        output *= factor.evaluate(input_variables, parameter_values)\n\n    return output  # type: ignore\n</code></pre>"},{"location":"reference/api/model_component/#pyForwardFolding.model_component.ModelComponent.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>String representation of the ModelComponent object.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>A string representation of the model component.</p> Source code in <code>pyForwardFolding/model_component.py</code> <pre><code>def __repr__(self):\n    \"\"\"\n    String representation of the ModelComponent object.\n\n    Returns:\n        str: A string representation of the model component.\n    \"\"\"\n    lines = []\n    lines.append(f\"ModelComponent: {self.name}\")\n    lines.append(f\"  Factors ({len(self.factors)}):\")\n    for factor in self.factors:\n        # Use the factor's __repr__ method and indent it\n        factor_repr = str(factor).replace('\\n', '\\n    ')\n        lines.append(f\"    {factor_repr}\")\n    lines.append(f\"  Required variables: {sorted(self.required_variables)}\")\n    lines.append(f\"  Exposed parameters: {sorted(self.exposed_parameters)}\")\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/model_component/#pyForwardFolding.model_component.ModelComponent.repr_markdown","title":"repr_markdown","text":"<pre><code>repr_markdown(\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_summary: bool = True,\n) -&gt; str\n</code></pre> <p>Configurable markdown representation of the ModelComponent object.</p> <p>Parameters:</p> Name Type Description Default <code>indent_level</code> <code>int</code> <p>The level of indentation (each level adds 2 spaces). Default is 0.</p> <code>0</code> <code>bullet_style</code> <code>str</code> <p>Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".</p> <code>'-'</code> <code>include_summary</code> <code>bool</code> <p>Whether to include required variables and exposed parameters summary. Default is True.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>A configurable markdown representation of the model component.</p> Source code in <code>pyForwardFolding/model_component.py</code> <pre><code>def repr_markdown(\n    self,\n    indent_level: int = 0,\n    bullet_style: str = \"-\",\n    include_summary: bool = True,\n) -&gt; str:\n    \"\"\"\n    Configurable markdown representation of the ModelComponent object.\n\n    Args:\n        indent_level (int): The level of indentation (each level adds 2 spaces). Default is 0.\n        bullet_style (str): Style for bullet points (\"-\", \"*\", \"+\"). Default is \"-\".\n        include_summary (bool): Whether to include required variables and exposed parameters summary. Default is True.\n\n    Returns:\n        str: A configurable markdown representation of the model component.\n    \"\"\"\n    indent = \"  \" * indent_level\n    sub_indent = \"  \" * (indent_level + 1)\n\n    lines = []\n\n    # Header with appropriate level based on indent\n    if indent_level == 0:\n        lines.append(f\"#### ModelComponent: {self.name}\")\n    else:\n        lines.append(f\"{indent}{bullet_style} **ModelComponent:** {self.name}\")\n\n    # Factors section\n    if indent_level == 0:\n        lines.append(f\"**Factors ({len(self.factors)}):**\")\n    else:\n        lines.append(f\"{sub_indent}{bullet_style} Factors ({len(self.factors)}):\")\n\n    for factor in self.factors:\n        if hasattr(factor, 'repr_markdown'):\n            # Use the configurable rendering with proper indentation level\n            factor_md = factor.repr_markdown(\n                indent_level=indent_level + 1 if indent_level == 0 else indent_level + 2,\n                bullet_style=bullet_style,\n                include_type_in_name=True,\n            )\n            lines.append(factor_md)\n        else:\n            factor_indent = \"\" if indent_level == 0 else sub_indent\n            lines.append(f\"{factor_indent}{bullet_style} **{type(factor).__name__}** (`{factor.name}`) - No markdown representation available\")\n\n    if include_summary:\n        lines.append(\"\")\n        if indent_level == 0:\n            lines.append(f\"**Required variables:** `{sorted(self.required_variables)}`\")\n            lines.append(\"\")\n            lines.append(f\"**Exposed parameters:** `{sorted(self.exposed_parameters)}`\")\n        else:\n            lines.append(f\"{sub_indent}{bullet_style} Required variables: `{sorted(self.required_variables)}`\")\n            lines.append(f\"{sub_indent}{bullet_style} Exposed parameters: `{sorted(self.exposed_parameters)}`\")\n\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"reference/api/statistics/","title":"<code>pyForwardFolding.statistics</code>","text":"<p>Frequentist test machinery: <code>Hypothesis</code>, <code>HypothesisTest</code>, and <code>PseudoExpGenerator</code>.</p>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics","title":"pyForwardFolding.statistics","text":""},{"location":"reference/api/statistics/#pyForwardFolding.statistics.PseudoExpGenerator","title":"PseudoExpGenerator","text":"<pre><code>PseudoExpGenerator(\n    analysis: Analysis,\n    datasets: Dict[str, Dict[str, Union[Array, float]]],\n    parameter_values: Dict[str, float],\n)\n</code></pre> <p>Generator for pseudo-experiments based on an analysis.</p> <p>Uses the analysis to evaluate expected histograms and then generates pseudo-experiments by drawing from a Poisson distribution.</p> <p>Parameters:</p> Name Type Description Default <code>analysis</code> <code>Analysis</code> <p>The analysis to generate pseudo-experiments for.</p> required <code>datasets</code> <code>Dict[str, Dict[str, Union[Array, float]]]</code> <p>The input datasets for the model evaluation.</p> required <code>parameter_values</code> <code>Dict[str, float]</code> <p>The parameter values for the analysis.</p> required Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def __init__(\n    self,\n    analysis: Analysis,\n    datasets: Dict[str, Dict[str, Union[Array, float]]],\n    parameter_values: Dict[str, float],\n):\n    \"\"\"\n    Generator for pseudo-experiments based on an analysis.\n\n    Uses the analysis to evaluate expected histograms and then generates\n    pseudo-experiments by drawing from a Poisson distribution.\n\n    Args:\n        analysis (Analysis): The analysis to generate pseudo-experiments for.\n        datasets (Dict[str, Dict[str, Union[Array, float]]]): The input datasets for the model evaluation.\n        parameter_values (Dict[str, float]): The parameter values for the analysis.\n    \"\"\"\n    self.analysis = analysis\n    self.exp_hists, _ = analysis.evaluate(datasets, parameter_values)\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.PseudoExpGenerator.generate","title":"generate","text":"<pre><code>generate(\n    nexp: int,\n) -&gt; Generator[Dict[str, Array], None, None]\n</code></pre> <p>Generate a pseudo-experiment based on the analysis.</p> <p>Returns:</p> Type Description <code>None</code> <p>Dict[str, Array]: A dictionary mapping component names to generated pseudo-experiments.</p> Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def generate(self, nexp: int) -&gt; Generator[Dict[str, Array], None, None]:\n    \"\"\"\n    Generate a pseudo-experiment based on the analysis.\n\n    Returns:\n        Dict[str, Array]: A dictionary mapping component names to generated pseudo-experiments.\n    \"\"\"\n\n    for _ in range(nexp):\n        obs: Dict[str, Array] = {}\n        for key, hist in self.exp_hists.items():\n            obs[key] = backend.poiss_rng(hist)\n        yield obs\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.Hypothesis","title":"Hypothesis","text":"<pre><code>Hypothesis(\n    name: str,\n    likelihood: AbstractLikelihood,\n    fixed_pars: Optional[Dict[str, float]] = None,\n    minimizer: Optional[MT] = None,\n)\n</code></pre> <p>A class to represent a hypothesis for hypothesis testing.</p> <p>Default parameter values are taking from the likelihood's seeds. Fixed parameters can be provided to create a null hypothesis. The minimizer can be customized by providing a different implementation of AbstractMinimizer. Args:     name (str): The name of the hypothesis.     likelihood (AbstractLikelihood): The likelihood to evaluate the hypothesis.     fixed_pars (Optional[Dict[str, float]]): Parameters to be fixed in the hypothesis. Defaults to None.     minimizer (Optional[AbstractMinimizer]): The minimizer to use for fitting. Defaults to ScipyMinimizer.</p> <p>Initialize the Hypothesis.</p> Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    likelihood: AbstractLikelihood,\n    fixed_pars: Optional[Dict[str, float]] = None,\n    minimizer: Optional[MT] = None,\n):\n    \"\"\"\n    Initialize the Hypothesis.\n    \"\"\"\n    self.name = name\n    self.fixed_pars = fixed_pars if fixed_pars is not None else {}\n    self.likelihood = likelihood\n    self.seeds = likelihood.get_seeds()\n    self.minimizer = minimizer or ScipyMinimizer(likelihood)\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.Hypothesis.model_parameters","title":"model_parameters  <code>property</code>","text":"<pre><code>model_parameters: Set[str]\n</code></pre> <p>Get the model parameters for the hypothesis.</p> <p>Returns:</p> Type Description <code>Set[str]</code> <p>Set[str]: A set of model parameter names.</p>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.Hypothesis.nparams","title":"nparams  <code>property</code>","text":"<pre><code>nparams: int\n</code></pre> <p>Get the number of parameters in the hypothesis.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>The number of parameters.</p>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.Hypothesis.evaluate","title":"evaluate","text":"<pre><code>evaluate(\n    observed_data: Dict[str, Array],\n    dataset: Dict[str, Dict[str, Array | float]],\n    parameter_values: Optional[Dict[str, float]] = None,\n    detailed=False,\n) -&gt; Union[Array, Tuple[Any, Dict[str, Array], Array]]\n</code></pre> <p>Evaluate the hypothesis against observed data.</p> <p>Parameters:</p> Name Type Description Default <code>observed_data</code> <code>Dict[str, Array]</code> <p>The observed data to evaluate.</p> required <code>dataset</code> <code>Dict[str, Dict[str, Array | float]]</code> <p>The input datasets for the model evaluation.</p> required <code>parameter_values</code> <code>Optional[Dict[str, float]]</code> <p>The parameter values for the hypothesis. If not provided, the seeds will be used.</p> <code>None</code> <code>detailed</code> <code>bool</code> <p>If True, return detailed results including the minimizer result.</p> <code>False</code> <p>Returns:</p> Name Type Description <code>float</code> <code>Union[Array, Tuple[Any, Dict[str, Array], Array]]</code> <p>The log-likelihood value for the hypothesis.</p> Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def evaluate(\n    self,\n    observed_data: Dict[str, Array],\n    dataset: Dict[str, Dict[str, Array | float]],\n    parameter_values: Optional[Dict[str, float]] = None,\n    detailed=False,\n) -&gt; Union[Array, Tuple[Any, Dict[str, Array], Array]]:\n    \"\"\"\n    Evaluate the hypothesis against observed data.\n\n    Args:\n        observed_data (Dict[str, Array]): The observed data to evaluate.\n        dataset (Dict[str, Dict[str, Array | float]]): The input datasets for the model evaluation.\n        parameter_values (Optional[Dict[str, float]]): The parameter values for the hypothesis.\n            If not provided, the seeds will be used.\n        detailed (bool): If True, return detailed results including the minimizer result.\n\n    Returns:\n        float: The log-likelihood value for the hypothesis.\n    \"\"\"\n\n    all_fixed_pars = deepcopy(self.fixed_pars)\n\n    if parameter_values is not None:\n        all_fixed_pars.update(parameter_values)\n\n    res = self.minimizer.minimize(observed_data, dataset, all_fixed_pars)\n\n    if detailed:\n        return res\n    else:\n        return res[2]  # Return the log-likelihood value\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.Hypothesis.generate_pseudo_experiments","title":"generate_pseudo_experiments","text":"<pre><code>generate_pseudo_experiments(\n    nexp: int,\n    dataset: Dict[str, Dict[str, Array | float]],\n    parameter_values: Optional[Dict[str, float]] = None,\n) -&gt; Generator[Dict[str, Array], None, None]\n</code></pre> <p>Generate pseudo-experiments for the hypothesis.</p> <p>Parameters:</p> Name Type Description Default <code>nexp</code> <code>int</code> <p>The number of pseudo-experiments to generate.</p> required <code>parameter_values</code> <code>(Optional, Dict[str, float])</code> <p>The parameter values for the hypothesis. If not provided, the seeds will be used.</p> <code>None</code> <p>Returns:</p> Type Description <code>None</code> <p>Generator[Dict[str, Array], None, None]: A generator yielding pseudo-experiments.</p> Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def generate_pseudo_experiments(\n    self,\n    nexp: int,\n    dataset: Dict[str, Dict[str, Array | float]],\n    parameter_values: Optional[Dict[str, float]] = None,\n) -&gt; Generator[Dict[str, Array], None, None]:\n    \"\"\"\n    Generate pseudo-experiments for the hypothesis.\n\n    Args:\n        nexp (int): The number of pseudo-experiments to generate.\n        parameter_values (Optional, Dict[str, float]): The parameter values for the hypothesis. If not provided, the seeds will be used.\n\n    Returns:\n        Generator[Dict[str, Array], None, None]: A generator yielding pseudo-experiments.\n    \"\"\"\n\n    all_parameter_values = (\n        self.seeds | self.fixed_pars if self.fixed_pars else self.seeds\n    )\n    if parameter_values is not None:\n        all_parameter_values.update(parameter_values)\n\n    gen = PseudoExpGenerator(\n        self.likelihood.get_analysis(), dataset, all_parameter_values\n    )\n    return gen.generate(nexp)\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.Hypothesis.asimov_experiment","title":"asimov_experiment","text":"<pre><code>asimov_experiment(\n    dataset: Dict[str, Dict[str, Array | float]],\n    parameter_values: Optional[Dict[str, float]] = None,\n)\n</code></pre> <p>Generate the Asimov dataset for the hypothesis.</p> <p>Parameters:</p> Name Type Description Default <code>dataset</code> <code>Dict[str, Dict[str, Array | float]]</code> <p>The input datasets for the model evaluation.</p> required <code>parameter_values</code> <code>Optional[Dict[str, float]]</code> <p>The parameter values for the hypothesis. If not provided, the seeds will be used.</p> <code>None</code> <p>Returns:     Dict[str, Array]: The Asimov dataset.</p> Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def asimov_experiment(\n    self,\n    dataset: Dict[str, Dict[str, Array | float]],\n    parameter_values: Optional[Dict[str, float]] = None,\n):\n    \"\"\"\n    Generate the Asimov dataset for the hypothesis.\n\n    Args:\n        dataset (Dict[str, Dict[str, Array | float]]): The input datasets for the model evaluation.\n        parameter_values (Optional[Dict[str, float]]): The parameter values for the hypothesis. If not provided, the seeds will be used.\n    Returns:\n        Dict[str, Array]: The Asimov dataset.\n    \"\"\"\n\n    all_parameter_values = (\n        self.seeds | self.fixed_pars if self.fixed_pars else self.seeds\n    )\n    if parameter_values is not None:\n        all_parameter_values.update(parameter_values)\n\n    hist, hist_ssq = self.likelihood.get_analysis().evaluate(\n        dataset, all_parameter_values\n    )\n\n    return hist\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.HypothesisTest","title":"HypothesisTest","text":"<pre><code>HypothesisTest(\n    h0: Hypothesis,\n    h1: Hypothesis,\n    dataset: Dict[str, Dict[str, Array | float]],\n)\n</code></pre> <p>A class to perform hypothesis testing.</p> <p>This class encapsulates two hypotheses (null and alternative) and provides methods to perform hypothesis tests, generate null and alternative distributions, calculate discovery potential, and compute power.</p> <p>Parameters:</p> Name Type Description Default <code>h0</code> <code>Hypothesis</code> <p>The null hypothesis.</p> required <code>h1</code> <code>Hypothesis</code> <p>The alternative hypothesis.</p> required <code>dataset</code> <code>Dict[str, Dict[str, Array | float]]</code> <p>The input datasets for the model evaluation.</p> required <p>Initialize the HypothesisTest with two hypotheses.</p> <p>Parameters:</p> Name Type Description Default <code>h0</code> <code>Hypothesis</code> <p>The null hypothesis.</p> required <code>h1</code> <code>Hypothesis</code> <p>The alternative hypothesis.</p> required Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def __init__(\n    self,\n    h0: Hypothesis,\n    h1: Hypothesis,\n    dataset: Dict[str, Dict[str, Array | float]],\n):\n    \"\"\"\n    Initialize the HypothesisTest with two hypotheses.\n\n    Args:\n        h0 (Hypothesis): The null hypothesis.\n        h1 (Hypothesis): The alternative hypothesis.\n    \"\"\"\n    self.h0 = h0\n    self.h1 = h1\n    self.dataset = dataset\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.HypothesisTest.free_parameters","title":"free_parameters  <code>property</code>","text":"<pre><code>free_parameters: Set[str]\n</code></pre> <p>Get the free parameters for the hypothesis test.</p> <p>Returns:</p> Type Description <code>Set[str]</code> <p>Set[str]: A set of free parameter names.</p>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.HypothesisTest.dof","title":"dof  <code>property</code>","text":"<pre><code>dof: int\n</code></pre> <p>Calculate the degrees of freedom for the hypothesis test.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>The degrees of freedom.</p>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.HypothesisTest.from_likelihood","title":"from_likelihood  <code>classmethod</code>","text":"<pre><code>from_likelihood(\n    likelihood: AbstractLikelihood,\n    dataset: Dict[str, Dict[str, Array | float]],\n    fixed_params: Dict[str, float],\n)\n</code></pre> <p>Create a HypothesisTest from a likelihood.</p> <p>Parameters:</p> Name Type Description Default <code>likelihood</code> <code>AbstractLikelihood</code> <p>The likelihood to create hypotheses from.</p> required <code>dataset</code> <code>Dict[str, Dict[str, Array | float]]</code> <p>The input datasets for the model evaluation.</p> required <code>fixed_params</code> <code>Dict[str, float]</code> <p>The parameters to be fixed in the null hypothesis.</p> required <p>Returns:</p> Name Type Description <code>HypothesisTest</code> <p>The constructed hypothesis test.</p> Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>@classmethod\ndef from_likelihood(cls, likelihood: AbstractLikelihood, dataset: Dict[str, Dict[str, Array | float]], fixed_params: Dict[str, float]):\n    \"\"\"\n    Create a HypothesisTest from a likelihood.\n\n    Args:\n        likelihood (AbstractLikelihood): The likelihood to create hypotheses from.\n        dataset (Dict[str, Dict[str, Array | float]]): The input datasets for the model evaluation.\n        fixed_params (Dict[str, float]): The parameters to be fixed in the null hypothesis.\n\n    Returns:\n        HypothesisTest: The constructed hypothesis test.\n    \"\"\"\n    h0 = Hypothesis(\n        name=\"H0\",\n        likelihood=likelihood,\n        fixed_pars=fixed_params,\n    )\n    h1 = Hypothesis(name=\"H1\", likelihood=likelihood)\n\n    return cls(h0, h1, dataset)\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.HypothesisTest.test","title":"test","text":"<pre><code>test(\n    observed_data: Dict[str, Array],\n    parameter_values: Optional[Dict[str, float]] = None,\n) -&gt; float\n</code></pre> <p>Perform a hypothesis test against observed data.</p> <p>Parameters:</p> Name Type Description Default <code>observed_data</code> <code>Dict[str, Array]</code> <p>The observed data to test against.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>The p-value of the hypothesis test.</p> Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def test(\n    self,\n    observed_data: Dict[str, Array],\n    parameter_values: Optional[Dict[str, float]] = None,\n) -&gt; float:\n    \"\"\"\n    Perform a hypothesis test against observed data.\n\n    Args:\n        observed_data (Dict[str, Array]): The observed data to test against.\n\n    Returns:\n        float: The p-value of the hypothesis test.\n    \"\"\"\n\n    llh0 = cast(\n        float,\n        self.h0.evaluate(\n            observed_data, self.dataset, parameter_values, detailed=False\n        ),\n    )\n    llh1 = cast(\n        float,\n        self.h1.evaluate(\n            observed_data, self.dataset, parameter_values, detailed=False\n        ),\n    )\n\n    dllh = 2 * (llh0 - llh1)\n\n    return dllh\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.HypothesisTest.null_dist","title":"null_dist","text":"<pre><code>null_dist(nexp: int) -&gt; List[float]\n</code></pre> <p>Generate the null distribution for the hypothesis test.</p> <p>Parameters:</p> Name Type Description Default <code>nexp</code> <code>int</code> <p>The number of pseudo-experiments to generate.</p> required <p>Returns:</p> Type Description <code>List[float]</code> <p>List[float]: A list of p-values from the null distribution.</p> Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def null_dist(self, nexp: int) -&gt; List[float]:\n    \"\"\"\n    Generate the null distribution for the hypothesis test.\n\n    Args:\n        nexp (int): The number of pseudo-experiments to generate.\n\n    Returns:\n        List[float]: A list of p-values from the null distribution.\n    \"\"\"\n\n    ts_vals = []\n\n    for exp in self.h0.generate_pseudo_experiments(nexp, self.dataset):\n        ts_vals.append(self.test(exp))\n\n    return ts_vals\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.HypothesisTest.alt_dist","title":"alt_dist","text":"<pre><code>alt_dist(\n    nexp: int,\n    parameter_values: Optional[Dict[str, float]] = None,\n) -&gt; List[float]\n</code></pre> <p>Generate the null distribution for the hypothesis test.</p> <p>Parameters:</p> Name Type Description Default <code>nexp</code> <code>int</code> <p>The number of pseudo-experiments to generate.</p> required <p>Returns:</p> Type Description <code>List[float]</code> <p>List[float]: A list of p-values from the null distribution.</p> Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def alt_dist(\n    self, nexp: int, parameter_values: Optional[Dict[str, float]] = None\n) -&gt; List[float]:\n    \"\"\"\n    Generate the null distribution for the hypothesis test.\n\n    Args:\n        nexp (int): The number of pseudo-experiments to generate.\n\n    Returns:\n        List[float]: A list of p-values from the null distribution.\n    \"\"\"\n\n    ts_vals = []\n\n    for exp in self.h1.generate_pseudo_experiments(\n        nexp, self.dataset, parameter_values\n    ):\n        ts_vals.append(self.test(exp))\n\n    return ts_vals\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.HypothesisTest.discovery_potential","title":"discovery_potential","text":"<pre><code>discovery_potential(\n    nexp_null: int,\n    nexp_alt: int,\n    sigma_level: float = 3,\n    xtol=0.05,\n    maxiter=25,\n    null_dist: Optional[Array] = None,\n) -&gt; Tuple[Array, Array, float]\n</code></pre> <p>Calculate the discovery potential of the hypothesis test.</p> <p>Parameters:</p> Name Type Description Default <code>nexp_null</code> <code>int</code> <p>The number of pseudo-experiments for the null hypothesis.</p> required <code>nexp_alt</code> <code>int</code> <p>The number of pseudo-experiments for the alternative hypothesis.</p> required <code>sigma_level</code> <code>float</code> <p>The sigma level for the test. Defaults to 3.</p> <code>3</code> <code>xtol</code> <code>float</code> <p>The tolerance for root finding. Defaults to 0.05.</p> <code>0.05</code> <code>maxiter</code> <code>int</code> <p>The maximum number of iterations for root finding. Defaults to 25.</p> <code>25</code> <code>null_dist</code> <code>Optional[List[float]]</code> <p>Precomputed null distribution. Defaults to None.</p> <code>None</code> <p>Returns:</p> Type Description <code>Tuple[Array, Array, float]</code> <p>Tuple[Array, Array, float]: A tuple containing the null distribution, alternative distribution, and the parameter value at which the alternative hypothesis is discovered.</p> Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def discovery_potential(\n    self,\n    nexp_null: int,\n    nexp_alt: int,\n    sigma_level: float = 3,\n    xtol=0.05,\n    maxiter=25,\n    null_dist: Optional[Array] = None,\n) -&gt; Tuple[Array, Array, float]:\n    \"\"\"\n    Calculate the discovery potential of the hypothesis test.\n\n    Args:\n        nexp_null (int): The number of pseudo-experiments for the null hypothesis.\n        nexp_alt (int): The number of pseudo-experiments for the alternative hypothesis.\n        sigma_level (float, optional): The sigma level for the test. Defaults to 3.\n        xtol (float, optional): The tolerance for root finding. Defaults to 0.05.\n        maxiter (int, optional): The maximum number of iterations for root finding. Defaults to 25.\n        null_dist (Optional[List[float]], optional): Precomputed null distribution. Defaults to None.\n\n    Returns:\n        Tuple[Array, Array, float]: A tuple containing the null distribution, alternative distribution, and the parameter value at which the alternative hypothesis is discovered.\n    \"\"\"\n\n    if self.dof != 1:\n        raise ValueError(\n            \"Discovery potential is only implemented for one degree of freedom\"\n        )\n\n    free_param = list(self.free_parameters)[0]\n\n    # Calc p-value for sigma level\n    p_value = 1 - backend.norm_sf(sigma_level)\n\n    if null_dist is None:\n        # Generate the null distribution\n        null_dist = backend.array(self.null_dist(nexp_null))\n\n    # Calculate the threshold for discovery\n    threshold = backend.quantile(null_dist, p_value)\n\n    if threshold == backend.max(null_dist):\n        raise ValueError(\n            \"The null distribution does not contain a value below the threshold. \"\n            \"Increase nexp_null or decrease sigma_level.\"\n        )\n\n    param_bounds = self.h1.likelihood.get_bounds()[free_param]\n\n    def pexp_and_fit(x):\n        \"\"\"\n        Generate pseudo-experiments and fit the alternative hypothesis to find the median.\n        \"\"\"\n        pexps = self.h1.generate_pseudo_experiments(nexp_alt, self.dataset, {free_param: x})\n        alt_dist = backend.array([self.test(exp) for exp in pexps])\n        return backend.median(alt_dist)\n\n    alt_param_val = cast(\n        float,\n        sopt.brentq(\n            lambda x: pexp_and_fit(x) - threshold,\n            param_bounds[0],\n            param_bounds[1],\n            xtol=xtol,\n            maxiter=maxiter,\n        ),\n    )\n\n    pexps = self.h1.generate_pseudo_experiments(\n        nexp_null, self.dataset, {free_param: alt_param_val}\n    )\n    alt_dist = backend.array([self.test(exp) for exp in pexps])\n\n    return null_dist, alt_dist, alt_param_val\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.HypothesisTest.discovery_potential_asimov","title":"discovery_potential_asimov","text":"<pre><code>discovery_potential_asimov(\n    sigma_level: float = 3, xtol=0.05, maxiter=25\n) -&gt; float\n</code></pre> <p>Calculate the discovery potential of the hypothesis test using the Asimov dataset.</p> <p>Parameters:</p> Name Type Description Default <code>sigma_level</code> <code>float</code> <p>The sigma level for the test. Defaults to 3.</p> <code>3</code> <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>The discovery potential value.</p> Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def discovery_potential_asimov(\n    self, sigma_level: float = 3, xtol=0.05, maxiter=25\n) -&gt; float:\n    \"\"\"\n    Calculate the discovery potential of the hypothesis test using the Asimov dataset.\n\n    Args:\n        sigma_level (float, optional): The sigma level for the test. Defaults to 3.\n\n    Returns:\n        float: The discovery potential value.\n    \"\"\"\n\n    if self.dof != 1:\n        raise ValueError(\n            \"Discovery potential is only implemented for one degree of freedom\"\n        )\n\n    free_param = list(self.free_parameters)[0]\n    threshold = scipy.stats.chi2.ppf(\n        1 - 2 * scipy.stats.norm.sf(sigma_level), self.dof\n    )\n\n    def asimov_and_fit(x):\n        \"\"\"\n        Generate the Asimov dataset and fit the alternative hypothesis to find the median.\n        \"\"\"\n        asimov_exp = self.h1.asimov_experiment(self.dataset, {free_param: x})\n        return self.test(asimov_exp)\n\n    param_bounds = self.h1.likelihood.get_bounds()[free_param]\n\n    alt_param_val = cast(\n        float,\n        sopt.brentq(\n            lambda x: asimov_and_fit(x) - threshold,\n            param_bounds[0],\n            param_bounds[1],\n            xtol=xtol,\n            maxiter=maxiter,\n        ),\n    )\n\n    return alt_param_val\n</code></pre>"},{"location":"reference/api/statistics/#pyForwardFolding.statistics.HypothesisTest.power","title":"power","text":"<pre><code>power(nexp: int, sigma_level: float = 3) -&gt; float\n</code></pre> <p>Calculate the power of the hypothesis test.</p> <p>Parameters:</p> Name Type Description Default <code>nexp</code> <code>int</code> <p>The number of pseudo-experiments to generate.</p> required <code>sigma_level</code> <code>float</code> <p>The sigma level for the test. Defaults to 3.</p> <code>3</code> <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>The power of the hypothesis test.</p> Source code in <code>pyForwardFolding/statistics.py</code> <pre><code>def power(self, nexp: int, sigma_level: float = 3) -&gt; float:\n    \"\"\"\n    Calculate the power of the hypothesis test.\n\n    Args:\n        nexp (int): The number of pseudo-experiments to generate.\n        sigma_level (float, optional): The sigma level for the test. Defaults to 3.\n\n    Returns:\n        float: The power of the hypothesis test.\n    \"\"\"\n\n    null_dist = backend.array(self.null_dist(nexp))\n    threshold = backend.quantile(null_dist, 1 - backend.norm_sf(sigma_level))\n\n    alt_dist = backend.array(self.alt_dist(nexp))\n\n    # Calculate the power as the fraction of alternative distribution above the threshold\n    power = backend.mean(alt_dist &gt; threshold)\n\n    return cast(float, power)\n</code></pre>"}]}