# `pyForwardFolding.factor`

All per-event (`AbstractUnbinnedFactor`) and per-bin (`AbstractBinnedFactor`) factor classes, plus
the `FACTORSTR_CLASS_MAPPING` registry used by YAML config dispatch.

See the [Factor reference](../factors.md) for a per-factor summary of parameters and required
variables.

::: pyForwardFolding.factor
