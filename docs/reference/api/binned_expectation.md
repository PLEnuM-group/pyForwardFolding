# `pyForwardFolding.binned_expectation`

A `BinnedExpectation` pairs one or more `(dataset_key, Model)` tuples with a binning, a lifetime,
and (optionally) a list of binned factors.

::: pyForwardFolding.binned_expectation
